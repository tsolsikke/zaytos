---
description: カーネルの観測の方針（シリアルログ最優先とパニックポリシー）。kernel を読むときに入る
paths:
  - "kernel/**/*.rs"
  - "common/**/*.rs"
---

## シリアルログ最優先

**本体は ADR-0003 にある。**

## パニックポリシー: Halt and Dump

**本体は ADR-0004 にある。** **Ring 0 に限る**のは同 ADR の Addendum（S8）。

**ここに規則を写さない。** 手掛かりを足すと、それ自体が古くなる側になる——
**この 2 つは、写していたために ADR と二重になっていた**（実測。2026-08-28）。
