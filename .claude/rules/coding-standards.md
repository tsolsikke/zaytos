---
description: コーディング規約の要点（本体は docs/coding-standards.md）。コードを読むときに入る
paths:
  - "kernel/**/*.rs"
  - "common/**/*.rs"
  - "bootloader/**/*.rs"
  - "xtask/**/*.rs"
---

## コーディング規約（要点）

- **`unsafe` には必ず `// SAFETY:` コメント。**
  本体は `CLAUDE.md` の「絶対に守るべきルール」にある。
- **ホスト側テスト可能なロジックは `#[cfg(test)]` の単体テストを添える。**
- 詳細な規約は `docs/coding-standards.md` に、必要が生じた項目から追記する。
  **コードそのものの書き方は、そちらの「コードの書き方」にある。**
  ここに複製しない——**一覧を括弧で再掲していたが、それも写しだった**
  （実測。2026-08-28）
