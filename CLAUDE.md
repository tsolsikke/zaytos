# CLAUDE.md — ZaytOS 開発協働ガイド

このファイルは、Claude Code / Codex 等の AI エージェントが ZaytOS の
コーディングを行う際に**必ず最初に読む**べき運用ルールである。
セッションをまたいでも設計方針がぶれないようにするための基準文書であり、
本ファイルと矛盾する指示が出た場合は、原則として本ファイルを優先し、
矛盾点を人間（運用者）に確認すること。

---

## 0. プロジェクト概要

- **名称**: ZaytOS（ゼイトス）
- **目的**: 学習目的を主軸とした、x86_64 向けホビー OS の自作
- **開発言語**: Rust
- **開発体制**: 設計は人間 + Claude（対話）で決定し、実装は Claude Code / Codex が担う
- **開発スタイル**: 細く長く（長期・低速・堅牢優先）。短期的な機能量より、
  一つ一つを理解しながら確実に積み上げることを優先する。

---

## 1. 確定済みの設計制約（変更には ADR が必要）

以下は既に決定済みであり、AI が独断で覆してはならない。
変更が妥当と考える場合は、実装を進める前に必ず人間に提起し、
新規 ADR（`docs/adr/`）として記録した上で行うこと。

| 項目 | 決定 |
|---|---|
| カーネル方式 | モノリシックカーネル |
| メモリ保護 | あり（ページング、ユーザー/カーネル権限分離を持つ） |
| カーネルの配置 | higher-half（`0xFFFFFFFF80000000`。非 PIE。ADR-0024） |
| 対象アーキテクチャ | x86_64 のみ（他アーキテクチャへの抽象化は行わない） |
| ファームウェア境界 | UEFI（`uefi` クレート / `x86_64-unknown-uefi` ターゲット） |
| コア数 | マルチコア（SMP）。BKL で守る（ADR-0023 / ADR-0026） |
| ユーザー空間 | Ring 3 のプロセス。アドレス空間はプロセスごと（ADR-0022） |
| システムコール | `int 0x80`。番号は Linux x86-64 から採る（ADR-0020） |
| ファイルシステム | ext2（読み取り。ADR-0025）。VFS は `inode` / `file` まで |
| テスト環境 | QEMU + OVMF（パッケージ導入。EDK2 のビルドシステムは使わない） |
| 開発環境 | WSL2（Ubuntu 系）+ WSLg |

詳細な背景と却下した代替案は `docs/architecture.md` および
`docs/adr/` を参照すること。

---

## 2. 絶対に守るべきルール（Absolute Rules）

1. **本ファイルと `docs/architecture.md` を読まずにコードを書き始めない。**
   セッション開始時、両ファイルの現在の内容を確認してから作業に入る。
   **あわせて、走っている検査プロセスが無いことを確認する**
   （`ps -eo pid,comm | grep -E 'qemu|xtask'`）。前セッションの
   `cargo xtask check --full` が生き残っていることがある。`target/` と
   `disk0.img` を共有するので、並走させると両方が汚れる。止め方は §14.1 にある。

2. **確定済み設計制約（§1）を独断で変更しない。**
   変更が必要と判断したら、実装より先に人間へ提起し ADR 化する。

3. **`unsafe` ブロックは最小範囲に限定し、必ず理由をコメントで明記する。**
   なぜ safe に書けないのか、何を前提に安全性が保証されるのか（invariant）を
   `// SAFETY:` コメントとして各 `unsafe` に添える。

4. **シリアルログを最優先の観測手段として常に維持する。**
   新機能を追加する際、その動作が serial ログで追跡可能であることを担保する。
   画面描画が未実装の段階でも、状態は serial に出力できる状態を保つ。

5. **ハードウェア依存部と純粋ロジックを分離し、ロジックはホスト上でテスト可能にする。**
   スケジューリング判断、アロケータのビット演算、データ構造操作などは、
   割り込み・MMIO 等のハード依存から切り離し、`cargo test`（ホスト実行）で
   検証できる形に保つ。§6 参照。

6. **人間のレビューが必須の変更は、AI 単独で確定・マージしない。**（§5 参照）

7. **設計判断は ADR に、実装トラブルは troubleshooting に、必ず記録する。**（§7 参照）

---

## 3. デバッグ・観測方針

### 3.1 シリアルログ最優先

**本体は `.claude/rules/kernel-observability.md` へ移した（§6の整備。T-1）。**
### 3.3 QEMU の「沈黙の失敗」を必ず可視化する

**本体は `.claude/rules/qemu-and-build.md` へ移した（§6の整備。T-1）。**
## 4. 実装ループの標準手順

**本体は `.claude/skills/verify-change/SKILL.md` へ移した（§6の整備。T-1）。**
## 5. 人間レビューが必須の変更

以下に該当する変更は、AI が単独で確定・マージしてはならない。
実装案・差分・関連ログを提示し、人間の確認を得ること。

- `unsafe` ブロックを新規に含む、または既存の `unsafe` の前提を変える変更
- 割り込みハンドラ（IDT）・例外ハンドラに関わる変更
- ページテーブル操作・仮想メモリマッピングに関わる変更
- GDT / TSS / セグメンテーション周りの変更
- クリティカルセクション（`cli`/`sti` による割り込み禁止区間）の設計変更
- マイルストーン到達時（ADR を書くタイミングと重なる）
- §1 の確定済み設計制約に影響する変更

上記以外の、純粋ロジック・ドキュメント・テスト・ビルドスクリプトの
変更は、ループ内で自律的に進めてよい。

---

## 6. 同期方針（SMP。BKL）

**本体は `.claude/rules/kernel-synchronisation.md` へ移した（§6の整備。T-1）。**
## 7. 記録の運用

**本体は `.claude/skills/record-a-decision/SKILL.md` へ移した（§6の整備。T-1）。**
## 8. コーディング規約（要点）

**本体は `.claude/rules/coding-standards.md` へ移した（§6の整備。T-1）。**
## 9. ビルド環境の再現性

**本体は `.claude/rules/qemu-and-build.md` へ移した（§6の整備。T-1）。**
## 10. スコープ外（今は手を出さない）

以下は将来の楽しみとして保留する。先回りの抽象化・作り込みをしない。

- ARM / RISC-V 等、x86_64 以外への移植・抽象化
- ローダブルカーネルモジュール（動的ドライバ読み込み）
- 独自ファイルシステムの設計（既存の形式に従う。いまは ext2。ADR-0025）
- ネットワークスタック
- POSIX 互換・Linux syscall ABI 互換（**番号と構造体の形は Linux から採るが、
  互換を目標にしない**）
- ジョブ制御（シェルは組み込みの `exit` と外部コマンドの実行まで）

「これらに今は手を出さない」という判断自体が、本プロジェクト最大の設計判断である。
スコープ拡大の提案は歓迎するが、提案にとどめ、独断で着手しないこと。

**この一覧から2つ外した。** **マルチコア（SMP）は S1 から S8 で実装した。**
**ファイルシステムは「FAT など」と書いていたが、ADR-0025 で ext2 を選び、
S10 で読み取りを実装した。** 現在地は `docs/roadmap.md` にある。

---

## 11. 人間による実機確認
マイルストーン（M1, M2-x, M3...）完了時の報告には、以下を含めること。
- 運用者自身が手元で実行するための正確なコマンド
- そのとき期待される出力（シリアルログの要点、画面表示の有無）
- 確認してほしい観点（何が見えれば成功か）

AI 側の実行結果報告のみで完了としない。運用者による確認を経て
commit・次ステップへ進む。

---

## 12. 成果物の記述方針

本リポジトリの公開成果物（コミットメッセージ、README、docs、
コード内コメント、PR 説明）には、AI の関与を示す署名・メタデータを
含めない。具体的には:

- Co-Authored-By 等の attribution トレーラーを付けない
- "Generated with"、AI ツール名、モデル名を書かない
- 絵文字を使わない（コミットメッセージ、ドキュメント、コメントすべて）
- コミットメッセージは運用者本人が書いたものとして記述する。
  簡潔な事実の記述にとどめ、過度に整った定型文や、
  変更の意義を大げさに説明する書き方を避ける
- 文体は `docs/coding-standards.md` の「文体」に従う（コミットメッセージの
  件名・Markdownドキュメント・コード内コメント）。**規約の本体はそちらにある。**
  ここに複製しない。二重に持つと片方だけが古くなる

このルールは、公開リポジトリを実装内容そのもので評価できる状態に
保つためのものである。**エージェント向けの設定ファイルそのものと、
その公開を決めた記録（`ADR-0031`）は、この制限の対象ではない。**

文体を機械的に変換したときは、変換後に `python3 tools/docstyle.py` を通し、
レンダリング結果が意図どおりに変わっていることを確認する。
**このスクリプトは公開の対象である**（`ADR-0031`）。
**ただし追跡下へ入るまでは、公開文書から参照しない**
（公開文書は、リポジトリに無いファイルを指してはならない）。

### 12.1 リポジトリ監査の手順

**本体は `.claude/skills/audit-the-repository/SKILL.md` へ移した（§6の整備。T-1）。**
### 12.2 エージェント向け設定ファイルの扱い

**本ファイルを含むエージェント向けの設定ファイルは、公開できる水準へ
整えた上でリポジトリへ追加する**（`ADR-0031`）。
**以前は「追跡対象外とし、公開文書から参照しない」としていた。**
決定の理由と却下した案は ADR にある。ここに複製しない。

- **公開しないものは `.git/info/exclude` に残す。** 理由は
  「個人の環境設定を、公開リポジトリの設定に混ぜない」である。
  `.gitignore` ではなく `.git/info/exclude` を使うのは、
  **除外そのものが各人の環境の都合であって、リポジトリの設定ではない**ため
  （**以前の理由は「除外設定自体が痕跡になるため」だった。公開後は当たらない**）
- **公開したものは、docs・README・コード内コメントから参照してよい。**
  追跡下へ入る前のものは参照しない
- **参照は節番号ではなく節の名前で行う。** 節番号は編集で動くが、
  名前は内容が変わらない限り動かない。実際、この体制は既に
  `docs/coding-standards.md` を「回帰チェックの必須条件」
  「一時的な破壊・変更の手順」のように名前で指している
- **節の見出しを変えるときは、変える前の名前でリポジトリ全体を grep する。**
  名前で指しているので、**見出しを変えると参照が切れる。**
  切れても機械は止めない（リンクではないので `docstyle.py` の `S5` も見ない）
- **前節の監査で、キーワード grep が当たる場所が増える。**
  設定ファイル自身と `ADR-0031` がそれで、**想定内である。**
  **想定外なのは、それ以外の場所に AI 関連の語が出た場合である**

---

## 13. コミットの運用

### 13.1 コミットするタイミング

- 各段階（M4-b-1 のような細分化された単位）の完了時、実機検証が通った
  状態で必ずコミットする
- 「動く状態」を残さずに次の危険な実装へ進まない。低レイヤでは
  「昨日まで動いていたのにどの変更で壊れたか分からない」が起きやすく、
  git bisect できる状態の価値が高い
- レビュー必須項目（§5）を含む変更は、運用者の承認を得てからコミットする。
  承認前にコミットしない
- ドキュメントのみの変更、リファクタリングのみの変更は、機能変更と
  混ぜず別コミットにする

### 13.2 コミットの粒度

- 1 コミット = 1 つのまとまった変更。段階の完了と一致させるのが基本
- 純粋なリファクタリング（振る舞いを変えない変更）は単独のコミットにする。
  **本体は `docs/coding-standards.md` の「振る舞いを変えない変更は、単独の
  コミットにする」にある。** ここに理由を複製しない
- 検証のための一時的な変更（毒値の意図的な無効化、パニックの仕込み等）は
  コミットしない。revert してからコミットする

### 13.3 報告の義務

- 段階の完了報告には、必ずコミットハッシュと、そのコミットに含まれる
  変更の範囲を記載する
- コミットしていない場合は「未コミット」と明記する。報告に書かないまま
  済ませない
- コミット後は `git log --oneline -3` と `git status` を確認し、作業ツリーが
  クリーンであることを報告する

### 13.3.1 報告の形式

**本体は `.claude/output-styles/zaytos-report.md` へ移した（§6の整備。T-1）。**
### 13.3.2 報告の頻度

**段階が完了してから 1 回だけ報告する。** 1 つの段階に対して報告が
4 回も 5 回も分かれると、全体像を把握するのにかえって手間がかかる。
ここでいう段階とは、M5-a-2-1 のような細分化された単位、または
独立した調査と修正のことである。

- 途中で判断が必要になった場合のみ、その時点で止めて報告してよい。
  「判断を仰ぐ」以外の理由で途中報告をしない
- 背景実行の完了待ちや、その結果の確認だけを内容とする報告はしない。
  完了を待ってから、まとめて次の作業へ進む
- 複数回の実行や試行錯誤があった場合、その経緯は最終報告の中に含める。
  長くなるなら要約してよい

### 13.4 コミットメッセージ

- §12（成果物の記述方針）に従う。トレーラー・絵文字・AI への言及を
  含めない
- 何を変えたかを簡潔に書く。なぜ変えたかは ADR に書く（メッセージで
  設計判断を長々と説明しない）
- 運用者本人が書いたものとして記述する

**件名だけで足りるなら、1 行で終える。** **複数行は読むのが大変なので、本文は必要なときだけ書く。** **本文を書くのは、件名から「なぜそうしたか」が読めないときだけである**——設計の判断とその根拠、測って結論が変わったこと、検査の弱点とその手当て、が目安である。

**本文を書く場合は 2 行から 5 行にする。** 件名、空行、本文の順で、
**本文は 5 行を超えない。** **2 行目は必ず空ける**（`git` の慣行。
実測で、本文を持つ全コミットが既にそうなっている）。

**件名の接頭辞は次の 7 つに限る**（Semantic Commit Messages）。

| 接頭辞 | 使う場面 |
|---|---|
| `feat` | 振る舞いが増える |
| `fix` | 壊れていたものを直す |
| `docs` | 文書だけの変更（`docs/`・`README`・`CLAUDE.md`） |
| `refactor` | 振る舞いを変えずに形を変える |
| `test` | 検査・破壊 feature の追加や変更 |
| `style` | 整形だけ（`cargo fmt` の適用など） |
| `chore` | 上のどれでもない雑務 |

**実測で決めた。** 履歴に出るのはこの 7 つだけである
（`docs` 307 / `feat` 172 / `refactor` 28 / `fix` 28 / `test` 20 /
`style` 1 / `chore` 1。件数は増減するので、比だけを見ること）。

**標準の集合から 3 つを外した。** `perf` は**測って速くする変更がまだ無い**
（性能の項目は `deferred-decisions` にあり、どれも未着手である）。
`build` と `ci` は、**この体制ではビルドの変更が `xtask` の変更として出る**
——`cargo xtask check` そのものが CI の代わりなので、**`test` と区別が付かない。**
**要る場面が出たら足すこと。** 外した理由をここに書いておくのは、
**「標準に在るのに無い」を見た人が、抜けなのか判断なのかを分けられるようにするためである。**

本文に書くのは**件名から読めないこと**だけである。

- 採った理由が自明でないなら、その理由を 1 行
- 却下した案があるなら、**その所在**を 1 行（内容は書かない。ADR か docs を指す）
- 踏んだものがあるなら、それが何だったかを 1 行

**書かないもの**は 5 つある。

- 件名の言い換え
- 変更したファイルの一覧（`git show --stat` が出す）
- 実測値・判定行・コマンドの出力
- 規約や族の内容そのもの（`docs/coding-standards.md` を指すだけでよい）
- 確かめた手順

**理由は、コミットメッセージが後から直せないことである。**
公開後は履歴の書き換えができないので、**長く書くほど、古くなった記述が消せない形で残る。**
**実測値と判定行は docs か報告に置く**——そちらは直せる。

---

## 14. シェルコマンドの制約

低レイヤ開発では QEMU 実行やログ待ちで待機処理を書く場面が多いが、
待機の書き方を誤ると「ハングしているのか待っているのか」が区別できなくなり、
放置事故につながる。以下を守ること。

- **上限のない待機ループを書かない。** `until` / `while` による待機は、
  必ず最大試行回数または `timeout` で上限を設ける。上限に達したら、
  待っていた条件が満たされなかった旨を明示して終了する。
  条件が永久に成立しない状況（パイプが閉じてログが止まる、対象プロセスが
  既に死んでいる等）は普通に起きる
- **長時間動作しうるコマンドには `timeout` を付ける。**
  QEMU 実行、ビルド、テストいずれも対象
理由: 上限のないループはハングと区別がつかない。実際に空転が発生している。

### 14.1 プロセスを止める手順

**本体は `.claude/skills/stop-a-process/SKILL.md` へ移した（§6の整備。T-1）。**
### 14.3 一時的な破壊・変更の手順

**本体は `.claude/rules/temporary-changes.md` へ移した（§6の整備。T-1）。**
### 14.2 バックグラウンド実行の手順

`コマンド &` を使わない。完了を追跡できず、出力も取り逃がす。
シェルの `&` ではなく、**Bash ツールの `run_in_background` を使う**。
完了すると通知が届き、出力はファイルへ残る。

待機が必要な場合も、`sleep` を並べずに `run_in_background` で
条件付きの `until` ループを投げる。上限は §14 の規則どおり付ける。

**シェルの `&` を使った違反が 1 回発生している。** §14.1 と同じ形で
記録しておく。禁止だけを書いても作業中には思い出せず、記録が無ければ
次に踏んだときに反復ではなく単発の失敗として扱われる。§14.1 の
`pkill` が 2 回目を「この節を読んだ上で」踏んだのと同じ経路である。

害は 3 つあり、いずれも「気づけない」形で出る。

- 完了が追跡されない。ターンが終われば、走っているかどうかを見る手段が無い
- 出力を取り逃がす。失敗しても、失敗したという事実自体が残らない
- **上の 2 つの結果として、失敗が成功と区別できない。** §14 が
  「ハングと待ちが区別できない」を禁じているのと同じ理由である

判別は 1 点で足りる。**行末に `&` を書こうとしたら止まること。**
背景で走らせたいと考えた時点で、シェルではなく Bash ツールの
`run_in_background` を選ぶ。`&` を書いてよい例外は無い。

**2 回目が発生した（S4-b-4）。** BKL のタイムアウトが発火するまでの実時間を
測るために、QEMU を `&` で起こして前景のループで待った。**上の「1 回」を
読んだ上で踏んでいる。** §14.1 の `pkill` と同じ経路である。

害は出なかった。前景のループで完了を待ち、`kill` で止め、出力もファイルへ
残した。**害が出なかったのは、たまたま「待つ形」を同じコマンドの中に書いたから
であって、規則が守られたからではない。**

**構造では防げない。規律で守る。** §14.1 の `pkill` にはブラケット記法という
「道具の側で安全にする」書き換えがあったが、**`&` にはそれが無い。**
検出するなら自分のコマンドを自分で検査する形になり、**書く前に自分で止まる
以外の経路が存在しない。** したがってこの節は規律であり、
**破られた回数を数え続けるしかない。**

**3 回目が発生した（S12-a）。** `pmemsave` が仮想と物理のどちらを取るかを
切り分けるために、QEMU を `&` で起こした。**害は出なかったが、出なかったのは
socket のパス長で相手側が先に落ちたからで、規則が守られたからではない。**

**3 回に共通する形が 1 つある。** どれも**正規の経路の外で、一回限りの切り分けを
していた**——起こす・話す・止めるを**1 つのコマンドに書きたくなる**場面である。
**`run_in_background` が使いにくいのではない**（実際、取り直しはそれで済んだ）。
**1 つのコマンドに収めたい誘因のほうが原因である。** **切り分けで QEMU を
起こすと気づいたら、その時点で 2 手に分ける**——起こすのは `run_in_background`、
話すのは次のコマンドである。

**5 回目が発生した（VIEW-b の締め）。** `--full` を通し直すときに `nohup ... &` と
書いた。**Bash ツールの `run_in_background` と同時に使ったので `&` は不要だった。**
害は出なかったが、**harness が 5 秒で「完了」と通知し、実体は切り離されたまま
走っていた**——§14.2 が書いている「完了が追跡されない」がそのまま出た。
**§14.1 の手順で PID を特定して止め、`&` を書かずに起こし直した。**

**3 回に共通する形（正規の経路の外で、一回限りの切り分け）とは違う。**
**今回は正規の経路の中に居ながら、余分な `&` を書いた。** **意志で守る段階は
過ぎている**——**行末の `&` を機械で拒む（PreToolUse の hook）を、`.claude/` の
整備で最優先の項目にすること**（運用者の指示。2026-08-24）。
