//! PS/2 キーボード（M4-e）。
//!
//! - [`decode`]: スキャンコードから文字への変換（**純粋ロジック**、
//!   ホスト `cargo test` で検証）。ハードウェアに一切触れない。
//!
//! ハードウェア依存部（i8042 の検証・IRQ1 ハンドラ・リングバッファ）は
//! M4-e の後半で追加する。先に純粋ロジックだけを完成させておけば、実機で
//! 問題が出たときに「デコードは正しいのだからハードウェア側だ」と切り分け
//! られる。

pub mod decode;
