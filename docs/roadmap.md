# ZaytOSロードマップ

マイルストーンの現在地を管理する。
細く長く進める前提のため、各段階は「ここで中断しても死なない」区切りになるよう意識する。

M6より先の興味の方向は`vision.md`に別途書いてある。
あちらは構想であって決定ではなく、現在の設計判断を縛らない。
先回りの抽象化の根拠にはならない。

状態: [ ]未着手 / [~]進行中 / [x]完了

---

## M0. 環境構築 [x]
- [x] WSL2 + Rustツールチェイン（`x86_64-unknown-uefi`ターゲット追加）
- [x] QEMU + OVMF導入
- [x] `cargo xtask run`でQEMUが起動する骨組み
- [x] `rust-toolchain.toml` / `Cargo.lock`固定

## M1. UEFI Hello World [x]
- [x] **シリアルログ出力（COM1）を最優先で確立**（`common/src/serial.rs`, `common/src/log.rs`）
- [x] パニックハンドラ雛形（`bootloader/src/panic.rs`; RSP + `PanicInfo`ダンプ + halt。GPRフルダンプは行わない。M4-bで例外側だけが解禁され、`panic!()`については据え置きが確定した。ADR-0018 §4）
- [x] UEFIアプリとして起動し、画面に文字列を出力（`bootloader/src/main.rs`）
- [x] パニックハンドラの回帰チェックを常設化（`cargo xtask run --panic-test`）

## M2. メモリ管理の基礎 [x]

M2-0a〜M2-0cでbootloader/kernel分離（ADR-0008）とELFローダーを実装した結果、当初の「UEFIメモリマップ取得」「ExitBootServices」はM2-0cの中で実質的に完了している。
以降はM2-c（自前ページテーブル構築）・M2-d（物理フレームアロケータ）の**実装順序を入れ替えている**: ページテーブルを構築するにはページテーブル自身を置く物理フレームが必要であり、それを供給するのがフレームアロケータであるため、フレームアロケータを先に実装しなければ成立しない。

- [x] M2-0a: crate再編（`kernel` → `bootloader`へ改名、`serial`/`log`/`cpu`を`common`クレートへ共有化、ADR-0008）
- [x] M2-0b: kernelクレート新規作成（`x86_64-unknown-none` + リンカスクリプト、最小カーネル。リンクアドレスはADR-0009）
- [x] M2-0c: ELFローダー実装（bootloaderがkernel.elfをロードし、GOPフレームバッファ情報取得 → ExitBootServices → BootInfo経由でkernelへジャンプ。UEFIメモリマップ取得・ExitBootServicesはここに含まれる）
- [x] M2-a: UEFIメモリマップ取得（M2-0cで完了。`BootInfo.memory_map`経由でkernelへ引き渡し、内容をシリアルへログ出力済み）
- [x] M2-b: ExitBootServices実行（M2-0cで完了。以降もシリアルログが生存することを確認済み）
- [x] M2-c: 物理フレームアロケータ（`BootInfo`のメモリマップを解析し、空き物理フレームを管理。範囲リスト方式、ADR-0011）。「空き」の判定は`EfiConventionalMemory`のみとし、`EfiBootServicesCode`/`Data`はADR-0010により当面除外。kernel本体・`BootInfo`・メモリマップバッファは予約済みとして除外する。ロジックはホスト`cargo test`で検証済み
- [x] M2-d: 自前ページテーブル構築（M2-cのフレームアロケータから物理フレームを受け取って構築する）。d-1でCR3を切り替えずに新テーブルを構築・検証し、d-2で実際にCR3を切り替えて実機確認済み（マッピングの穴に関する申し送りは §6.4参照）
- [x] M2-e: カーネルヒープ（`alloc`クレート有効化）。連結リストアロケータ（ADR-0012）。`Vec`/`Box`/`String`の実機動作、二重解放検出、ヒープ枯渇時のパニック合流を確認済み

### 将来の検討項目（今は着手しない）
- **BootServices領域の回収**（ADR-0010）: `EfiBootServicesCode`/`Data`を空き物理メモリとして回収することは、自前のページテーブル・スタック・GDT/IDTがすべて揃うまで（M4以降が目安）保留する。

## M3. 画面描画 [x]

描画方式はADR-0013。
扱うのは`Rgb`/`Bgr`の32bppのみで、bootloaderから渡された形状はkernel側で検証してから使う。

- [x] GOP（Graphics Output Protocol）でフレームバッファ取得（情報自体はM2-0cで`BootInfo.framebuffer`として取得・kernelへ引き渡し済み）
- [x] M3-a: 描画の基盤（`kernel::graphics`）。形状の検証、色とピクセルフォーマットの変換、点・矩形の描画と画面外の切り詰め。起動時テストパターンを実機で目視確認済み
- [x] M3-b: ビットマップフォントによる文字描画（GNU Unifont、ADR-0016）。半角8x16と全角16x16を最初から区別し、検索は`char`を鍵にする。現在の収録はASCIIとU+FFFDのみで、未収録文字はU+FFFDへフォールバックする。日本語はhexに範囲を追記して`cargo xtask gen-font`を再実行すれば追加できる
- [x] M3-c: コンソール抽象（`core::fmt::Write`、スクロール）。設計はADR-0017
  - [x] M3-c-1: 純粋ロジック（カーソルと桁送り、未転送範囲の追跡）。全角の折り返し・制御文字・ダーティ矩形の結合をホスト`cargo test`で検証
  - [x] M3-c-2: バックバッファの確保とダーティ矩形フラッシュ、`core::fmt::Write`実装（unsafeを含む）。起動時テストパターンは`gfx-test-pattern` feature（`cargo xtask run --gfx-test`）へ移した
  - [x] M3-c-3: 起動ログを画面にも出す統合。シリアルは独立を維持する。コンソールはフレームアロケータと新ページテーブルを必要とするため起動シーケンスの途中でしか作れず、それより前のログはシリアルにしか出ない。画面の先頭に省略された行数を出して誤解を防ぐ

## M4. 割り込み [x]

着手前に`docs/deferred-decisions.md`の「M4で解禁・見直しになるもの」を通しで読むこと。
M4は複数の保留項目の解禁条件を同時に満たす節目であり、一覧を見ないと解禁できる状態に気づかないまま進むことになる。

設計はADR-0018。
`sti`はM4-dの1箇所だけで行い、その時点で下の層がすべて実機検証済みになるよう下から積む。
例外は`IF`に関係なく発生するため、IDTと例外ハンドラは割り込みを有効化しないまま検証しきれる。

- [x] M4-a: GDT / TSS / 自前カーネルスタック（`.bss`の静的領域。通常64KiB、ダブルフォルト用IST1が16KiB）。切り替え後のRSP・ローカル変数の位置・旧スタック不参照・読み書き・カナリアを実機で検証済み
- [x] M4-b: IDT / 例外ハンドラ / 例外時のGPRフルダンプ
  - [x] M4-b-1: IDTエントリの符号化、全256ベクタへの割り込みゲート、`lidt`/`sidt`の読み戻し検証、`--exception-test divide-by-zero`と`invalid-opcode`（シリアルと`-d int`の突き合わせ）
  - [x] M4-b-2: GPRフルダンプ、CR2、エラーコードの解釈、ISTを使うダブルフォルト（`--exception-test page-fault` / `double-fault`）。GPRの並び順は既知値15個で実値検証。ダブルフォルトは #PFゲートのPresentビットを落として誘発し、IST1の使用とCPU Reset非増加を確認
- [x] M4-c: クリティカルセクション、`Locked<T>`の差し替え、PIC再マップ
  - [x] M4-c-1: `InterruptGuard`（RAIIの`cli`/`sti`）。入れ子でも余計に有効化しないこと、Dropで元の状態へ戻ることを実機のRFLAGSで確認
  - [x] M4-c-2: `Locked<T>`を`common::critical`へ移し、取得中だけ割り込みを禁止する形に差し替え。前提が「割り込み常時禁止」から「取得中は割り込み禁止」に変わった（ADR-0012 Addendum）。二重取得検出つき（`--critical-test double-lock`）
  - [x] M4-c-3: PICを0x20-0x2Fへ再マップし、全IRQをマスク。再マップ前のIMRを記録し、再マップ後はIMRを読み戻して照合する。ベクタオフセット自体はICW2が書き込み専用のため検証できず、M4-dで最初のタイマ割り込みがベクタ0x20で届くまで未検証のまま持ち越す（ADR-0018 Addendum）。IF=1から入る復元経路もここで確認済み（`--critical-test restore-enabled`）
- [x] M4-d: タイマ割り込み（**PIT**。ここで初めて`sti`）。APICはACPIを辿る必要があり、切り分け軸が増えるためM4の範囲外とする。「`sti`すること」と「割り込みが実際に来ること」を別々に検証するため2段階に分けた
  - [x] M4-d-1: `sti`できる状態を作る（タイマは動かさない）。IRQ用の復帰経路（GPR退避・復元 + `iretq`、例外経路とは独立）、DS/SSの読み戻し、`sti`前7項目の実行時検証とfail-fast、全IRQをマスクしたままの`sti`。スタックの16バイト境界は実測値で検証し、わざと外して両経路で発火することを確認済み（ADR-0018 Addendum 2）
  - [x] M4-d-2: タイマを動かす。PITを100Hz（分周値11932、実周波数99.998Hz）で設定し、IRQ0を解禁してEOIを発行する。ADR-0018 §2の項目4（PICのベクタオフセット）と項目7（EOI）がVerifiedへ移った。前者は最初のティックがベクタ0x20で届いたことと`alt-offset-test`（0x30-0x3Fへ再マップしても追随すること）で、後者はティックが増え続けることと`no-eoi-test`（落とすと1回で止まること）で証明した。スプリアス割り込みの判定も実装（純粋ロジックはホストテストで固定）。通常起動でタイマが動くため、カーネルは停止しなくなった。到達点の目印は`kernel: halting`から「最初のティックがベクタ0x20で届いた」行へ変えた
- [x] M4-e: キーボード割り込み（PS/2、IRQ1）。i8042のコンフィグバイトを読んで翻訳（セット1）と割り込みが有効かを確認してから解禁する。ハンドラはリングバッファへ積むだけで、**バッファが満杯でもデータポートは必ず読む** （読まないと以後のIRQ1が来ず、症状がEOI忘れと区別できなくなる）。変換は純粋ロジックとしてホストテストで固定。エコーはメインループが行う。`cargo xtask run --interrupt-test keyboard`でmonitorの`sendkey`により自動検証する（キーリピートだけは`sendkey`が模擬しないため手動確認）

## M5. マルチタスク（プリエンプティブ）[x]

M5-a〜M5-f-2まで完了。**プロセス別アドレス空間（M5-f-3）はADR-0022で「higher-half B完了後の独立マイルストーン」として切り出したため、M5本体の完了判定には含めない**（下記の別項目として残す。B完了で解禁された）。

段階分割と方式はADR-0019。
ページング拡張を先頭に置いて下から積む。
途中に`PhysAddr`/`VirtAddr`の型分離とhigher-half移行（ADR-0024でADR-0009をSupersede。
物理メモリへのアクセスはADR-0021のdirect physical map）を挟む。
システムコールのレジスタ規約はADR-0020で決定済み。

- [x] M5-a: `PageTableBuilder`のページ分割・アンマップ対応
  - [x] M5-a-1: エントリのビット操作を`paging::entry`へ分離（2MiBと4KiBで位置が違うPATの移送を含む。ホストテストで固定）。稼働中テーブルの読み戻し（`paging::active`）を実装し、起動時に計画と突き合わせる。CR4.PGEが無効でGビットも未使用のため、CR3リロードでTLBを全部追い出せることを実測で確認
  - [x] M5-a-2: 2MiBページの分割・アンマップ・TLB無効化
    - [x] M5-a-2-1: `split_huge_page` / `unmap_4kib` と、影響のない領域での一巡。分割はPTを確保・512エントリを書いてからPDエントリを8バイト1回で差し替え、CR3リロードでTLBを落とす。アンマップは`invlpg`。物理フレームもPTも解放しない。操作全体を`InterruptGuard`で囲む。照合は`split_child_entry`と同じ式では行わず`translate()`経由で行う。対象はフレームアロケータから確保した2MiB境界の未使用領域（2MiBを恒久的に予約する）
    - [x] M5-a-2-2: わざと壊しての確認（`cargo xtask run --paging-test <kind>`の7種）と、稼働中のヒープが載る2MiBページを使いながら分割する確認。実行中のコードとスタックは4KiBに載っているため、自己マッピングの分割は設計上安全だが実機での直接検証は行わない
- [x] 型分離: `PhysAddr` / `VirtAddr`（恒等マッピングのまま導入し、取り違えをコンパイルエラーにする。higher-half移行で触るべき箇所をコンパイラに列挙させるための布石。T-1からT-4、H-1、H-2で完了）
- [x] higher-half移行（ADR-0024でADR-0009をSupersede。ADR-0021とそのAddendum）。恒等を外す難所は低位ポインタ（RSP・ヒープ・boot_info）の高位化で、これはカーネルイメージの高位化と不可分なため、AとBに分ける
  - [x] A: direct physical mapの導入（恒等は外さない。既存を壊さず能力を足すだけ）
    - [x] A-1: 恒等とdirect map窓の両方を持つ新テーブルを構築しCR3を切り替える。登録`DirectMap`は恒等のまま。独立walkerで窓を検証し、窓経由で書いて恒等経由で読む別名の直接証明まで行う
    - [x] A-2: `replace_direct_map`で登録窓を高位へ差し替え、以降の`phys_to_virt`が高位を返すようにする
  - [x] B: カーネルイメージの高位化と恒等の除去
    - [x] B-0: split/unmapテストを恒等窓からdirect map高位窓へ一般化（恒等除去でも壊れない形にする）
    - [x] B-1: 高位実行の機構実証（テスト高位ベースへ別テーブルで切り替え。その後B-2a-3aで撤去。実証はコミット履歴に残る）
    - [x] B-2a: 再リンク（`KERNEL_VIRT_BASE`=高位、`code-model=kernel`）+ 低位トランポリン + 高位main到達。恒等は残す。破壊feature4種とトランポリンのバイト単位一致検査を含む（B-2a-1〜5）
    - [x] B-2b: 低位ポインタの高位化と恒等（`PML4[0]`）の除去。boot_infoは除去前に消費されるため高位化不要と判明（B-2b-1）、ヒープを高位化（B-2b-2）、恒等前提を網羅列挙し反転関門で順序を守り（B-2b-0/3）、除去を「検証可能で復帰可能な前段付きの不可逆な一手」として実装（B-2b-4）。除去は既定で走り恒等なしで完走する。破壊feature3種（remove-verify-fail/remove-before-highify/panic-after-remove）で5a復帰・順序必要性・パニック経路の恒等非依存を実証
- [x] M5-b: カーネルスタックのガードページ化（#PF専用のIST2を含む。溢れが「#DF」ではなく「CR2つきの #PF」として報告されるようにする）
- [x] M5-c: タスク構造とコンテキストスイッチ（**協調的**。自発的yieldのみ。TSSのRSP0更新をこの時点から設計に含める）
- [x] M5-d: プリエンプティブ化（タイマからのスケジューリング）
- [x] M5-e: ユーザーモードへの遷移（Ring 3、単一アドレス空間のまま）
- [x] M5-f: int 0x80システムコール（単一アドレス空間のまま。ADR-0020のレジスタ規約、ADR-0022で範囲を確定）
  - [x] M5-f-1: int 0x80の中核（ゲートDPL=3・専用スタブ・戻り値のRAX書き戻し・レジスタ規約）
  - [x] M5-f-2: ユーザーポインタ検証（実PTEを読んでユーザーアクセス可能性を確かめる。検証済みトークンとcopy_from_user）

## プロセス別アドレス空間（M5-f-3。higher-half B完了後の独立マイルストーン）

M5-fの子番号を持つが、ADR-0022でM5本体から切り出した独立マイルストーンである（M5本体は[x]でこの段には及ばない）。改名しないのはADR-0022とコミット履歴が「M5-f-3」で参照するため。B完了（恒等除去で`PML4[0]`が空いた）で解禁された。

**内容と到達条件は下記「その先」の段階列のS7に置く。この節はそこへのポインタである。** 同じ段の内容を2箇所に書くと片方が古くなるので、実体はS7だけに置く（M6節も同じ扱いにしてある）。進捗のチェックボックスも段階列の側だけに置く。

段階列での位置はS6（SMPの安定化）の後である。SMP-BKLを先にするのは、「現在のプロセス」のper-CPU化・CPUごとのCR3状態・`AddressSpace`を使用中のCPU集合・ページテーブル変更時の排他・破棄時のTLBシュートダウンを、後から作り直さずに済ませるためである。

## M6. ファイルシステム / ユーザーランド

下記「その先」の段階列のS9からS12がM6の中身である。段階ごとの内容・到達条件・進捗のチェックボックスはそちらに置き、この節はポインタにとどめる。

- S9: 埋め込みELFからのユーザープログラム起動（ファイルシステムに依存しない）
- S10: RAM上のext2読み取りと最小VFS、ファイル関連syscall（ADR-0025）
- S11: initと簡易シェル
- S12: ext2書き込み

---

## その先（S0からS13の段階列）

臨界経路「ストレージ → ファイルシステム → exec → ユーザーランド」を意識し、基本設計までをロードマップに置く。

**順序はS0からS13で確定している。** かつてこの節は「基本設計を置く場であって番号どおりの実行順ではない」と書いていたが、長期方針を実コードと照合して依存関係を洗い出し、順序を決めた。各段は「振る舞い不変の準備を積んでから危険な一手を打つ」形に分けてある（higher-half Bで採った形）。M5-f-3（プロセス別アドレス空間）はS7として、この列の中に位置づける。

各段に「到達条件」と「その段階では実装しないもの」を書く。細かい実装分割は着手時に決める（ADR-0022 / ADR-0023 / ADR-0025）。

### SMP（BKLファースト。ADR-0023）

- [x] **S0-a. seam整備**（先行。振る舞い不変。ADR-0023 §3の3項目）: 項目3（per-CPUアクセサ）= 3a器の導入 / 3b入れ子深さ / 3c GDT・TSS / 3d現在タスク。項目2（排他の監査）= 直接`cli`/`sti`を許可リスト方式の静的検査で固定（許可エントリ数と出現数は`cargo xtask check`が毎回印字する。いずれも排他目的ではない。ただしasm内の`GPR_BUF`保護のみ例外で、S3-aで扱う）。項目1（割り込み層の境界）= `kernel/src/irq/`へ`pic`/`pit`を集約し、呼び出し側を問いと観測値のAPIへ寄せ、内部を非公開にしたうえで、可視性を静的検査で固定した（1aから1d）。**到達条件は満たした**: `pic`/`pit`への参照は境界の外に無く、外から触るとビルドが落ちる。その保証が外されないことを検査が守る。`--full`が緑。実装しなかったもの=APIC、IPI、trait化（実装が2つになるまではモジュール集約でよい。traitはS2で切る）
- [x] **S0-b. `SCHEDULER`の別名違反の解消**（振る舞い不変。単独の段。**seam整備（ADR-0023 §3）の3項目には含まれない別作業である**。番号がS0-aに続くのは実施順が隣り合うためで、seam整備の4項目目ではない）: 2つの`&mut Scheduler`が同一コア上で並行しうる現在のUBを解消した。実体を`kernel/src/task/scheduler.rs`へ移し、外へはフィールド単位の操作だけを出したので、構造体全体への参照はモジュールの外から書けない（コンパイラが保証する。`static_mut_refs` lintでは解けないことを実測で確認した）。書き手と読み手が並行する3つのフィールド（`iterations` / `resumes` / `switches`）を`volatile`にした（それまでは素の読みで、たまたま動いていた）。**到達条件は満たした**: `&mut Scheduler` / `&Scheduler`を作る箇所が0、`--full`が緑、`task-test`6種が緑、起動ログの会計判定行が不変。実装しなかったもの=`Scheduler`の分解、`Locked<Scheduler>`化（S4）、`TIMER_TICKS`や`GPR_BUF`のper-CPU化（S3。**どちらもS3では行っていない。** `TIMER_TICKS`は**S3で実施も判断もせずに落とし、承認によりS4へ移した**。`GPR_BUF`は解禁条件が未達のまま先送りで、こちらもS4へ入る）
- [x] **S1. SMPの下ごしらえ**（単一コアのまま。APは起こさない）: bootloaderがRSDPの物理アドレスを`BootInfo`へ載せる。`BOOT_INFO_VERSION`のbumpを伴う（ExitBootServices後はUEFIのconfiguration tableを引けないので、渡せるのはbootloaderだけである）。ACPIテーブル領域をマッピング計画へ足す。direct mapは`classify()`が通した範囲だけを写すので、ACPI・Local APIC・IO-APICの領域は現在マップされていない。MADTを解析してLocal APIC IDを列挙しログへ出す。APトランポリン用フレームの確保方式を決めて実装する。**ACPIテーブル用の一時マッピングは不要と結論した**（テーブルが載る記述子の型がマップ対象であり、型が変われば実行時の`translate`チェックが検出する。`verification-coverage.md`の「ACPIテーブルへ触る前提」）。**S1-cでLocal APIC（`0xfee00000`）とIO-APIC（`0xfec00000`）のMMIOを4KiB粒度・PCDでdirect map窓へ写像し、Local APICのIDとVersionを読めることを確かめた**（`verification-coverage.md`の「APIC MMIOを写像する前提」）。粒度は`map_4kib`を通すことで構造的に決まり、アドレスはMADTから取り、`IA32_APIC_BASE`のEN／EXTDを見てから触る。**割り込みはPICのままである。** **到達条件は満たした**: 単一コアで`--full`が緑（76項目）、`-smp 2`で起動しても列挙結果がQEMUの指定と一致すること（手動確認ではなく検査項目`acpi-smp-test smp2-enumeration`にした）、トランポリン用フレームが1MiB未満かつ4KiB境界であることを起動時に検証してログへ出すこと。壊れたテーブルの検出も破壊feature 6種で実証した。実装しない=AP起こし、APIC移行、BKL、IPI、per-CPU、IO-APICのレジスタ読み書き（IOREGSELへの書き込みを伴うのでS2）
- [x] **S2. Local APIC / IO-APICへの移行**（単一コアのまま）: S0-aの境界の裏でPIC/PITからLocal APICタイマとIO-APICへ差し替える。**S2-d-2の到達条件に`irq::mask_all()`が実際に呼ばれることを入れる**（S2-bで足したが、S2-d-2まで呼び出し元が無い。`pub`だから`dead_code`にならないだけで、呼び忘れても警告では気づけない。S1-dの`smp::trampoline_frame()`と同じ扱いである）。**S2-d-0（前段）でInterrupt Source Overrideを復号し、GSIの解決を用意した**（実測は5件で、IRQ0がGSI2へ移る。ただしS2-d-1が移すIRQ1には上書きが無く、S2-d-2のタイマはLocal APICタイマなので、**どちらの配送経路も非恒等の解決を使わない**。`verification-coverage.md`の「Interrupt Source Overrideの解決（S2-d-0）」）。**S2-d-1は3つに分ける**（1a=スプリアスベクタのIRQスタイル化、1b=trait化で振る舞い不変、1c=キーボードの配送をIO-APICへ切り替え。1本のコミットに積むと落ちたときに切り分けられない）。**S2-d-1cでキーボード（IRQ1）の配送をIO-APIC経由（ベクタ`0x42`）へ移した**（S2-d-1bで書いて未実行だった`Apic::entry_for_irq` / `route` / `unmask`の3経路が実際に通った。ただし`entry_for_irq`が通ったことは「解決表が効いている」証明ではなく、IRQ1にISOが無いので結果は恒等である。`verification-coverage.md`の「キーボードの配送をIO-APICへ切り替える（S2-d-1c）」）。**`sti`前の項目4は格上げできないと確定した**（S2-d-2。1cの時点では「S2-d-2で格上げする」と書いていたが、**順序の制約により恒久的にUNVERIFIABLEである。** 較正はPITのティックを要するので`sti`の後、切り替えは較正の後なので、**この検査点ではタイマは必ずまだ8259経由である。** 項目は消さず、覆っているものを明示した。ADR-0018 Addendum 8）。**S2-d-1aでスプリアスベクタ`0xff`をIRQスタイルのスタブへ載せた**（専用スタブから共通経路へ合流し、EOIを送らずに戻る。誘発手段が無いので実行時にはまだ通っていない。`verification-coverage.md`の「スプリアスベクタをIRQスタイルのスタブへ載せる（S2-d-1a）」）。**S2-d-2でタイマをLocal APICタイマ（LVT経由、ベクタ`0xFE`）へ移し、8259を全マスクした**（`verification-coverage.md`の「タイマをLocal APICタイマへ移す（S2-d-2）」）。**8259経由の通常の配送は1本も残らない。** ただし**PICは配送から消えただけで、経路としては残っている**: タイマはPITで起動して較正の後に移るので起動直後の窓ではPIT経由であり、スプリアスIRQ7が`0x27`で届きうるので`init()`（再マップ）も呼び続ける。**このため`alt-offset-test`は撤去しない**（以前「S2-d-2で意味を失う」と見込んでいたが前提が誤っていた。最初のティックは今も8259経由で届くので、ICW2の検査は今も生きている。覆う範囲が「切り替え前の窓」に狭まっただけである）。**較正の許容幅はカーネル側だけでは閉じない**（要求周波数と実効周波数の両辺を同じ較正値から導くので自己無矛盾になる）ため、**ホストの実時間を基準にする`lapic-timer-test`を足した**（4種。うち3種が破壊）。**到達条件は満たした**: 既存のinterrupt-test一式が緑、`--full`が緑、PICを全マスクした状態でタイマとキーボードが動くこと、**`irq::mask_all()`が実際に呼ばれること**（S2-bで足してからここまで呼び出し元が無かった。破壊`no-mask-all`が、呼ばれなければ落ちることまで示している）。実装しない=AP、IPI、`TIMER_TICKS`のper-CPU化（方針が見えるのはS3。**S3では方針も決めずに落とした。承認によりS4へ移した**）
- [x] **S3. AP起こし**（APは起きて待つだけ。BKLはまだ無い）: S3-a（振る舞い不変の準備）=タスクの状態を`runnable: bool`から状態機械へ広げる / `GPR_BUF`のデモがBSPでしか走らないことを`cpu_id() != 0`で検査して落とす（どちらも単一コアで等価に導入できる）。**`GPR_BUF`のper-CPU化はS3-aでは行わない**（asmは15本のGPR全部をパターンで埋めており、自コアのスロットを選ぶにはGS相対か集約ブロック形式が要る。**S3ではAPがタスクを実行しないので競合そのものが発生しない。** `deferred-decisions.md`の当該項目で解禁条件を書き換えた）。**S3-bは3つに分ける。** S3-b-1（**APは起こさない。`MAX_CPUS = 1`のまま**）=per-CPUスロットが起動しうるコア数を覆っていることを起動時に保証し（超えていたら停止する。**releaseでも消えない**）、実ID化の器（`common::percpu`の`install_cpu_id_reader`）を置く。**`cpu_id()`は定数`0`のままである。** **実ID化をここでやらないのは実測による**: Local APICのIDレジスタを`cpu_id()`から読む形を実装して測ったところ、プリエンプティブデモの周回数が**2214から374へ落ちた**（3回ずつ起動。幅は2180-2254と368-380で**重ならない**。約**5.9倍**の退行）。TCGはMMIO読みをメモリリージョンのディスパッチとして処理し、`cpu_id()`は`critical_nesting_depth`経由でクリティカルガードの出入りごとに通る。**`MAX_CPUS = 1`の間、この読みは費用だけで便益がゼロである**（答えは常に`0`）。**却下したのは「`cpu_id()`の呼び出しごとにMMIOを読む形」であって、「MMIOをIDの出所に使うこと」ではない。** b-2aでRSP由来のインデックスが取れれば、**APIC IDを起動時に1回だけ読んでper-CPUの場所へ保存する形はありうる**（呼び出しごとの読みが無くなるので5.92倍の話は当てはまらない）。**この区別を落とすと、b-2aで使える手を捨てることになる。**GSベースも使えない（`deferred-decisions.md`の「GSベースは`swapgs`が無いうちは`cpu_id()`の土台に使えない」。GSセレクタのロードでベースが0へ戻り、既定ビルドはRing 3へ2回降りるので必ず失われる）。S3-b-2a（**APはまだ起こさない**）=per-CPUスタックの敷設と、そこから導く`cpu_id()`の実ID化。`MAX_CPUS > 1`にはできる（コアが1つでも配列は複数持てる）。**RSP由来が難しいと分かった場合の代替は`CPUID.01H:EBX[31:24]`（initial APIC ID）である**（`common/src/cpu.rs`が既にCPUIDを使う前例がある。**ただしCPUIDは実機でもシリアライズ命令なので長期の答えではない。** 長期の答えはGSベースで、S9で`swapgs`と同時に来る）。**到達条件に「据えたreaderが本番経路から実際に呼ばれること」を入れる**（S3-b-1では器だけなので呼ばれない。`irq::mask_all()`や`smp::trampoline_frame()`と同じ扱いである）。**S3-b-2bは2つに分ける**（依存関係の実測から。`Logger`と`SerialPort`にはロックも`InterruptGuard`も無いので、**シリアルへ書く経路はper-CPUデータを一切触らない。** したがって「APが署名を出して`hlt`する」だけなら、per-CPUのGDT・TSS・IDTRも`cpu_id()`もper-CPUスタック一式も要らない）。**S3-b-2b-1=AP起こしの最小形。** トランポリン（実モードから long mode、共有の一時GDT）、INIT-SIPI-SIPI / BSPが渡すデータブロック（身元の索引と1枚のスタック）、**`trampoline_frame()`を実際に読むこと** / **S1から送った2件を致命へ格上げ**（トランポリン用フレームの予約失敗と、1ページに収まらない場合の隣接ページ）。**「AP用一時ページテーブル」は作らない**（この行は消さず状態を書き換えた）。理由は**静的な`zaytos_boot_pml4`が要件と一致するからである**——`PML4[0]`に低位1GiBの恒等（2MiBページ）と`PML4[511]`に高位を持ち、参照は全て読み取りで、**恒等除去は本流テーブルに対して行われこの静的テーブルには触らない。** **本番CR3では成立しない**（恒等が除去済みなのでトランポリンの次の命令フェッチが解決できない）。**この判断が終わるのはb-2b-2でAPが本番CR3へ移るときである。** **ただし`zaytos_boot_pml4`は`PML4[0]`と`PML4[511]`しか持たない。** `PML4[256]`（direct map）が無いので、**APのスタックのVAは恒等側（物理==仮想）にする**（direct map窓に取ると64bitへ入った直後の最初のpushで落ちる）。フレームアロケータの空きは低位1GiBに収まるので成立する（実測206 MiB）。署名の経路はポートI/Oと高位の静的データしか触らないのでdirect mapを要しない。**同じ物理フレームを2つのVAで見ることになる**——BSPは本番CR3なのでdirect map VAで触り、APは恒等VAで触る。**データブロック内のポインタはAPが使うVA（恒等）で書き、どちらの空間かを型か命名で区別する**（取り違えるとBSP側では正常に見えAP側でだけ落ちる）。**AP起こしの位置は`sti`より後**（10msの待ちをタイマの1ティックで作るため）で、**プリエンプティブデモの後・定常ループの前**に置く（デモの最中にAPが起きると観測が混ざる）。200µsの待ちは**10msで代用する**（規格の下限より長いだけで安全側。TSCは較正していないので新しい時間源を導入しない）。**イメージを増やさない**（スタックはフレームアロケータから1枚）。**起こすコア数は「`MAX_CPUS`を超えるコアは起こさない」とする**——`-smp 4`かつ`MAX_CPUS = 2`がその経路を実際に通す構成なので、**「起こさなかったコアがあること」を検査の主張に足して、この判断を検査で守る。** 決めておかないと、b-2b-2で「全部起こしてからスロットが足りないと気づく」形になる。**S3-b-2b-2=APが自分のper-CPU資産を持つ。** **per-CPUスタックは`PML4[258]`（`0xffff810000000000`）から4KiBページで張る。** **`PML4[257]`を使わない**——あそこは破壊feature`highhalf-remove-verify-fail`のサボタージュVA（`0xffff808000000000`）で、**空であることに依存している。** 使うと破壊が意味を失う（`verification-coverage.md`と`deferred-decisions.md`の2箇所に「置き場所を決めるときにサボタージュVAの移動をセットで扱う」と書いてある。**移さずに済むほうを選んだ**）。`[257..510]`をSMPのper-CPU用に温存してきた範囲の**目的どおりの初使用である。** 静的配列にしない理由は、`MAX_CPUS = 4`で2MiB境界を越えるためである（4択の比較は`deferred-decisions.md`）。**`cpu_id()`の`lgdt`前後の観測は成立しない。** readerは大域なので、APが`lgdt`より前に`cpu_id()`を呼ぶとフォールバックの`0`ではなく**GDTR由来のreaderが走り、トランポリンGDTを見て検証が崩れて停止する。** **これは欠陥ではなく、「誤認ではなく停止側に倒れる」性質が働くだけである。** したがって**この区間、すなわち`lgdt`より前は`cpu_id()`の呼び出し禁止区間である。** **その禁止は規律ではなく検証の停止が構造で強制している。** 観測は`lgdt`の後だけで行い、**`cpu_id()`の返す値とデータブロックで渡された索引が一致すること**を見る（GDTR由来とBSP由来という**独立な2つの出所の一致**である。`MAX_CPUS = 2`ならAPは`1`、BSPは`0`で値が分かれて観測できる）。**カーネルCR3に`PML4[0]`が無いことをAP側でも読み戻すこと**（**b-2b-1から移した条件である。** b-2b-1のAPは恒等のある`zaytos_boot_pml4`を使うのでそちらでは成立しない。**APが本番CR3へ移るこの段で着地する**）、**恒等VAのスタックが使えなくなること**（本番CR3には恒等が無いので、スタックのVAも本番テーブルに存在するものへ移す。**b-2b-1の恒等VAのスタックは一時的なものである**）、per-CPUスタックの敷設 / per-CPUのGDT・TSS・IDTRをAP自身が載せる / **身元の出所の非対称が実際に問われる**（`sgdt`由来は自GDTのロード後しか使えず、据える前のフォールバック`0`はAPでは誤り）、`deferred-decisions.md`の(ii)「全コアがタスク0」の決定（sentinelか明示設定か）、`Apic::mask_all`の扱い。**ここで`MAX_CPUS`を上げるなら2MiB境界に当たる**（静的配列だと`MAX_CPUS = 4`で越える。`common/src/percpu.rs`の`MAX_CPUS`のdoc）。**どちらの段も振る舞い不変ではない。** 到達条件=`-smp 2`と`4`で全APが起動署名を出すこと、per-CPUスロットの会計が起きたコア数と一致すること、`deferred-decisions.md`の「`MAX_CPUS > 1`で顕在化する前提」3件のうち**2件が閉じた**（(i)`cpu_id() < MAX_CPUS`の境界=起動時のhaltと`slot_ptr`の`debug_assert`、(ii)全コアがタスク0=sentinel）。**(iii)`GPR_BUF`の排他は解禁条件が未達のまま先送りである。** 解禁条件は「asmが自コアのスロットを選べる手段」で、**`sgdt`由来も使えない**（メモリへは書けるが、15本のGPRが全部パターンで埋まっているので引き算ができない）。**危険が実在しないのは、S3ではAPがタスクを実行せず`GPR_BUF`に触るのがBSPだけだからで、条件つきの安全である**（「読まれないから安全」と同じ形）。**現在の防衛線は`task::require_bootstrap_processor`**（BSP以外でデモが走ることを拒む）。**解禁条件の再評価は「APがタスクを実行する段」の設計に含める**（tripwireの機序の直接観測と同じ着地先なので、その段の設計時に一緒に出てくる）、**S3-aで入れた`cpu_id() != 0`の検査を破壊して落ちることを確かめること。これはS3-b-2a側の到達条件である**（`MAX_CPUS > 1`が要る。`MAX_CPUS = 1`のままsabotageで非`0`を返させると`this_cpu_ptr`が配列外を指すので、**破壊が別の未定義動作を作ってしまう。** S3-b-1では構成できない。**tripwireの破壊確認には、tripwireが守ろうとしている能力そのものが必要である**）、**asmから自コアのper-CPUスロットを選べること——これは未達のままS3を閉じた**（上の(iii)と同じ項目である。**到達条件として並べたまま段を閉じたので、この並びだけを読むと達成したように見える状態になっていた。** 段を閉じるときに到達条件の文自体を書き換える手順を`coding-standards.md`へ足した）。S3-aの`GPR_BUF`のper-CPU化は`MAX_CPUS = 1`でスロット0が固定オフセットになるから振る舞い不変で済むのであって、asmは`cpu_id()`も`this_cpu_ptr`も呼べない。APが自分のスロットを選ぶ手段はS3-bで用意する）。実装しない=BKL、APでのタスク実行、IPI、TLBシュートダウン。**S3の設計判断の詳細はコード内のdocにある。** `verification-coverage.md`にS3の節は作らない（同じ判断を2箇所に持つと片方が古くなる。この行の下にある落ちがその実例である）。読む入口は4つで、`kernel/src/smp.rs`（トランポリン、per-CPUスタックの置き場所、AP起こしの手順）、`kernel/src/gdt/mod.rs`（`cpu_id_from_gdtr`と`init_for_cpu`、身元の出所の非対称）、`kernel/src/task.rs`（`CURRENT`のsentinelと`require_bootstrap_processor`）、`common/src/percpu.rs`（`MAX_CPUS`と`this_cpu_ptr`）である
- [x] **S4. BKL本体**: BKLの取得と解放を入れる（カーネル入口で取りRing 3復帰で離す）。カーネル入口を分類する（syscall・IRQ・例外・ダブルフォルト・パニック・AP起動直後）。`Locked<T>`の中身をスピンロック併用へ差し替える。APをスケジューラへ参加させる。**危険の形が恒等除去とは違う。** 恒等除去は真に不可逆なので復帰経路（書き戻してhalt）を用意したが、BKLはcfgで切れるし失敗しても再起動できる。危険は「デッドロックして静かに止まる」ことなので、前段に要るのは復帰可能性ではなく進行性の観測である。**承認によりAPのLAPICタイマを範囲に足した**（LVTはコアごとに独立なので、APが自分のLVTを開ければ自分にティックが来る。IPIを使わずに「参加」と「コアごとのハートビート」を両立する唯一の道である。**較正値をBSPと共有するのは「LAPICタイマの周波数がコア間で同じ」という仮定であり、仮定である。** AP側のハートビートのレートをホストの実時間と突き合わせる検査、すなわち`lapic-timer-test`と同型の独立基準で実測検証する。仮定が崩れる環境ではそこで捕まる）。**承認により`TIMER_TICKS`のper-CPU化をS3から移した**（S3で落とした項目である。到達条件「コアごとのハートビート」に必須で、APのタイマと同じ段に置く）。到達条件=カーネル内の同時進入数が1であることの実測、全コアが進行していることの実測（コアごとのハートビート。**ここでの「全コア」は起こしたコア全部を指す。** `MAX_CPUS`を超えるコアは起こさない方針なので、`-smp 4`かつ`MAX_CPUS = 2`なら対象は2コアで、残りはskippedである。**読み替えではなく文面で決めておく**）、デッドロック時にタイムアウトして原因を出すこと、`-smp 1`と`2`と`4`で緑、ある入口でBKLを取らないと同時進入が2になることの実証。実装しない=細粒度ロック、IPI、TLBシュートダウン、NUMA、負荷分散。**取得と解放の点はADR-0023のAddendumで定義しなおした**（あちらが正である）。

  **S4は3つに分ける。順序は依存関係で決まっており入れ替えられない。** **S4-a=APにLAPICタイマを持たせ、ティックをper-CPUにする**（BKLはまだ無い）。**最初に置く理由は、BKLの効果が「2つのコアが同時にカーネルへ入ろうとすること」でしか観測できないからである。** APをカーネルへ入れる手段はタイマしかない（IPIはS4では実装しない）。**BKLを先に入れると、競合が起きない状態で「同時進入数が1」を観測することになり、何も確かめていない。** やること=AP自身が自分のLAPICのSVRを書く（**BSPの`set_spurious_vector`はBSPのLAPICにしか効いていない**）。ソフトウェア有効化のbit 8を読み戻して確かめる / AP自身が`program_timer`と`unmask_lvt_timer`を自分のLAPICへ行う / `TIMER_TICKS`のper-CPU化 / APのハンドラがスケジューラへ入る手前で戻る。**sentinelは外さない。入れてしまえばsentinelが止める** / コアごとのハートビート / 同時進入数のカウンタ（**この段では2になるのが正しい。ただし観測にはKVMが要る**——下記）。**較正値をBSPと共有するのは「LAPICタイマの周波数がコア間で同じ」という仮定であり、仮定である。** **この段の安全は条件つきである**——BKLが無いまま2コアが`irq_entry`に入るが、APのハンドラが触るものがper-CPUかアトミックだけだから成立する（`INTERRUPT_COUNTS`・`FIRST_PIC_VECTOR`・`LAPIC_SPURIOUS_COUNT`はアトミック、`TIMER_TICKS`はこの段でper-CPU、EOIは自コアのLAPIC、キーボードはAPに届かない、スケジューラは手前で戻る）。**この一覧は規律で守る。検査は作らない**（S4-bでBKLが入ればこの一覧そのものが不要になるので、2段しか生きない検査を作ることになる）。**S4-bで不要になることを、ここに申し送りとして書いておく。書かなければ実態と合わないまま古くなる。** **S4-b-2で実際に不要になった**（`irq_entry`がBKLを取るので、APのハンドラも排他される。**この一覧に頼った安全はもう要らない**）。**残るのは別の一覧である**——「BKLの外からシリアルへ書いてよい箇所」（APの起動途中・BKL待ちのタイムアウト診断・例外とパニックの3種）で、そちらはS4-bでも規律で守っており、**許可された箇所どうしの混線は防げない。静的検査はS6の到達条件にある。** 到達条件=`-smp 2`で両コアのハートビートがそれぞれ増え続けること / **APのティックのレートをホストの実時間と突き合わせて検証すること**。`lapic-timer-test`と同型の独立基準で、**較正値の共有が仮定であることの検証はこれである** / **同時進入数の最大が2になること。ただしTCGでは観測できない**（実測。`-smp 2`を86秒=17,060ティック回しても1のままで、入口の窓を3,000回の`spin_loop`ぶん広げても0回だった。**「稀」ではなく「起きない」である。** KVMでは2になる。**したがってこの主張はKVMで手動で観測する**（`cargo xtask run --kernel-entry-concurrency`）。**KVMでも決定的ではない**——4回のうち3回は2が出たが、1回は116秒（両コアで約23,000回の入口通過）のあいだ1のままだった。**確率的なものを`--full`へ入れると、落ちたときに退行か揺らぎかが区別できなくなる**ので、検査項目にはしていない。機序までは特定していない。**「KVMが使えない」と「主張が落ちた」は出力で区別する** / **キーボードの配送先が物理モードのdestination 0（BSP）であることを、routeの読み戻しの主張に足すこと**。現在の読み戻しはlow dwordだけで、ベクタとマスクしか見ていない。**「APにキーボードが届かない」はS4-aの安全の根拠なので、実測の記憶ではなく主張にする** / `-smp 1`で振る舞いが変わらないこと / `-smp 4`かつ`MAX_CPUS = 2`で1 AP参加 + 2 skippedのまま緑。**S4-bへの申し送り**——**BKLを入れた後にTCGで「同時進入数が1」を観測しても、それはBKLの証明にならない。** BKLが無くても1だからである（「同値である間は分類の誤りが観測できない」）。**S4-bの「ある入口でBKLを取らないと同時進入が2になることの実証」もKVMでなければ構成できない。** 破壊=`smp-ap-timer-no-svr`（APが自分のSVRを書かない。**LVTへの書き込みそのものは成功し、SVRのbit 8が落ちているので配送されない**。APのティックが0のまま止まる）、`smp-ap-timer-share-ticks`（`TIMER_TICKS`を共有のまま数える。**コアごとのハートビートが合計に化ける**。per-CPU化が「済んだように見えて共有のまま」を防ぐ）、`smp-ap-enter-scheduler`（APがスケジューラへ入る。sentinelが止める）、`ioapic-keyboard-broadcast`（配送先をlogical/broadcastにする。destinationの主張が落ちる）。実装しない=BKL、APでのタスク実行、IPI、TLBシュートダウン、APへのキーボード配送、`Locked<T>`の変更。

  **S4-b=BKL本体、入口の分類、`Locked<T>`のスピン化。** **スピン化は実装しない**（S4-b-1の実装で判明。行は消さずに状態を書き換える）——**BKLの下では本番経路の`Locked<T>`の競合が構造的に起きない**（`SCANCODES`もヒープもBKLの内側でしか触らない）。**待機化は死んだコードになる。** S4-b-1で入れたのは**分類**（保持者のCPU番号を持ち、競合と同一コアの二重取得を分ける）だけで、**競合側は従来どおり停止する。** 待機へ変えるのは、競合が実際に起きうる設計になった段である。**BKLは`InterruptGuard`を使わない**（実装前の確認で判明。下記）。やること=BKLを置く。中身は`AtomicBool`と、保持者のCPU番号・入口の分類・取得時のTSCを持つ診断用のフィールドである / 取得は`swap(true, Acquire)`、解放は`store(false, Release)` / **競合は最初から常時ある**（両コアが100Hzでカーネルへ入る）のでスピンは`core::hint::spin_loop()`つき / **TSC基準のタイムアウト**（`FIRST_TICK_TIMEOUT_CYCLES`の前例）。超えたら「どのコアが、どの入口で、何サイクル待っているか」と「誰が、どの入口で保持しているか」を出して停止する / 入口を分類する列挙。**例外とパニックが取らないことを列挙で明示する** / 定常ループを「1周の中の一部だけIF=0で保持する」形にする / `Locked<T>`に保持者のCPU番号を持たせ、**競合と同一コアの二重取得を分ける**（**今は両方とも`acquired`が`true`という同じ観測になっており、分類の誤りが観測できない**）。**コミットを分ける**——同時進入数の定義をBKL基準へ移す作業とBKLの導入を別にする。値が2から1へ変わるのは**BKLが効いたからでもあり定義が変わったからでもある**ので、同じコミットに入れると切り分けられない。**BKL待ちはIF=0のスピンになる。保持者が必ず解放するので安全だが、その安全はIPIが存在しないことに依存している**（`deferred-decisions.md`の「BKL取得待ちのIF=0とIPIのデッドロック」がS5の着地点である）。到達条件=**カーネル内の同時進入数の最大が1であることの実測**。S4-aの2から下がる / 両コアのハートビートが増え続けること / **ある入口でBKLを取らないと同時進入が2になることの実証** / デッドロックを作ったときにタイムアウトして原因が出ること / `-smp 1`と`2`と`4`で緑 / 既存の`critical-test`が緑。破壊=`bkl-skip-timer-entry`（タイマ入口でBKLを取らない。同時進入が2になる）、`bkl-hold-across-hlt`（定常ループが`hlt`の前に離さない。**もう一方のコアがタイムアウトして原因を出す。「静かに止まる」を「うるさく止まる」へ変えた形の実証**）、`bkl-hold-with-if-set`（保持したままIF=1にする。**再帰検出が働くこと。§4の不変条件の破壊確認である**）、`locked-treat-remote-as-double`（`Locked<T>`が別コアの競合を二重取得として扱う。**現行設計では構成不能。復活条件は下記**）。**S4-bは4つに分ける。** S4-b-1（振る舞い不変）=`Locked<T>`に保持者のCPU番号を持たせ、**競合と同一コアの二重取得を分ける**（今は両方とも`acquired`が`true`という同じ観測で、分類の誤りが観測できない）。**この段では競合が起きないので分岐が増えるだけである**が、`GPR_BUF`で退けた「形だけ作って安全になったように見える」形とは違う——あちらは配列にしても全コアがスロット0を叩く作り込みで、こちらは**分類の追加であり、足した瞬間から「どちらの原因か」が観測できるようになる。** 到達条件=既存の`critical-test`2種が緑 / `--full`が緑 / 起動ログの判定行が不変。**`locked-treat-remote-as-double`はこの段では構成できない**——本番経路で`Locked<T>`に触るのはbootstrap processorだけなので競合が起きず、**分類を壊しても観測が変わらない**（どちらの原因でも停止するので、違うのは文言だけである）。**破壊はS4-b-2へ送る**と書いたが、**S4-b-2にスピンは入らなかった**（上記のとおりスピン化そのものを実装しない）。**この前提は実際と食い違っていたので訂正する。** **現行設計では構成不能である。** 理由は「壊したときに落ちることを示せない」ではない——**`skip`構成なら本物の競合が起きるので、文言の差（`contended acquisition` と二重取得）が観測になる。** 構成できない理由は**競合の発火が決定的でないこと**である（プリエンプトが`Locked<T>`の保持窓へ食い込むかどうかがタイミング次第である）。**復活条件は2つ**——(a)`Locked<T>`の競合が停止ではなく待機になったとき、(b)S4-b-4の測定で`contended halt`が決定的に発火すると分かったとき。**(b)は評価した（2026-08-01）。`widen` + `skip`の5回の測定で発火は0回で、満たされない。** (a)は生きたままである。S4-b-2（危険な一手）=BKL本体、入口の分類、定常ループの区間化、再帰検出、タイムアウト。S4-b-3=同時進入数の定義をBKL基準へ移す。**単独のコミットにする**（値が2から1へ落ちる理由が2つあるため）。S4-b-4=`bkl-widen-entry-window`と`bkl-skip-timer-entry`、および2構成の検査。**S4の相互排除の証明はここで閉じる。** **BKLは`InterruptGuard`を使わない。実装前に確かめた結果である**——`InterruptGuard`は`CRITICAL_NESTING_DEPTH`を増やし、その値を`task::on_yield`と`task::on_timer_tick`が見ている。**BKLを`irq_entry`の先頭で`InterruptGuard`として取ると、`on_timer_tick`の防御スキップが常に発火してプリエンプションが完全に止まり**（静かに壊れる）、**`on_yield`は「保持したままyieldした」と判定して停止する**（うるさく壊れる）。**カウンタの意味は「割り込みが禁止されているか」ではなく「呼び出し側が自発的にガードを保持しているか」である**——`on_yield`のコメントが「intゲート自身が積んだぶんは`InterruptGuard`ではないのでカウンタには乗らない」と既に線を引いており、**BKLは入口自身が取るものなので同じ側に落ちる。** したがってBKLは`CRITICAL_NESTING_DEPTH`に触らない専用のガードで`cli`する。**「保持区間=IF=0」の不変条件は保たれる**（数えないだけで`cli`はする）。再帰検出はカウンタではなく保持者のCPU番号で行う。**`task-preempt-in-critical`の破壊は`InterruptGuard`側だけに効き、BKLには効かない**（あちらが狙うのは自発的ガードの区間であり、意図は変わらない）。**ログの規約はS4-bでは規律で守る。** BKLの外からシリアルへ書いてよいのは3種類（APの起動途中 / BKL待ちのタイムアウト診断 / 例外とパニック）で、**許可された箇所どうしの混線は防げない。守れないと書く。** 静的検査はS6へ回す。実装しない=細粒度ロック、IPI、TLBシュートダウン、NUMA、負荷分散、APでのタスク実行、`GPR_BUF`のper-CPU化、タスクのコア間移動、BKL待ちのIF=0とIPIのデッドロックの解決（S5）。

  **S4-c=APをスケジューラへ参加させる（sentinel解除）。** **守りは2層にする。** 第1層=**静的な担当コア**（各タスクが担当コアを持ち、`pick_next`は自コアが担当のものだけを候補にする）。第2層=**`CURRENT`条件**（候補が**他コアの**`CURRENT`に入っていないこと）。**第1層が先に防ぐので、第2層は本番では発火条件が無い構造的なガードである**（S4-bで`Locked<T>`の分類を「起きない競合への備え」と書いたのと同じ形。**起きないものを起きると書かない**）。**自コアの`CURRENT`は除外しない**——除外すると現タスクの再選択が消え、**「`pick_next`は現タスクを返しうる」というホストテストで固定した契約が壊れる**（毎ティック必ず切り替わる形になる）。**候補ゼロの経路は新設ではない**——`pick_next`は既に「走行可能なワーカーが無ければタスク0（メイン）を返す」形で、その経路はホストテストで固定されている。**担当コアを入れると、APにとっての「タスク0」に相当するものが要る**ので、**AP用のアイドルタスクを1つ新設する**（下記）。**事実の確認**——APに割り当てるタスクは**新設**であり、`TASK_COUNT`は3から4へ増える（メイン + ワーカー2 + AP用1）。`WORKER_COUNT = 2`はBSPのワーカー数として据え置く。**`scheduler::switches`は大域の単一カウンタであってper-CPUではない**ので、「両コアで`switches`が増える」は**そのままでは観測できない**。到達条件に使うなら**per-CPU化するか、コアごとの別の観測量（各コアの`CURRENT`の遷移）に変える**。**着手時に決める。** やること=`CURRENT`のsentinelを、APに割り当てるのと同時に解く。**外し忘れは静かに壊れず落ちるので安全側である** / APのハンドラが`on_timer_tick`へ入るようにする / APに走らせるタスクを用意する。**タスクが2コアから同時に選ばれうる**——ADR-0023 Addendum §5のとおりBKLの解放は`iretq`より前で、**タスク本体はBKLの外を走る。** したがってBSPが`pick_next`で選んだタスクを、APも`Ready`のまま選びうる。**2コアが同一タスクのスタックを同時に走ることになる。** **`GPR_BUF`のコア間移動とは別の危険である**（あちらはデモの照合、こちらはスタックそのもの）。**S3-aで`TaskState`に`Running`を持たせないと決めた判断を、この段で再訪する必要がある**（`CURRENT`との二重管理を避けるためだったが、選んだ瞬間に他コアが選べなくする印が要る）。設計はこの段で行う。**`GPR_BUF`の解禁条件はこの段でも未達のままである**——ADR-0023 Addendum §5のとおりタスク本体はBKLの外を走るので、**BKLは`GPR_BUF`を守らない。** GPR照合デモはBSP専用のままにし、`task::require_bootstrap_processor`をそのまま残す。APには`GPR_BUF`に触らない別のタスクを割り当てる。**tripwireの機序の直接観測（S3から2段繰り越した宿題）はこの段で構成可能になる**（APにGPR照合デモを割り当てれば実際の並行アクセスが起きる）。**構成は「APにデモを開始させる」形でなければならない**——`require_bootstrap_processor`の射程はデモの入口だけで、**継続では鳴らないが開始では鳴る**（`deferred-decisions.md`の`GPR_BUF`の項目）。**したがってtripwireを外したうえでAPに`run_preemptive_demo`を呼ばせる。** APがワーカーの続きを走るだけの形では、tripwireは鳴らず並行アクセスの観測にもならない。**決定的にできるかを先に測り、できなければ一度きりの手動実証にして恒久featureにしない**（`walk_user_accessible`の中間階層U=0と同じ扱い）。到達条件=APがタスクを実行し両コアで`switches`が増えること / 同時進入数の最大が1のままであること / `-smp 1`と`2`と`4`で緑 / sentinelを外し忘れたら停止すること。破壊=`smp-ap-no-sentinel-clear`（sentinelを解かずに割り当てる）、`sched-ignore-owner`（**第1層だけを無効にする**。第2層が防ぐので2コア選択は起きない。**第2層が働いていることの実証である**）、`sched-ignore-owner` + `sched-ignore-current`（**2層とも無効**。同じタスクが2コアから選ばれる）。**破壊は1つにつき1層だけを無効にし、2つを組んで初めて事象を作る**（`widen` + `skip`と同じ形）。**破壊ビルドと本番の差をdocに書く**——`acquire_counting_only`と同じで、**破壊ビルドの`pick_next`は本番と別の関数である。** 判定の観測点は**ハートビートに出すコアごとの`CURRENT`**とする。**BKLの外から書いた行は判定に使えない**（S4-b-4で混線の標本を得た）。ハートビートはBKLの内側で書かれるので混ざらない。実装しない=**タスクのコア間移動**（負荷分散を実装しないことの帰結ではなく、`GPR_BUF`の安全がそれに依存しているからである。理由が違うので別に書く）、負荷分散、細粒度ロック、IPI、TLBシュートダウン、`GPR_BUF`のper-CPU化

  **この項目には設計の記述が残っている。**「roadmap=段階と到達条件」という正の定義からは外れた残りで、S4-aの安全の一覧（per-CPUかアトミックだけを触るという根拠）、TCGでの実測（86秒・17,060ティックで重なり0）、BKL外からシリアルへ書いてよい箇所の3種である。**重複ではない**——**いずれも他所に持ち主がおらず、消すと事実が失われる。** そのため整理の段では触っていない。**移すかどうかは、これらが他所に持ち主を得たときに判断する。**

  **S4完了（S4-a / S4-b / S4-c）。到達条件は満たした。**カーネル内の同時進入数が1であることの実測（`max kernel entry depth=1`）、全コアが進行していることの実測（コアごとのハートビート、APの`ap_sched_passes`が伸びること）、デッドロック時のタイムアウト、`-smp 1` / `2` / `4`で緑、ある入口でBKLを取らないと同時進入が2になることの実証。**APのスケジューラ参加はS4-cで入り、守りは2層になった**（静的な担当コアと`CURRENT`条件）。**`KernelEntry::ApBringUp`の取得箇所はS4-c-3-2bでできた。**

  **繰り越す項目と宛先（段閉じのgrepで洗った全件）。**
  - `Apic::mask_all`が一度も実行されていない: **宛先なしと決めた**（`verification-coverage.md`。S5でも来ない見立てで、現実的にはシャットダウン経路かS6。決まらないと書いて残す）
  - `locked-treat-remote-as-double`: **現行設計では構成不能。** 復活条件は(a)`Locked<T>`の競合が停止でなく待機になったとき、(b)contended haltが決定的に発火すると分かったとき。**(b)は評価済みで不満足**（5回で発火0）なので、生きているのは(a)である
  - BKL外のログの混線の実物（S4-b-4で観測）: **S6のログ規約へ結線済み**
  - `GPR_BUF`のper-CPU化: **解禁条件未達**（asmが自コアのスロットを選べる手段。15本のGPRが全部パターンで埋まる）。**S4-c以降の実際の保護は第1層の静的担当とタスクのコア間移動を実装しないことである**
  - MADTの先頭をBSPと仮定している潜在的欠陥: **解禁条件あり**（MADTがBSPを先頭に置かない構成を作れた時点）。**「S4でAP起こしの経路に触るならついでに再評価」という早い機会は成立しなかった**（S4-cが触ったのは`ap_after_switch`以降で、`wake_application_processors`のBSP判定には触れていない）
  - `demo_deadline`がコアごとのティックで比較されている（S4-c-4-2で発見）: **S5で再検討**
  - `default_task_for`が`MAX_CPUS > 2`で破れる: **`MAX_CPUS`を上げる作業に含める**（ホストテストが`0..MAX_CPUS`を回すので上げた瞬間に落ちる。落ちるのが正しい）
  - 起動ログの行の会計と、xtaskが経過時間を出す案: **どちらもS6の候補として記録済み**
  - 第2層と検出器の実証: **S4-c-4-3で解消した**（繰り越さない）
- [x] **T1. コメントとドキュメントの整理**（S4とS5の間に挟んだ独立した段。振る舞いを変えない）: 運用者の「コード内コメントの過多とドキュメントの冗長さ」への対処として、**削除ではなく圧縮**と、**同じ事実が2箇所にあるときは正を1つ決めて他方を参照に置き換える**ことを軸にした。

  **結論は「減らなかった」である。これがこの段の成果である。**

  **量はほとんど減っていない。** 棚卸しで4ファイルを標本抽出したところ、**圧縮候補はコメント行の5〜8%**しかなく、**中身は名前の言い換えが主体**で、消しても読みやすさは上がらないと分かった。**そこでC型（純粋な圧縮）は全面的に見送った。** 見送りは失敗ではなく、**測ったうえでの範囲の確定である**（`verification-coverage.md`の「設計判断を記録したコメントかどうかの判定は機械化できない」）。

  **機械化できないことが2つ分かった。** **(1) コメントが設計判断かどうかの判定**——語で拾おうとすると両方向へ外れる（「デモは浅いので16KiBで足りる」には「なぜ」も「理由」も出てこないが、値を選んだ理由そのものである）。**(2) 長い段落の分割**——1,000字という基準は段落・会計行・表を区別せず、機械的に割ったら**会計行と表の行を壊した**（どちらも検査が捕まえた）。

  **代わりに進んだのは、重複の解消と「正」の明確化である。** `MAX_CPUS = 2`の失効条件が4箇所にあったものをADR-0026へ、イメージ会計の数字をverification-coverageへ、`GPR_BUF`の保護の時期をADR-0026へ、切り替えの機序をモジュールdocへ寄せた。**ADR-0026の最終節で決めた「文書ごとの正」（ADR=決定と理由 / verification-coverage=検査と事例 / roadmap=段階と到達条件）を、実際に適用した段である。**

  **副産物として、docs側に機械的な下限を1つ足した**（`coding-standards.md`の正規化差分の手順）。**コードはビルドと検査が落ちるが、docsは何も落ちない**という、この段でずっと問題だった点への直接の答えである。

  **やり残し**——1,000字超の段落が11件ある。**件数は増えたが最長は9,619字から3,534字へ下がっている**（代理指標として機能しないことを`coding-standards.md`に書いた）。**それぞれのファイルを次に構造的に触るときに割る。**

- [x] **T2. コメントの書き方の統一**（S7とS8の間に挟んだ独立した段。振る舞いを変えない）: 運用者の要望で始めた。目的は3つ——コメントの書き方を文書に明記する / すべての`.rs`をそれに沿った書き方にする / AIが書いたような分かりにくい文でなく、簡潔で要点を捉えた自然な日本語にする。**T1が測ったのは「消せるか」で、T2が問うたのは「どこに書くべきか」と「どう書くか」である。**

  **完了ではなく打ち切りである。** 処理したのは13ファイル・6,692行で、**追跡下の`.rs`のコメント全体の45%**である。**残り55%は未処理のまま閉じる。**

  **打ち切った理由。** 削減率が**-2.4%**（14,771 → 14,412行）に留まり、**残るファイルは構造化型で削減がほぼ出ない見込み**だった（型の判定は機械で行った。`deferred-decisions.md`の「T2-cの締め」）。**往復の費用が、得られる削減に見合わなかった。**

  **削減はこの段の成果ではない。** 成果は3つある。**(1) 規約**（`coding-standards.md`のコメントの節）。**これは残り、これから書くコードに掛かる。** **(2) コード内の強調の解消**（3,056 → 1,530行。処理した13ファイルでは1,546 → 20行）。**(3) 掃きの途中で見つかったもの**——docの付け先の壊れ7件、**属性が効いていなかった1件**（`allow(dead_code)`が別の項目に付いており、抑止したい警告が実際に出ていた）、内容の食い違い4件、空の`# Safety`節1件、**確率的な`--full`の項目が4つあることと方針の適用漏れ。**

  **残ったものと宛先。** **段名では書かない**（T1・T2で3度確認した規律である）。
  - `xtask/src/main.rs`（1,760行、強調543）: **規約の当てはめ方そのものに判断が要る。** 宛先は**あのファイルを構造的に触るとき**（検査項目の追加・削除や、一覧の作り替え）
  - `kernel/src/bkl.rs`（333行、強調152）: 宛先は**BKLの設計に手を入れるとき**
  - 小さいファイル群と中規模ファイル: 宛先は**そのファイルを構造的に触る機会**
  - `kernel/src/main.rs`に残る強調20行: 内訳は`deferred-decisions.md`の「T2-cの締め」。**5行は意図して残した恒等前提マーカー**で、**12行は強調を全廃する方針が固まる前に掃いた範囲**である

  **再開するなら、まず測ること。** 「コメントの量が問題になったとき」では発火しない（量は常に多い）。**測る手段を先に書く**——**`///`と`//`の内訳を測っていない。** `///`が大半なら`cargo doc`で読めば足り、削除は要らない。**内訳を測るのは1往復で済むので、再開の第1手はそれである。**

  **持ち越し12件は生きたまま`deferred-decisions.md`にある**（T2-bの締めとT2-cの締め）。**機械化する検査と点検の候補、確率的な4項目の扱い、未処理ファイルの扱いが、次に判断する材料である。**

- [x] **S5. IPIとTLBシュートダウン**（**用途が生まれるのは先の段である。ここは前倒しで置く**——現在の本番には写像を実行中に変える経路が無く、**シュートダウンすべき対象が存在しない**（`verification-coverage.md`の「S5の機構は、現時点では発火条件の無い備えである」）。**用途が生まれるのは ext2 / VFS / ユーザープログラムなど、実行中に写像を変える段**であり、**S5はそこへの前倒しである。** 先に置く理由は、BKLがまだ単純なうちに相性の論点を片付けられることにある）: BKL取得待ちのIF=0とIPIのデッドロックを構造的に避ける方式を選ぶ（`deferred-decisions.md`に4案）。世代番号による要求の識別、完了通知、タイムアウト、全対象コアの完了前に物理フレームを再利用しないこと。到達条件=リモート無効化を意図的に省く破壊で検査が落ちること、`-smp 2`と`4`で緑、タイムアウトが無言で継続しないこと。実装しない=`AddressSpace`単位のシュートダウン（この段の対象はカーネル領域と仮想アドレス指定に限る。使用中CPU集合はS7で設計する）
  **S5完了（S5-a / S5-b / S5-c）。到達条件は満たした。**
  IPIが届くこと（送受信の会計が閉じる。TCGで観測できた。KVMは要らなかった）、
  世代方式が1ティックで整合すること、**シュートダウンの有無で見え方が変わること**
  （世代を上げた側は2回目の触りで#PF、上げない側は古い翻訳で成功）。
  **`-smp 1`/`2`/`4`で緑、同時進入数の最大は1のまま。**

  **繰り越す項目と宛先。**
  - `Apic::mask_all`: **宛先なしと決めたまま。S5でも来なかった**（見立てどおり）
  - `demo_deadline`のコアごとのティック比較: **S5では発火しなかった。**
    宛先を**「APが締切を使う経路を持つとき」**へ付け替えた（条件で書く）
  - MADT先頭=BSP仮定: **S4・S5と2段続けて空振りしたので、解禁条件を
    「実機で起動を試すとき」へ付け替えた**
  - **S5の設計判断はADR-0027にある**（ackを待たない選択、案5改、案1を予備とした理由）

- [x] **S6. SMPの安定化**: 機能を足さず、反復・長時間・異なるコア数・意図的破壊で、デッドロック・競合・停止・会計不一致を洗う。到達条件=S4とS5の破壊featureがすべて意図どおりに落ちること、通常構成が長時間緑であること、**BKLの外からシリアルへ書いてよい箇所の許可リストを静的検査で固定すること**（S4-bから移した。あちらでは規律で守っており、許可された箇所どうしの混線は防げないと書いてある。`cli`/`sti`の許可リストと同じ形にできる）。**ログ規約の作業に「起動ログの行の会計をxtask側の定数で持つか」を一項目として置く**（S4-c-2で検討して見送った。見送りの理由は、行形の全件突き合わせのほうが厳密に強く、数だけの定数は更新の手間を増やすだけであること。**捨てずに候補として残す。** 持つなら`EXPECTED_CHECK_COUNT`と同じく定数と実測を照合する形で、docsに数字を書く形にはしない。それまで行数は段ごとに突き合わせる観測量として扱い、**docsには書かない**）。**あわせて「イメージ会計の最新値を機械で突き合わせる」を候補として置く**（xtaskが既定ビルドの起動ログから読んだ`__kernel_end`と、docsの会計に記録された最新値を突き合わせ、食い違えば落とす。**理由は、会計行の欠落が3件目だからである**——S2-d-1b/1c、S4-a/b/c-1、S4-c-4-3。**3回起きたなら規律では止まっていない。** 検査項目の数が`EXPECTED_CHECK_COUNT`と対で照合され、実際に2度誤りを捕まえたのと同じ形にできる。**`target/`の状態に依存せずに値を取れることも利点である**——破壊feature付きの成果物が残った`target/`を読む事故が3度起きており、起動ログを出所にすればその経路が構造から消える。**T1では作らない**）。**あわせて「`cargo xtask check`が実行終了時に経過時間を出す」を候補として置く**（S4-c-3-1で検討。`--full`の所要時間を知りたいという要求はあるが、壁時計時間は機械と負荷に依存するのでdocsに書けば必ず腐る。**値と導出を同居させる形、すなわち実行そのものが出す形が筋である。** S4では入れない）。**「行数の定数を持つか」は候補から外した（S6-dで決着）。** 行形の正規化差分を作ったので、数だけの定数はそれに包含される（1行減って1行増えれば通る弱さがある）。**あわせて「静的な検査が空振りしていないかの棚卸し」を候補として置く**（S5-bで規律にした「新しい静的な検査は、主張が偽の状態を一度作って落ちることを確かめてから置く」（`coding-standards.md`）は、**これから置くものにしか掛からない。** 既存の静的検査には遡って掛かっていない。**ただし遡って壊す必要があるとは限らない**——規律の目的は「主張が偽のときこの検査は落ちる」と分かっている状態であって、その根拠は**意図した破壊でも、実際に観測された失敗でもよい。後者のほうが強い証拠である。** したがって作業は3段に分かれる。**(c)まず一覧を作る**——「既存の静的検査」が3つで尽きている保証は無いので、**棚卸しを先に行い、観測済みと未観測に分ける。** **(a)項目会計とTEST_HOOKSの網羅は既に落ちるところを見ている**（項目会計が2回、TEST_HOOKSが1回）。**遡って壊さない。**「いつ・何で落ちたか」を確認済みの記録として、規律の側から`verification-coverage.md`の事例を指す形にする。**(b)スタブ表の一致は一度も落ちるところを見ていない。ここが本当の空白であり、一度だけ意図的に落とす。** ただし**主張を偽にする最小の変更が作りにくい可能性がある**——本数は型で対になっているので、崩そうとするとビルドが落ちて検査まで到達しないかもしれない。**作れないなら、「作れない」こと自体が記録に値する**——それは「この検査は構造で守られていて、空振りしうる形が存在しない」という別の主張になるためである）

  **S6完了（S6-aからS6-e）。到達条件は3つの定義で置き換えた。** 段に「これが動けば終わり」という機能が無いので、**何をもって安定と言うかを先に定義した。**

  **定義1（`--full`を5回連続で緑）**: 達成。順序と先行実行に依存する不安定を出す唯一の手段である。
  **定義2（10分の実行で漂流が無いこと）**: 達成。既定構成と`-smp 2`で59標本すべて同一。**覆うのは漂流であってデッドロックではない**（BKLのタイムアウトが約15秒で報せる）。
  **定義3（コア数を変えても、コア数に依らない部分が同じ）**: 達成。`-smp 1` / `2` / `4`の正規化済み起動ログが284行で一致。**110項目を3構成で回してはいない。**

  **足した検査は3種**（`--full` 110から113へ、baseは19から20へ）。IRQスタブ表の配置検証の破壊、シリアルの口の許可リスト、起動ログの正規化差分。**機能は足していない。**

  **副産物として、既存の一覧の穴が3つ見つかった。** `cli`/`sti`の許可リストの死んだエントリ2件、除外の目印の効かない項目1件、`SABOTAGE_FEATURES`の照合がbootloaderを見ていなかったこと。**いずれも「一覧が静かに広すぎる」形で、これまで守ってきた「静かに狭くなる」の鏡像である。**

  **繰り越す項目と宛先（段閉じのgrepで洗った全件）。**
  - 定常経路に確保が入らないことの静的検査: **候補のまま残す**（S6-cで穴として書いた。`--full`に10分の実行を入れない代わりの継続的な守り）
  - 除外の目印（`BOOT_LOG_CORE_COUNT_MARKERS`）の効き目の機械化: **候補のまま残す**（S6-dで手で当てた。機械にすると範囲が広がる）
  - 許可リストの18件がBKLを保持していないことの実行時の確認: **`deferred-decisions.md`へ置いた**（選択肢が並ぶため）
  - `Apic::mask_all`が一度も実行されていない: **宛先なしのまま。S6でも来なかった**（S5と同じ結論）
  - `xtask`が経過時間を出す案: **候補のまま残す**（S6では入れなかった。壁時計時間を判定に使わないと決めてあるので、出すのは観測の便宜である）
  - 起動ログの行の会計を定数で持つ案: **S6-dで候補から外した**（行形の正規化差分に包含される）
  - ADRを書くか: **書かないと決めた**（下記）

  **定義1と定義2は機械で守られていない。** `--full`の項目になっているのは定義3だけである。**残る2つは手順であって検査ではない**（`verification-coverage.md`）。**定義2は「S6を閉じるための測定」で、以後も段ごとに測ることは課していない**——その空白と、埋める案（未採用）も同じ場所に書いてある。

  **刻みは12コミットだった。想定は10である。** 内訳はS6-a 3、S6-b 2、S6-c 3、S6-d 2、S6-e 2。**2倍（20）には達していないので止まっていないが、超過したことを書いておく。** 超過の主な出所は**軸の分割**である——S6-aは既存の検査の誤りの訂正が独立の軸として出て3つになり、S6-cはヒープの静的の移動が振る舞いを変えない単独の変更として分かれて3つになった。**見積りが甘かったのは「1つの段は1つの軸で終わる」と置いたところである。**

  **ADRは書かない。** 4条件に当てた結果、当たるのは条件(1)（複数の有力案から一つを選ぶ）だけである。定義2は「二値」から「傾き」を経て「全標本が同一」へ**2度書き換わっており**、**定義が測定に耐えるかは測る前には分からないことが実証された。** 条件(4)（将来の保守者が単純化すると壊す）は**検査そのものとdocsが担っている**——3つの定義はいずれも実行できる形で残っており、**文章だけの決定ではない。** 条件(2)（作り直しが生じる）は当たらない。**「決定」ではなく「測り方」なので、ここ（roadmap）と`verification-coverage.md`が正である。**
- フル細粒度SMP（性能向上）はロードマップに載せず`vision.md`（長期目標）に据え置く

### ユーザーランドとファイルシステム（M5-f-3とM6）

- [x] **S7. プロセス別アドレス空間**（M5-f-3。ADR-0022。上のM5-f-3節はここへのポインタである）: 下位を差し替え上位=カーネルを共有する。M5-eの暫定配置（`PML4[1]`=512GiB）を通常の低位へ広げる。フレームアロケータの容量とデータ構造の見直しは、この中でプロセスの生成と破棄を実装する直前に行う。`user_range_accessible`と`copy_from_user`のレイアウト・構造条件依存の再評価も含む（`verification-coverage.md`）。到達条件=プロセスAとBの同じ仮想アドレスが異なる物理ページを指すこと、Aの書き込みがBから見えないこと、ユーザーからカーネル領域へアクセスできないこと、CR3切り替え後もカーネルが動くこと、`AddressSpace`破棄後に古いTLBでアクセスできないこと、稼働中テーブルの共有カーネル部分が一致すること。実装しない=fork、Copy-on-Write、共有メモリ、複数スレッド

  **S7はS5の決定を再訪する段でもある。** ADR-0027の失効条件の1つ目（解放・再利用を伴う操作）がここで発火する。**Addendumで閉じた**——**フレームは、全コアの`SEEN_GENERATION`が解放時の世代を追い越すまで配られない。** ackも新しいプロトコルも要らない。

  **不変条件の実装の置き場所は、フレームアロケータのデータ構造の見直しと同じ場所で決める。** 別の隔離リストで持つか、フリーリストのエントリに解放時の世代を刻むかの2案がある。**ADRの対象ではない**（どちらでも不変条件は同じ）。**決めたらここへ書く。**

  **管理構造はヒープではなく固定配列で足りないかを先に問う。** 理由は2つ。**このkernelの様式に合う**（`MAX_CPUS`・`WORKER_COUNT`・フレームアロケータの範囲上限、いずれも固定である）。そして**S6-cで測って確かめた性質が守れる**——「定常経路に解放されない確保は無い」は59標本の実測で裏づけた資産であり、**プロセスの生成と破棄をヒープに載せると、生成・破棄を繰り返す経路が新しい漂流の面になる。** 固定配列で足りればADR-0012にもS10にも触れずに閉じる。**足りないと分かってから扱う。**

  **到達条件6件の反証可能性（着手前に確かめた）。** (1)(2)(4)(5)(6)は反証の形が見えている。(1)と(2)は破壊を共有する（(1)は翻訳が違うこと、(2)はその帰結を見る）。(5)はS5-cのシュートダウンの証明と同じく両側に肯定的な証拠を置ける。**(3)「ユーザーからカーネル領域へアクセスできないこと」だけ、反証の形が未設計である。** 守りの形（プロセスごとのユーザーサブツリー）が決まってからでないと破壊が書けないためである。**(3)は6件の中で最も安全に関わり、空振りが最も高くつく。S7-fの締めで「(3)は反証可能な形になったか」を必ず問うこと。** 他の5件が揃った時点で満足して閉じないために書いておく。

  **崩れる既存の前提（言い換えが要るもの）。** `user_range_accessible`の隔離不能がレイアウトに依存していること（「U=1はユーザーサブツリーの外に無い」がプロセスごとになる）、`UserSlice`の有効期間をCR3を跨がせない担保、`USER_PML4_INDEX` / `USER_VIRT_MIN` / `USER_VIRT_MAX`の見直し。**いずれも`verification-coverage.md`と`deferred-decisions.md`が正である。**

  **TOCTOUの構造条件のうち「ユーザーページをアンマップする経路がsyscall中に走らない」は、既に根拠が入れ替わっている（着手前に確認した）。** `syscall_entry`は`KernelEntry::Syscall`でBKLを取り、**ガードは関数の最後まで生きる。** 定常ループのアンマップも`KernelEntry::SteadyLoop`のBKLの内側である。**BKLは1本なので、片方が保持している間もう片方は入れない。** つまり根拠は「シングルコアだから起きない」から「**アンマップにはBKLが要り、syscallはBKLを保持している**から起きない」へ移っている。**S4で『シングルコア』の根拠がBKLへ移ったのと同じ形である。** **したがってこれは言い換えの1件であって、新しい設計は要らない。**

  **ただし根拠の種類が弱くなっている。ここを書き分ける。** 旧い根拠（シングルコア）は**構造的に真**だった——**コアが1つなら、どう書いても並行しない。** 新しい根拠（アンマップにはBKLが要る）は**すべての経路がそう書かれていることに依存する全称的な性質である。** **現在は真だが、将来1つ経路が増えれば静かに崩れる。** **「根拠が入れ替わった」だけでなく「保証の種類が構造から全称的な性質へ落ちた」と読むこと。** そして**その全称性は機械で守られていない**（`cli`/`sti`やシリアルの口のような許可リストの検査が無い）。

  **刻みは12コミットの想定である。** 内訳はS7-a（Addendum）1、S7-b（世代の読みと隔離）2、S7-c（`AddressSpace`とCR3切り替え）2、S7-d（生成と破棄）2、S7-e（前提の言い換え）2、S7-f（締め）1に、**軸が2つ増える見込み**（既存の誤りが見つかる／振る舞い不変の移動が分かれる。`coding-standards.md`）を足したものである。**S7は最も広い段なので、各刻みの切れ目で累計を報告する。** 24で止まる。

  **S7完了（S7-aからS7-f）。到達条件6件のうち5件が達成、1件が未達である。**

  | 条件 | 結果 | 観測 |
  |---|---|---|
  | 1 同じVAが別の物理を指す | 達成 | AとBで`0x52000`と`0x53000` |
  | 2 Aの書き込みがBから見えない | 達成 | CR3を差し替えて`0xaaaa…`と`0xbbbb…`を読み分けた |
  | 3 ユーザーからカーネル領域へアクセスできない | **未達** | 下記 |
  | 4 CR3切り替え後もカーネルが動く | 達成 | 切り替えた後の行が出る。対照`addrspace-no-kernel-share`はトリプルフォルト |
  | 5 破棄後に古いTLBでアクセスできない | **機構まで** | 下記 |
  | 6 共有カーネル部分が一致する | 達成 | 本番・A・Bの上位256本で食い違い0 |

  **条件3は未達である。着手前に「反証の形が未設計」として追跡すると決めた項目で、設計しなかった。** S7はRing 3へ一度も行っていない——張るのはユーザーページだが、読むのはRing 0からである。**構造の側は前進した**——`AddressSpace`が自分のユーザーサブツリーの添字を持ち、**U=1がその外に存在しないことを空間ごとに監査している**（違反0）。**しかし「Ring 3から触って#PFになる」は観測していない。** **S8（ユーザー例外の区別）で戻る経路ができてから、S9でユーザープログラムを走らせるときに観測できる。** **未達のまま閉じる。**

  **条件5は「機構まで」である。** 破棄したフレームが**全コアが世代を追い越すまで配られない**ことは観測した（破棄の瞬間はアロケータの空きが動かず、追い越してから5枚が返る）。**破棄したVAへ実際に触って#PFになることは観測していない**——戻ってくる#PFの経路が無いので、触れば止まる。**S8待ちである。**

  **S5の決定を再訪した。** ADR-0027の失効条件の1つ目が発火し、**Addendumで閉じた**——**フレームは、全コアの`SEEN_GENERATION`が解放時の世代を追い越すまで配られない。** ackも新しいプロトコルも要らない。**Supersedeしていない。**

  **実装で見つけた欠陥が1件ある。** S7-bで置いた判定が`>`で1世代きつく、**「遅い」ではなく「返らない」だった**（写像を変える操作が他に無ければ世代は進まない）。**順序の誤りも同時に見つかった**（上げてから外すと、上げた直後にフラッシュしたコアがまだ生きている写像を読み直しうる）。**`>=`で足りるのは正しい順序による。**

  **繰り越し（段閉じのgrepで洗った全件。宛先は材料が揃う場所で書く）。**
  - **到達条件3の反証の形**: **Ring 3から触れる構成ができたとき**（S8で戻る経路ができ、S9でユーザープログラムが走る）
  - **到達条件5の直接の観測**: 同上
  - **`unmap_4kib`に`&BklGuard`を要求する形が現実的か**: **`unmap_4kib`の呼び出し箇所を数える必要が生じたとき**（新しいアンマップ経路を足すとき）。`destroy`は型で閉じたが`unmap_4kib`は閉じていない
  - **`USER_PML4_INDEX` / `USER_VIRT_MIN` / `USER_VIRT_MAX`の値の見直し**: **ユーザープログラムを実際に置き、そのアドレスが正しいかを確かめる相手ができたとき**（`deferred-decisions.md`）
  - **`UserSlice`を型で持たせるか**: **`UserSlice`が`syscall`の外へ出る経路ができたとき**（`verification-coverage.md`）
  - **管理構造（プロセス表）**: **走るユーザープログラムが複数になったとき**。S7では要らなかった
  - **フレームアロケータの量を漂流の測定へ載せるか**: **隔離が動く量になったとき**。S7-dで動く量になったので、条件は満たされつつある

  **ADRは書かない。** 4条件に当てた結果、**条件(1)（複数の有力案から一つを選ぶ）は当たる**が、**その決定は既にADR-0027のAddendumが持っている**（ack/隔離/漏らす/静止点の4案から隔離を選んだ）。**S7そのものに残る決定は「上位を共有し下位を差し替える」だけで、これはADR-0024（higher-half）とADR-0022（M5-f分割）の帰結であって新しい選択ではない。** 条件(4)（保守者が単純化すると壊す）は**型と検査が担っている**——`AddressSpace`が添字を持ち、`destroy`が`&BklGuard`を要求し、`addrspace-no-kernel-share`が対照を置いている。**文章だけの決定ではない。**

  **刻みは14コミットだった。想定は12である。** 内訳はS7-a 1、S7-b 2、S7-c 3、S7-d 4、S7-e 2、規律と類型で2。**2倍（24）には達していない。**

  **超過の診断。S6で見込んだ2つの型のうち、効いたのは片方だけだった。** 見込んだのは「**既存の誤りが見つかる**」と「**振る舞い不変の移動が分かれる**」だったが、**実際の超過2件はどちらでもなく「規律や類型が見つかる」だった**（`0e5f01d`と`29b8cc7`）。**見込みの型は当たっていたが、内訳の比率が違った。** **次の段の見積もりでは両方を見込む。**
- [x] **S8. ユーザー例外の区別**: Ring 0の致命的例外は従来どおりfail-fastで停止し、Ring 3の通常の違反は**プロセスだけを終了させてカーネルは継続する**。現在はRing 3由来の違反でもカーネルが止まるので、S9の前提になる。到達条件=Ring 3の #PF・#UD・#GP・#DEで**プロセスだけが終了し**カーネルが継続すること、Ring 0の同じ例外はfail-fastのままであること、例外フレーム自体が壊れている場合にカーネル停止を選べること。実装しない=シグナル、コアダンプ、プロセスの生成と破棄

  **到達条件の文言を着手前に言い換え、S9-b-3-1で元へ戻した。** 元は「プロセスだけが終了し」だったが、**S8ではプロセスの実体を作らない**ので、その字面のままでは実装できない。設計方針(A)——`ring3.rs`の既存の遠征の機構を一般化し、プロセスの実体はS9で作る——を採ったため、いったん「Ring 3の実行だけが中断され」へ言い換えていた。

  **戻した根拠は`exit`である。** S9-b-3-1で`hello`がプロセスとして走り、`exit(0)`でアドレス空間ごと畳まれ、カーネルが続いた。**畳み（Ring 3の例外）で戻る経路も同じところへ合流する**ので、Ring 3の違反も同じくプロセスの終了になっている。**観測は「終了状態が0であること」「消えたフレームと隔離へ入れたフレームが一致すること」「終了後にカーネルが続いたこと」の3つである。**

  **観測していないものも書いておく（S9-b-3-2aで観測できた）。** 「その1本が終わり、**別の1本が始まる**」は、S9-b-3-1の時点では**Ring 3で走るプログラムが`hello`の1本しかないので構成できていなかった。** 走らせる一覧が3本になったS9-b-3-2aで、`fault-test`が畳まれて終わった後に`syscall-test`が始まって正常に終わるところまで観測している。

  **S7から2件を預かっている。** **片側だけの記録では受け取る側が知らないので発火しない**（「ついでに」の失敗と同じ構造である）。**S8の締めで、次の2件を明示的に問うこと。**

  - **S7の到達条件3（ユーザーからカーネル領域へアクセスできない）の反証の形。** S7では未達のまま閉じた。**Ring 3から触って`#PF`になることを観測していない**——S7はRing 3へ一度も行っていない。**構造の側は済んでいる**（`AddressSpace`が自分のユーザーサブツリーの添字を持ち、U=1がその外に存在しないことを空間ごとに監査して違反0）。**足りないのは観測である。**
  - **S7の到達条件5（破棄後に古いTLBでアクセスできない）の直接の観測。** S7では「機構まで」で閉じた。**破棄したフレームが全コアの追い越しまで配られないことは観測したが、破棄したVAへ触って`#PF`になることは観測していない。**

  **どちらもS8が作る「戻ってくる`#PF`の経路」が前提である。** S7の中では原理的に観測できなかった——**触れば止まるので、触れなかった。**

  **預かった2件の決着（S8-f）。1件は決着、1件はS9へ持ち越す。**

  - **条件3は決着した（S8-e）。** カーネル像の先頭`0xffffffff80100000`をRing 3から読み、**CR2が一致し、エラーコードが0x5（存在・ユーザー・読み）であること**まで観測した。**エラーコードまで見るのが要点である**——未マップでも#PFにはなるので、「触ったら落ちた」では条件3の主張にならない。**0x4（不在）と0x5（存在するが権限で拒否）の区別が、条件3が言っていたことそのものである。** 判定行は参照の起動ログに入っている。
  - **条件5はS9へ持ち越す。理由は原理的なものである。** `AddressSpace`の破棄はCR3の載せ替えを伴い、**載せ替えは非グローバルのTLBを全部落とす**（Gビットは立てておらず、起動時に検査している）。したがって**単一コアでは「破棄した後に古い翻訳が残っている」状態を作れない。** 作れるのは破棄した空間を別のコアがまだ見ている場合で、それは**APがその空間でタスクを走らせていること、すなわちプロセスの実体を要求する。** S8は設計方針(A)でプロセスの実体を作らないので、S8の範囲では構成できない。**S9へ渡す。**

  **S9への申し送り（片側だけにしない）。** S9の記述に「S8から1件を預かっている」として条件5を引き取らせること。**S7からS8への引き渡しで有効だった形をそのまま使う。**

  **S8完了（S8-aからS8-f）。到達条件3件はすべて達成した。**

  **条件1（Ring 3の4ベクタで中断+継続）。** #DE・#UD・#GP・#PFを1本ずつ、計5本の遠征（#PFは未マップのユーザーVAとカーネルVAの2本）で観測した。**判定行はベクタごとに分けてある**——1つにまとめるとどのベクタで落ちたかが読めない。**判定行が続けて出ること自体がカーネル継続の観測である**（2本目が出た時点で、1本目の中断から戻って次の仕事へ進んだことが確定する）。**フォルトするのは先頭の命令とは限らない**（#DEは0除算の準備の後、#PFはアドレスを積んだ後）——ベクタごとにRIPを主張する形にしていたので、最初の見積もりの誤りをこの主張が捕まえた。

  **条件2（Ring 0はfail-fastのまま）。** 新しい検査を作らず、既存の4件（`exception-test`の divide-by-zero / invalid-opcode / page-fault と、`ring3-test user-desc-dpl0`のRing 0の#GP）が引き続き緑であることで示す。構造上そうなるのは、畳みの条件からCS.RPL==3を緩めなかったためである。

  **条件3（フレームが壊れていればカーネル停止を選べる）。** `ring3-test corrupt-frame-cs`（主=CSが既知のセレクタでない）と`ring3-test drop-rsp0`（従=ハンドラが想定のスタックにいない）が、畳まずに停止することを示す。**畳む対象を4ベクタへ広げた後の構成でも両方が効くことを、締めで単独実行により確かめた。**

  **反証の限界を書いておく。** 畳みを落とす破壊（`no-fold-flag`）は**共有機構について1本だけ**である。**「4ベクタとも畳まれないことを確かめた」とは言えない**——あの破壊は最初の遠征で止まるので、4本の遠征には到達しない。示しているのは畳みの条件が生きていることまでで、4ベクタ個別の肯定的な観測は判定行が持つ。ベクタを選べる破壊を4本置く案は、**畳みの条件がベクタごとに分岐しない**ため採らなかった（失効条件つきで`FOLDABLE_VECTORS`のdocに記録した。S9でシグナルやプロセス終了が入って分岐したら再検討する）。

  **ADRは書いた（ADR-0004のAddendum）。** 4条件のうち条件(1)（複数の有力案から一つを選ぶ。フレームの健全性判定の候補5つから主と従を選んだ）と条件(2)（後から変更すると作り直しになる。畳む境界を後から変えると、Ring 3から到達できる範囲の前提が全部動く）が当たる。**`deferred-decisions.md`が「ADR-0004のAddendumとして書く」と指定していた**ので、新規ADRにはしていない。

  **S8で見つけた欠陥は3件、いずれも自分の変更が既存の検査に捕まった。** (a) `assert_folded_at`を`paging-test`のcfgで落として、cfgが外れない呼び出し元2つがコンパイルできなくなった。(b) フレームの健全性の従を、ゲートのIST番号ではなくベクタから決め打ち、`stack-overflow-df-test`（#PFにISTを与えない構成）で正当なフレームを破損と判定した。**「S8-cで実際に通るのは#GPの腕だけ」という限定は既定ビルドについてのもので、破壊ビルドでは通っていた。** (c) #DEの期待RIPを+0と書いた（落ちるのは3命令目）。

  **`smp-ap-test tlb-no-shootdown`が決定的に落ち、検査の側の誤りと判明した。** 詳細は`verification-coverage.md`の「S5-cの対の主張は片側が過剰だった」。**カーネルの欠陥ではない**——TLBが翻訳を保持し続けることはアーキテクチャが保証せず、TCGのソフトウェアTLBがレイアウト依存でエントリを捨てていた。対の主張を非対称に作り直した（保証される側だけを主張する）。**規約「アーキテクチャが保証しない性質を判定の根拠にしない」はここから出た。**

  **`--full`は114から115へ、1しか増えていない。足した検査は1本（`ring3-test corrupt-frame-cs`）だけである。** `no-fold-flag`は既存で、**4ベクタの観測とS7の条件3の観測は、検査項目ではなく既定ビルドの判定行として置いた**（`ring3-vectors`の5本＋要約2本。参照の起動ログが差分で守る）。**遠征を5本足したのに項目が+1なのは、観測を検査項目にしなかったという判断の結果である**——項目数は段の規模の目安に使われてきたので、数字だけからは読めないこの判断を書いておく。**破壊は項目にし、観測は判定行にする**という置き分けで、前者は「壊したら落ちること」、後者は「既定で成り立つこと」を守る。

  **刻みは11コミットだった。想定は10である。** 内訳はS8-a 1、S8-b 1、S8-c 1、S8-d 2、S8-e 1、S8-f 4に、S8の外で見つけて単独コミットにした記録2件（`map_4kib`のW^X、APの制御レジスタ）を足したもの。**20（2倍）には達していない。** 想定を1つ超えたのは、検査の作り直し（`tlb-no-shootdown`）が段の途中で必要になったためで、**これは着手前に見えていなかった軸である。**
- [x] **S9. 埋め込みELFからのユーザープログラム起動**: ファイルシステムに依存せず、ELFローダー・アドレス空間・Ring 3遷移を検証する。対応範囲はELF64・x86_64・静的リンク・非PIE・`PT_LOAD`のみ。**ユーザーELFの読み込みは、いかなる入力に対してもカーネルをfail-fastさせず、プロセスを終了させるエラーを返す**（`common::elf`は信頼済みの`kernel.elf`向けで境界検査を持たない箇所がある。共有はパース骨格までとする）。到達条件=hello・syscall-test・fault-testが動くこと、壊したELFでカーネルが止まらずプロセスだけが失敗すること。実装しない=動的リンク、共有ライブラリ、PIE

  **S8から1件を預かっている。** **片側だけの記録では受け取る側が知らないので発火しない**（S7からS8への引き渡しで有効だった形をそのまま使う）。**S9の締めで、次の1件を明示的に問うこと。**

  - **S7の到達条件5（`AddressSpace`破棄後に古いTLBでアクセスできない）の直接の観測。** S7は「機構まで」、S8は原理的に構成できずに持ち越した。**破棄はCR3の載せ替えを伴い、載せ替えは非グローバルのTLBを全部落とす**ので、単一コアでは古い翻訳が残る状態を作れない。**作れるのは、破棄した空間を別のコアがまだ見ている場合だけである。** S9でプロセスができ、APがその空間でタスクを走らせるようになったら構成できる。

  **S8から引き継ぐ判断が2つある。** **到達条件の文言**——S8は「プロセスだけが終了し」を「Ring 3の実行だけが中断され」へ言い換えた（プロセスの実体が無いため）。**S9でプロセスができたとき、元へ戻すかを判断すること。** 戻すなら、中断がプロセス終了に変わったことの観測が要る。**畳みの反証の形**——現在は共有機構について破壊1本で、ベクタごとには置いていない。**シグナルやプロセス終了が入って畳みの条件がベクタごとに分岐したら、4本置く案を再検討すること**（`idt/mod.rs`の`FOLDABLE_VECTORS`のdocに失効条件として書いてある）。

  **S9-aからS9-bへ渡すもの（片側だけの記録にしない）。**

  - **`USER_PML4_INDEX`の値。** S9-aでは動かしていない。解禁条件は「ユーザープログラムを実際に置き、そのアドレスが正しいかを確かめる相手ができたとき」で、**それはS9-bである。** `syscall::USER_VIRT_MIN`/`USER_VIRT_MAX`と、`verification-coverage.md`の「U=1はユーザーサブツリーの外に無い」に依存する検査の再評価が同時に来る
  - **`W^X`の`X`の側。** S9-aで入れたのは`W`だけである。`EFER.NXE`の有効化は別項の解禁条件（「実行してはいけないページを張る必要が生じたとき」）に従う。**`PT_LOAD`で`X`の立っていない区画を写像に落とすなら、そこで発火する**
  - **ELFパーサが扱わない5件。** `p_vaddr`の範囲、セグメントの重なり、`p_align`、`PT_LOAD`が0個、エントリポイントが`PT_LOAD`の中にあるか。**いずれも配置の方針を決めてからでないと判定できないので、ロードする側で行う**（`common/src/elf.rs`のモジュールdocに列挙してある）（**S9-b-3-2bで数え直した。** 5件のうち2件は既定ビルドが見ており、1件は`map_user_4kib`が実質の判定を持ち、1件は読んでいないので害が無い。**残る穴は区画の重なり1件である**）
  - **`DirectMap::identity`の5箇所が恒等除去より前に走ること。** B-2からの繰り越しで、`deferred-decisions.md`に記録がある。**条件つきの安全であり、起動シーケンスの順序を変えると壊れる。** S9-bはユーザープログラムのロードを起動シーケンスへ入れるので、**順序を触るなら再確認すること**
  - **`ring3::enter`の引数化には、まだ観測が無い**（**S9-b-1で解消した。** `hello`のentry `0x400010`は既存の遠征の飛び先`0x8000000000`と別で、引数で渡した飛び先で実際に走った）

  **S9-b-1（`hello`が走るまで）は完了した。** 埋め込んだELFを読み、専用のアドレス空間へ写像し、Ring 3で走らせ、`write`が届き、`ud2`が畳まれてカーネルが続く。**S9の到達条件のうち`hello`の分が揃った。** 残るのは`syscall-test`・`fault-test`と、壊したELFの扱いである。

  - **到達条件は4つとも達成した。** 畳んだベクタが6、フォルト RIP が`entry + 0x20`、CSのRPLが3、`write`がfd=1で18バイトを届け中身が一致する
  - **S9-aの未達1件が解消した。** `ring3::enter`の引数化は「飛び先が2つ以上にならないと観測できない」として未達のまま閉じたが、`hello`のentryで実際に走った
  - **S7-cの到達条件4（CR3を切り替えてもカーネルが動く）が、実プログラムで初めて使われた**
  - **到達しないと宣言したものは、そのとおり到達していない。** プロセスの実体は無く、`exit`も無い。`USER_PML4_INDEX`の値は動かしていない。NXも入れていない

  **刻みは11コミットだった。想定は10、上限は20である。** 内訳は機能4（埋め込み・写像APIの波及・写像・実行）、記録6、訂正1。**S9-aで更新した見込み（記録は作業と同数程度）が当たった**——作業4に対して記録6で、S9-aの4対10よりも比が下がっている。**見込みの立て方としては、S9-aの実績が上振れだったことになる。**

  **S9-b-2（壊した像の扱い）は完了した。** 埋め込んだ像を壊してパーサへ渡し、ローダーへ渡し、どちらでもカーネルが止まらないことを見る。

  - **到達条件のうち「壊したELFでカーネルが止まらない」は満たした。** `ElfError`の11種すべてがパーサに拒まれ、ローダーの中で拒まれる2種（入口の`Parse`と、途中の`Mapping`）も置いた。**いずれも既定ビルドで毎回走る**
  - **「プロセスだけが失敗する」は言い換えたが、S9-b-3-1で戻した。** 言い換えた時点ではプロセスの実体が無く、**言えるのは「ロードが失敗し、カーネルは継続する」までだった。** S8が同じ理由で「プロセスだけが終了し」を「Ring 3の実行だけが中断され」へ言い換えたのと同じ形である。**S9-b-3-1でプロセスの実体と`exit`ができ、失敗した像もアドレス空間ごと畳まれる形になったので、元の文言へ戻した**（S8の項に観測を書いた）
  - **後始末まで含めて主張している。** 途中で落ちた場合、そこまでに張った写像と中間テーブルを畳み、**消えたフレーム数と隔離へ入れた数が一致すること**を見る。**この会計が実害を1件見つけた**（写像に失敗したページのフレームが`destroy`から見えず戻っていなかった。`verification-coverage.md`の「`leaked=0`は『漏れていない』を意味していなかった」）

  **刻みは4コミットだった。想定は9、上限は18である。** 内訳は機能3（`Result`化、パーサの11種、ローダーの2種）、記録1。**宣言を大きく下回った。** 見込みで「記録4・訂正1」としたが、**記録は1、訂正は0だった。**

  **下回った理由は、S9-b-1の実績を根拠にしたことである。** S9-b-1は新しい機構（埋め込み、写像、実行）を置く段で、**やり方についての発見が多かった。** S9-b-2は**既にある機構へ入力を通すだけ**で、**新しい型の発見が出にくい。** 見込みの立て方としては「作業と同数程度」より「その段が新しい機構を置くかどうか」で分けるほうが当たる。

  **分類には機序がある。** 新しい機構を置くと、置き方そのものについての発見が出る（詰め物が消える、詰め物が実行される、層が入れ替わる）。既にある機構へ入力を通すだけなら、**通す入力が増えても、やり方は既に決まっている。** 数字だけの相関ではない。

  **ただし標本が少ない。** 「新しい機構を置く段」が2件（S9-a 4対10、S9-b-1 4対6）、「既存へ通す段」が1件（S9-b-2 3対1）である。**前者の2件も2.5倍と1.5倍で開いている。** 3段で傾向を語るのは早い。

  **予測を書いておく。S9-b-3は「新しい機構を置く段」である**（プロセスの実体を作る）。**したがって記録は作業と同数程度になると予測する。** S9-b-3の締めで突き合わせること。**外れたら分類のほうを疑う。**（**S9-b-3-2bの締めで突き合わせた。** 記録は作業と同数どころか上回った。**ただし尺を件数へ変えたので、予測とは別の量を測っている**——下記）

  **確率的な4項目を`--full`から外す作業は、この刻みに数えていない**（2コミット）。**S9-b-2の範囲外の独立した作業である。**

  **段閉じのgrepでは、古くなった記述が0件だった。** 説明は2つありうる——**申し送りを書いた側と記録を書いた側がどちらもこの段で書かれたばかりだから**と、**S9-b-2が4コミットの短い段で、古くなる時間が短かったから**である。**どちらが効いていたかは、次に0件でないときに分かる。**

  **段の完了時の`cargo xtask flaky`（初回）。** 4項目とも**1回目で通った**（`interrupt-test keyboard` / `smp-ap-test tlb-shootdown` / `smp-stimulus-layer1-off` / `smp-stimulus-both-layers-off`）。**通ったことは「揺らぎが消えた」の証拠にはならない**（`coding-standards.md`の「5回の緑では足りない」）。**回数を残すのは、次に回したとき何回目で通ったかを比べるためである。**

  **S9-b-3への申し送り。**

  - **プロセスの実体。** 到達条件の文言を戻すかの判断、プロセス表を持つか、フレームアロケータの容量（256）の再評価。**S7の到達条件5（破棄後の古いTLB）はAPがその空間を走らせて初めて構成できる**
  - **ポインタ検証の窓を1つに畳む。** 現在は起動時の検証用とユーザープログラム用の2つ。**カーネルが現在のアドレス空間を追跡できるようになってからである。** 畳むとき、下限を4 MiBに置いた理由（起動時の検証が恒等除去より前に走る）も一緒に扱う
  - **`ADR-0027`と`ADR-0023`の失効条件。** どちらも「Ring 3が定常状態で走るとき」で、**S9-b-3で発火する**（**発火しなかった。** S9-b-3の方針が「プロセスの実体は作るが、実行は起動シーケンスの直線上で順に行う」になったためで、**定常状態では走らせていない。** 両ADRの論証が依存する「遠征がAP起こしより前の単一コア区間に閉じている」は保たれている。**条件はS11のシェルへ持ち越す**）。**S11-11で発火し、処理済みである**——シェルが定常状態で走るようになり、両ADRに評価のAddendumを足した。**どちらも決定は生き延び、`ADR-0023`は根拠が置き換わった**（「単一コア区間に閉じている」から「戻った先が、per-CPUか自前の排他を持つものしか触らない」へ）
  - **`UserLoadError`の`Display`。** 現在`virt`が10進で出る。宛先は**ログへ出す箇所が2つ以上になったとき**
  - **S9-aから持ち越した3件**（`USER_PML4_INDEX`の値、`W^X`の`X`、ELFパーサが扱わない5件のうち残り）

  **S9は完了した。** 到達条件を2つとも達成し、副段は6つ（S9-a、S9-b-1、S9-b-2、S9-b-3-1、S9-b-3-2a、S9-b-3-2b）だった。

  **到達条件の突き合わせ。**

  - **hello・syscall-test・fault-testが動く。達成。** 3本が起動シーケンスの直線上を順に走る。`hello`は`write`して`exit(0)`、`fault-test`は自分の`.text`へ書いて#PFで畳まれ、`syscall-test`はasmで組み立てた引数でABIの契約を確かめて`exit(0)`
  - **壊したELFでカーネルが止まらず、プロセスだけが失敗する。達成。** パーサが拒む11種とローダーが拒む3種が、既定ビルドで毎起動走る。**プロセスの終了に変わったのはS9-b-3-1で、S8が言い換えた文言をそこで元へ戻した**
  - **実装しないと宣言したものは、そのとおり実装していない。** 動的リンク、共有ライブラリ、PIE

  **副段ごとに、置いたものと見つけたもの。**

  - **S9-a（振る舞い不変の土台）。** ELFパーサの境界検査、写像APIの`PageAttributes`、独自のsyscall番号を`ZAYTOS_PRIVATE_BASE`の上へ、`ring3::enter`の引数化。**見つけたのは「`SAFETY`が根拠にしている契約がそもそも存在しない」形と、「誤りは通らない場所にあると見つからない」形である**
  - **S9-b-1（`hello`が走る）。** `rustc`で建てて埋め込み、新しい空間へ写像し、Ring 3で走らせ、`write`が届き、`ud2`で畳んで戻る。**見つけたのは「詰め物は実行される」（ゼロ埋めは`add [rax], al`になる）と、「破壊が届く範囲は最も早い観測まで」である**
  - **S9-b-2（壊した像）。** パーサで拒む11種、ローダーで拒む2種。**会計（消えたフレームと隔離へ入れた枚数の一致）が実害を1件見つけた**——写像に失敗したフレームが`destroy`から見えず戻っていなかった
  - **S9-b-3-1（プロセスの実体と`exit`）。** `exit`が`leave_ring3`で戻る経路と、BKLを出口の外で解く形。**見つけたのは「取る入口に出口を通らない経路ができる」形と、`FAULT_CS`の戻し忘れである**
  - **S9-b-3-2a（`fault-test`と`syscall-test`）。** 期待する終わり方の表、ABIの二重の突き合わせ。**未達だった「1本が終わり別の1本が始まる」がここで揃った**
  - **S9-b-3-2b（整理）。** ポインタ検証の窓の統合、ELFの検査の3分割、docの付け先。**数え直しが穴を1つ見つけた**——区画が重なる像が受理されて1枚漏れ、**漏れが会計に出てカーネルが止まっていた**

  **未達4件。いずれも「やり残し」ではなく「まだ来ていない」である**（条件が発火していない）。

  - **S7の到達条件5（破棄後の古いTLB）。** APがその空間でタスクを走らせることを要求する。**直線上で走らせる限り構成できない。S11で構成できる**（**S11でも構成できなかった。** シェルは定常状態で走るが、走らせているのはBSPで、**APはRing 3へ入らないままである。****条件の本体は`kernel/src/ring3.rs`の`enter`へ移した**——段名で書き続けても発火しない）
  - **`USER_PML4_INDEX`の値。** 条件は「起動時の検証3本を恒等除去より後ろへ動かすとき」。**S9のプログラムは自分の空間の`PML4[0]`を使ったので、本番の`PML4[1]`を確かめる相手は現れなかった。****段名で書いていたら誤って発火と読んでいた**（`deferred-decisions.md`に書いた）
  - **`W^X`の`X`の側。** 条件は「実行してはいけないページを張る必要が生じたとき」。**`fault-test`を#PFで畳む形にしたので発火していない**
  - **`UserLoadError`の`Display`。** 条件は「ログへ出す箇所が2つ以上になったとき」。**1つのままである**

  **刻みはS9全体で56コミットだった**（S8の締めの次の`934edcd`から、この締めまで。`git rev-list --count`で実測）。内訳はS9-aが17、S9-bが39である。**S9-bがS9-aの2.3倍で、副段の数（1対5）ほどには開いていない。**

  **記録は件数で36だった。** S9-a 10、S9-b-1 6、S9-b-2 1、S9-b-3-1 6、S9-b-3-2a 6、S9-b-3-2b 7。**「新しい機構を置く段」と「既にある機構へ入力を通すだけの段」で比が違うという分類は、6段を通して保たれた**——S9-b-2だけが1で、他は6以上である。**ただし尺を件数へ変えたのはS9-b-3-1の締めなので、前半3段はコミット数から数え直した値である。**

  **段の完了時の`cargo xtask flaky`。** 4項目とも**1回目で通った。5回連続で1回目である**（S9-b-2、S9-b-3-1、S9-b-3-2a、S9-b-3-2b、ここ）。**`coding-standards.md`の「5回の緑では足りない」が言うのは「緑が続いても直ったとは言えない」ことなので、これは「直った」の証明ではない。****記録に値するのは、比べる材料が5つ揃ったことのほうである**——次に1回目で通らなかったとき、それが初めての例外だと分かる。

  **段閉じのgrep（`S9`で全docs。これまでで最も広い）。** **古くなった記述が3件見つかり、3件とも直した。**

  - **`ADR-0020`の「ABIの中身はS9で発火する」。** 発火して実施したので、何をしたかを足した
  - **`deferred-decisions.md`の「プロセス終了に変わるのはS9でプロセスができてから」。** S9-b-3-1で実施したので、その旨を足した
  - **`USER_PML4_INDEX`の条件。** **S9が終わっても発火していない**ことを書いた。**これが3件のうち最も重要である**——**段名を目安として書き、条件は材料で書いた形が、正しく判定したことの実例だからである**

  **S10への申し送り。**

  - **着手前に決めることが4件ある**（roadmapのS10の項と`deferred-decisions.md`）。**薄いVFSトレイト境界を先に引くか**、**ext2のパラメータ**、**イメージの持ち方**（カーネルへ埋め込むか、bootloaderが別モジュールとして読むか）、**カーネルヒープの方針**（現在1MiB固定で拡張機構が無い）
  - **ヒープの拡張は、S9で作ったものと噛み合う。** ユーザープログラムのロードはフレームアロケータから直接取っており、**ヒープを使っていない。** ext2のイメージとVFSの構造体はヒープに載るので、**1MiBで足りるかを先に測ること。** 拡張方式を選ぶときは`mmap`の土台になるかを判断材料に入れる（roadmapのS10）
  - **イメージの持ち方は、S9-b-1の`include_bytes!`がそのまま使える。** `build.rs`が`rustc`を呼ぶ形も、`mke2fs`を呼ぶ形へ置き換えられる。**ただしS9のユーザープログラムは8KiB程度で、ext2のイメージは桁が違う。** カーネルの`.rodata`が数MiB増えることの影響（起動時のコピー、`__kernel_end`の位置、`PML4[511]`のPT枚数）を先に見ること
  - **S9で作ったもののうちS10で効くもの。** `UserSlice`（検証済みユーザー範囲の証明トークン）は、ファイル関連syscallが**ユーザーのバッファを読み書きする**ので、そのまま使う。**`validate_user_range`の窓は1つに畳んである**ので、プロセスが増えても形は変わらない。`common::elf`の「どんな入力でもパニックしない」の書き方は、**ext2のパーサにそのまま当てはまる**（S10の到達条件が同じことを求めている）
  - **`syscall`/`sysret`への差し替えはS10でも来ない**（`ADR-0020`のAddendum 2。条件はユーザーTLSかLinuxバイナリの実行）

  **S9-b-3-2b（整理）は完了した。これでS9-bのすべての副段が終わった。S9自体はまだ閉じない**——締めを別の刻みにする（下記の判断）。

  **S9-b-3-2bは到達条件を持たない段である**（窓の統合・ELFの数え直し・docの付け先）。**その代わり、S9の到達条件がすべて揃ったことをここで確認する。**

  - **hello・syscall-test・fault-testが動く。** 3本が順に走り、`hello`は`exit(0)`、`fault-test`は#PFの畳み、`syscall-test`は4回のシステムコールを通って`exit(0)`（S9-b-3-2aで達成）
  - **壊したELFでカーネルが止まらず、プロセスだけが失敗する。** パーサが拒む11種と、ローダーが拒む3種（入口の`Parse`、`NotPrivate`、`AlreadyMapped`）。**いずれも既定ビルドで毎起動走る。****3種目はS9-b-3-2bで足した**——数え直しで、区画が重なる像が受理されて1枚漏れ、**漏れが会計に出てカーネルが止まっていた**ことが分かった。**「止まらない」に反していたので直した**
  - **1本が畳まれて終わり、次が始まって正常に終わる**（S9-b-3-2aで達成）

  **未達で次へ送るものが4件ある。いずれも条件が発火していない。**

  - **S7の到達条件5（破棄後の古いTLB）。****APがその空間でタスクを走らせることを要求するので、直線上で走らせる限り構成できない。****S9でも達成しない。** 構成できるのは定常実行の段（S11）である（**S11でも構成できなかった。** 定常実行になったのはBSPの側で、**要求されているのはAPがその空間を走らせることである。** 2つは別である。**条件の本体は`kernel/src/ring3.rs`の`enter`へ移した**）
  - **`USER_PML4_INDEX`の値。** 解禁条件は「起動時の検証3本を恒等除去より後ろへ動かすとき」で、**動かしていない**
  - **`W^X`の`X`の側。** 解禁条件は「実行してはいけないページを張る必要が生じたとき」で、**`fault-test`を#PFで畳む形にしたので発火していない**（発火させると`EFER.NXE`の有効化から芋づるで、独立の段の仕事になる）
  - **`UserLoadError`の`Display`。** 宛先は「ログへ出す箇所が2つ以上になったとき」で、**1つのままである。****発火していない条件を先回りで実装しない**

  **刻みは8コミットだった。宣言は4、上限は8である。上限ちょうどである。** 内訳は機能2（窓の統合、区画の重なり）、記録3、docの付け先2（訂正と書き直しを別にした）、締め1。

  **超過の出所は2つで、どちらも特定できている。**

  - **数え直しで見つかった穴を、同じ段の中で直した**（区画の重なり）。**整理の段で機能変更が1つ増えた**
  - **訂正と書き直しを別コミットにした**（付け先の訂正は移した文字列が1バイトも変わっていないことを差分で示せるが、書き直しはそれができない。混ぜると検証できる変更が検証できない変更に埋もれる）

  **記録は件数で7だった**（コミット数ではない。S9-b-3-1で尺を変えた）。
  (1) 窓の統合で条件が構造の帰結になったこと（「選べなくする」の適用4件目として数えた）、
  (2) 据える処理を通り道へ置く形（**「何を書けるか」ではなく「どこに置くか」の別の軸**）、
  (3) ELFの検査の3分割（**「パーサが扱わない」と「誰も見ていない」は別**）、
  (4) 区画の重なりが上書きされていたこと（**契約を対で照合する形の再発2件目**）、
  (5) Safety照合の範囲の限界（**平文のdocに書かれた契約は範囲外だった**）、
  (6) 会計が想定を超えて働いたこと（**会計が無ければ漏れは静かに起きていた**）、
  (7) 統合の合図のカウント1件目（**2つの経路が同じ性質を持つべきなのに片方だけが持っていた**）。
  **予測との突き合わせ**——S9-b-2の締めで「S9-b-3は新しい機構を置く段なので記録は作業と同数程度」と予測した。**S9-b-3全体では作業4に対して記録13で、上回った。** ただし**尺を件数へ変えたのはS9-b-3-1の締めなので、予測とは別の量を測っている。** 比較できるのは「コミット数で数えた場合」で、そちらなら作業4対記録5である。**予測は当たっていた側に近い。**

  **段の完了時の`cargo xtask flaky`。** 4項目とも**1回目で通った**。**4回連続で1回目である**（S9-b-2の締め、S9-b-3-1の締め、S9-b-3-2aの締め、ここ）。**それ自体が観測である**——`coding-standards.md`の「5回の緑では足りない」に照らせばまだ足りないが、**4回連続は「揺らぎが常に出る」形ではないことを示している。**

  **段閉じのgrepの結果。** `S9-b-3-2b`・`S9-b-3`・`S9-b`で全docsを洗い、**古くなった記述が2件見つかった。** どちらも**予告の側**で、S9-b-3-2aの締めと同じ形である。

  - **「S9-b-3の締めで突き合わせること」**（記録の量の予測）——**突き合わせたので、その旨を書き足した**
  - **「`ADR-0027`と`ADR-0023`の失効条件はS9-b-3で発火する」**——**発火しなかった。** S9-b-3の方針が「直線上で順に走らせる」になったためで、**予告が外れた側である。****時制ではなく内容が外れたのは今回が初めてである**（前回の2件はどちらも時制だった）

  **S9への申し送り（締めの刻みで使う）。**

  - **S9の到達条件はすべて揃った。** 上の3件で、**S9-b-3-2bの3種目の追加で最後の1つが閉じた**
  - **未達4件は上に列挙した。いずれも条件が発火していない**ので、「やり残し」ではなく「まだ来ていない」である。**区別して書くこと**
  - **`--full`は120、`cargo xtask check`は20、`flaky`は4である**
  - **S9-bの通算は39コミットである**（S9-a完了の`de0eae0`からの実測。この締めを含む）

  **S9-b-3-2a（`fault-test`と`syscall-test`）は完了した。S9はまだ閉じない**——残るS9-b-3-2b（窓の統合、ELFの数え直し、docの付け先）は**到達条件ではなく整理である。**

  - **S9の到達条件「hello・syscall-test・fault-testが動くこと」が揃った。** 3本が順に走り、`hello`は`exit(0)`、`fault-test`は#PFの畳み、`syscall-test`は4回のシステムコールを通って`exit(0)`で終わる
  - **S9-b-3-1が未達で送った1件が決着した。** 「その1本が終わり、**別の1本が始まる**」は、**`fault-test`が畳まれてアドレス空間ごと消えた後に、`syscall-test`が生成されて正常に終わる**ところで観測できる。**順序に意味がある**——失敗するプロセスの後ろに成功するプロセスを置かないと、畳んだ後に何かが始まるところを見せられない
  - **`fault-test`は#PFで畳む。`W^X`の`X`の解禁条件は発火させていない。** 実行してはいけないページへ飛ぶ形にすると`EFER.NXE`の有効化が要り、既存エントリの読み戻し検証・direct mapとユーザースタックのNX化まで付いてくる。**独立の段の仕事である。****発火しうる場所で、発火させない判断を明示的に記録した**（`userland/fault-test.rs`のdoc）
  - **`syscall-test`が確かめているのは`dispatch`ではない。** 起動時のprobeはユーザールーチンの機械語を**カーネルがRustで組み立てている**ので、ABIの符号化は著者の手にある。こちらは**asmで書き、アセンブラが符号化し、リンカが配置し、ローダーが写像し、`iretq`が飛ばす。****確かめているのは`ADR-0020`が決めたABIの契約そのものである。判定行にそう書いた**（`user-abi`の行）
  - **二重に見ている。** カーネル側は`dispatch`が受け取った6引数を既知値と突き合わせ、ユーザー側は戻り値を自分で検算して`exit`の終了状態で返す。**終了状態の一致はS9-b-3-1で判定行になっており反証も置いてあるので、新しい観測路を作らずに済んだ**
  - **終了状態の値には意味を持たせた。** どの検算が落ちたかは値でしか分からないので、**値と意味の対応をユーザープログラムのdocとカーネルの`SYSCALL_TEST_STATUS`の両方に置き、判定行が両方を出す。****`user-exit-wrong-status`の期待マーカーに値を入れなかったのとは逆で、こちらは値そのものが情報である**
  - **`--full`は120のまま。項目は増えていない。** 新しい破壊featureを置いていないためで、**理由は下記**
  - **段の完了時の`cargo xtask flaky`。** 4項目とも**1回目で通った**。S9-b-2の締め・S9-b-3-1の締めに続いて**3回連続で1回目である**

  **反証を置かなかった理由を書いておく。** `fault-test`の畳みと`syscall-test`のABIの突き合わせには破壊featureが無い。**原因の側を壊す破壊は既にあり、先に落ちるだけである**——`W`が効いていない状態は`hello`の段階で捕まり、ABIの置き違えは起動時のbatteryで捕まる。**両プログラムとも`hello`と同じローダー・同じ写像・同じ入口を通るので、固有の経路が無い。** 手で計4通り壊して判定行が止まることは確かめた（`verification-coverage.md`）。

  **刻みは5コミットだった。宣言は4、上限は8である。** 内訳は機能2（`fault-test`と表、`syscall-test`）、記録2、締め1。**1つ超えた出所は記録である**——宣言の時点では記録を1つと見ていたが、族の名前を広げる作業が独立したコミットになった。

  **記録は件数で6だった**（S9-b-3-1の締めで尺を変えた。コミット数ではない）。
  (1) `fault-test`の像は詰め物の生存を**結果でしか見ていない**こと（`hello`は原因の側で見ている）、
  (2) 族「1つしかない前提の記録」の3件目と、**名前を広げたこと**、
  (3) **予約番号**（未実装の番号を借りると検査が黙って主張を変える）、
  (4) 出口が3種類になったときに判定行の作りをどう変えたか、
  (5) 破壊が届かないのは「原因」ではなく「先に落ちた後にしか見えない差」であること、
  (6) `LAST_ARGS`が持つのは「直近の1回」で、**主張したい1回には枠が要る**こと。
  **うち3件はコードのdocに置いてある。** 見込みは6〜10で、**下限に当たった。**

  **段閉じのgrepの結果。** `S9-b-3-2a`と`S9-b-3-2`で全docsを洗い、**古くなった記述が2件見つかった。**
  **0件でなかったのは今回が初めてである**（S9-b-2・S9-b-3-1は2回続けて0件だった）。
  どちらも**予告の側**だった——「その1本が終わり、別の1本が始まるのはS9-b-3-2で観測できる」（2箇所）と、「同じ問いはS9-b-3-2で起きる」（出口が増えたときの観測）。
  **予告は当たっていたので、内容の誤りではない。古くなったのは「これから起きる」という時制である。**
  **0件でなかった理由は説明できる**——**S9-b-3-1が未達を明示的に次へ送ったからである。** 送った先で決着すれば、送った側の記述は必ず古くなる。**申し送りを書くほど、段閉じのgrepで拾うものが増える。** これは費用ではなく、**片側だけの記録にしない運用が働いている証拠である。**

  **S9-b-3-2bへの申し送り。**

  - **ポインタ検証の窓の統合。** 設計はS9-b-3-2aの着手前に決めてある——**「今Ring 3が使っている窓」を1つ持ち、`ring3::enter`に入る側が据えて出るときに戻す。** 起動時のbatteryは`[512 GiB, 1 TiB)`、プロセスは自分の空間の添字から導いた窓を据える。**またぐ範囲が受理されないのは「窓が1つしかないから」になり、条件が消える。** **下限4 MiBは方針として据え直す**（理由が「起動順の偶然」から「null近傍を範囲の側で拒む層」へ変わる。同じ値を違う根拠で持つ）。**`syscall-test validate-skip-us`がまだ落ちることを確かめること**——あれは下限を0にしたときに受理される形を捕まえた破壊である
  - **ELFパーサの件の数え直し（S9-b-3-2bの2本目で済ませた）。** **「パーサが扱わない」と「誰も見ていない」は別である。** (a)パーサが扱わない5件、(b)そのうち信頼済みの像について既定ビルドが見ている2件（`PT_LOAD`が0個、entryが`PT_LOAD`の中）、(c)信頼できない像について実際に何が起きるか、の3つに分けた。**残る穴は区画の重なり1件で、実測すると1フレーム漏れて会計が止める**（`verification-coverage.md`）
  - **`load_user_program_into`のdocの付け先。** **訂正（移すだけ）と書き直し（古い内容を直す）を別コミットにする**
  - **`UserLoadError`の`Display`**（宛先=ログへ出す箇所が2つ以上になったとき）
  - **S9-aから持ち越した3件。** `USER_PML4_INDEX`の値（**未着手。解禁条件は「起動時の検証3本を恒等除去より後ろへ動かすとき」で、発火していない**）、`W^X`の`X`（**S9-b-3-2aでは発火させていない。`fault-test`は#PFで畳む**）、**ELFパーサが扱わない件は数え直しで決着した**（`p_vaddr`の範囲は`map_user_4kib`の`NotPrivate`が実質の判定である）
  - **S7の到達条件5（破棄後の古いTLB）。** **APがその空間でタスクを走らせることを要求するので、直線上で走らせる限り構成できない。S9-b-3-2bでも達成できない。未達と書いて次へ送ること**

  **S9-b-3-1（プロセスの実体と`exit`）は完了した。** `hello`がプロセスとして走り、`exit(0)`で終わり、アドレス空間が畳まれ、カーネルが続く。**S8が言い換えた到達条件の文言を、ここで元へ戻した**（S8の項に根拠と観測を書いた）。

  - **到達条件は4つとも達成した。** プロセスとして走ったこと、`exit(0)`で終了したこと、空間を畳んだこと（消えた8枚と隔離へ入れた8枚が一致）、**終了後にカーネルが続いたこと**（この後タイマが動き定常ループへ入る）
  - **Ring 3で走ったことは`CS`ではなく別の量で見ている。** `exit`で戻ると畳みが起きず、フォルト時の`CS`が記録されない。代わりに「カーネルへ入った時点で`IN_RING3`が真だった」を判定行に出す。**観測する量が、出口の作りで変わった例である**
  - **未達1件。「その1本が終わり、別の1本が始まる」は構成できていない。** Ring 3で走るプログラムが`hello`の1本しかない。**中断と終了の違いをこの形で見るのは、走らせる一覧が3本になるS9-b-3-2が最短である。** 未達のまま次へ送る（**S9-b-3-2aで決着した。** 下記）
  - **フレームアロケータの容量（256）は、この段では足りている。** 解禁条件を材料の側へ書き換えた（`deferred-decisions.md`）。**足りない条件は「生成・破棄があること」ではなく「終了の順が生成の順と違いうること」だった。** 空き範囲の数を判定行に出したので、増え方は見える
  - **`ADR-0027`と`ADR-0023`の失効条件は発火していない。** どちらも「Ring 3が定常状態で走るとき」で、**この段は定常ループへ載せていない**（起動シーケンスの直線上で順に走らせる）。**両ADRの論証が依存している「遠征がAP起こしより前の単一コア区間に閉じている」は保たれている**
  - **`--full`は116から120になった。** 反証4本（`user-exit-ignored`・`user-exit-keep-bkl`・`user-exit-keep-space`・`user-exit-wrong-status`）。**当初は3本で宣言していたが、判定行が主張している道が4本あったので4本置いた。宣言の本数は見積もりであって上限ではない**
  - **段の完了時の`cargo xtask flaky`。** 4項目とも**1回目で通った**（S9-b-2の締めと同じ）。**2回連続で1回目である。** 揺らぎが消えた証拠にはならないが、比べる材料が1つ増えた

  **刻みは6コミットだった。想定は7、上限は14である。** 機能2（プロセスの型、`exit`と反証）、記録2、締め1に、承認前に見つけた欠陥の訂正を含む`refactor`が1つである。

  **S9-b-2の締めで立てた予測は、測っている量のほうが違っていた。** 予測は「新しい機構を置く段なので記録は作業と同数程度」で、**コミット数で見ると機能2に対して記録2だから当たっているように見えるが、記録した件数は6である。** 内訳は、docの付け先の壊れ（族の6件目）、検査の向きが片方向であること、破壊の射程が段の主張より広い形、`FAULT_CS`の戻し忘れ、揺れる値と主張を同じ行に載せない、`NoExitNoFold`が構成できないこと。**このうち2件は置き場所がコードのdocなので、コミット数には現れない。**

  **したがって尺を変える。次の段からは「記録の件数」を数える。** T2-cで「単位が作業量を表しているか」を問うたのと同じ形である——**予測が外れたのではなく、測っている量が違った。** 件数で数えれば、S9-b-3-1は作業2に対して記録6で、S9-a（4対10）やS9-b-1（4対6）と同じ側に並ぶ。

  **段閉じのgrepの結果。** `S9-b-3`で全docsを引き、古くなった記述は0件だった。**性質を変えた側からも引いた**——`ud2`（`hello`の出口が変わった）、`entry + 0x20`（受け皿の位置が`0x30`へ移った）、`exit`が無いという記述。**S9-b-1とS9-b-2の締めに残る「プロセスの実体は無く`exit`も無い」は、その段についての記述なので直していない。** 履歴として正しい。**S9-b-2の締めに続いて2回目の0件である**（前回「どちらが効いていたかは次に0件でないときに分かる」と書いたので、まだ分からない）。

  **S9-b-3-2への申し送り。**

  - **`syscall-test`と`fault-test`の2本。** 新しいのは「ユーザーが書いたコードから通る」ことだけだが、それは具体的には**カーネル側のprobeが引数をRustで組み立てるのに対し、ユーザープログラムはasmでABIに従って組み立てる**という違いである。**確かめているのは`dispatch`ではなく`ADR-0020`が決めたABIの契約そのものである。判定行にそう書くこと**
  - **未達1件（別の1本が始まる）は、一覧が3本になった時点で観測できる。** 締めで突き合わせること
  - **`load_user_program_into`がdocを持っていない。** S9-b-2で関数を割ったときの置き去りで、「埋め込んだユーザープログラムを新しいアドレス空間へ写像する」以下のブロックが`load_user_program`に付いたままである。**内容も古い**（「まだ走らせない」はS9-b-1の記述）。**付け先の訂正と書き直しは別コミットにする**（`verification-coverage.md`の付け先の壊れの記録）
  - **ポインタ検証の窓が2つある。** 1つに畳むには「今のプロセスの空間」から添字を取る必要がある。**下限を4 MiBに置いた理由（起動時の検証が恒等除去より前に走る）も一緒に扱うこと**
  - **`UserLoadError`の`Display`**（宛先=ログへ出す箇所が2つ以上になったとき）
  - **S9-aから持ち越した3件。** `USER_PML4_INDEX`の値、`W^X`の`X`（**`fault-test`の作り方によっては発火する**）、ELFパーサが扱わない件。**最後の1つは数え直すこと**——`common/src/elf.rs`は5つ挙げているが、うち2つ（`PT_LOAD`が0個、entryが`PT_LOAD`の中）は既定ビルドの`verify_embedded_user_elf`が見ている。**「パーサが扱わない」と「誰も見ていない」は別である。** その形で整理する
  - **S7の到達条件5（破棄後の古いTLB）。** **APがその空間でタスクを走らせることを要求するので、直線上で走らせる限り構成できない。S9-b-3-2でも達成できない見込みで、未達なら未達と書いて次へ送る**

  **S9-b-2への申し送り。**

  - **ポインタ検証の窓が2つある。** `[512 GiB, 1 TiB)`（起動時の検証）と`[4 MiB, 512 GiB)`（ユーザープログラム）で、またぐ範囲は受理しない。**本来は「稼働中の空間のユーザーサブツリー」を1つ持つのが筋だが、それにはカーネルが現在のアドレス空間を追跡する必要がある**（プロセスの実体を作るS9-b-3の仕事）。**1つに畳むとき、下限を4 MiBに置いた理由も一緒に扱うこと**——理由は「ポインタ検証のbatteryが恒等除去より前に走り、その時点の低位VAにはカーネルの恒等写像が居る」ことで、**起動順の問題である**（`DirectMap::identity`の5箇所と同じ根）
  - **区画が同じ4KiBページを共有する像は張れない。** `userland/user.ld`がページ境界へ揃えているので自作の像では起きないが、**揃えないリンカスクリプトで作った像は入力としてありうる。** 壊したELFの扱いで、この形も対象に入れること
  - **`USER_PML4_INDEX`の値**（条件は「起動時の検証3本を恒等除去より後ろへ動かすとき」）、**`W^X`の`X`の側**、**ELFパーサが扱わない5件**は、S9-aからの申し送りのまま残っている

  **入口の機構は`int 0x80`のままとする**（ADR-0020のAddendum 2）。`syscall`/`sysret`と`swapgs`・GSベースはS9の範囲外である。**ただしABIの中身はここで揃える。** S9はシステムコール番号を新しく割り当てるので、「番号・errnoの値・構造体レイアウトをLinuxから採る」の宛先がここで発火する。**現在の`PROBE_NUMBER`（0x2A）・`SYS_CHECK_PTR`（0x2B）・`SYS_CHECKSUM`（0x2C）はLinuxの42・43・44と衝突しており、移すか廃止するのもここである。**

  **S9-a（振る舞い不変の準備）は完了した。** S3-a・S4-aと同じ「振る舞いを変えずに土台を置く」副段である。**S9の到達条件は満たしていない。それはS9-bが満たす。** S9-aの到達条件はS9のものとは別に立てた。

  - **到達条件1: 振る舞いが変わらないこと。達成。** 4つの刻みのすべてで、判定行の判定結果が1つも変わっていない。起動ログの参照は4回録り直したが、**足した行（判定行3本）以外はすべてコードアドレスの移動である。** 既存の判定フィールドの変化は0だった
  - **到達条件2: 個々の土台が単独で観測できること。達成。** `map_4kib`の書き込み可否は`ring3-vectors`の6本目（エラーコード0x7）で、`common::elf`の全域性はホストテスト8本で、システムコール番号と`errno`は`checksum`の`over-long`で観測している。**`ring3::enter`の引数化だけは新しい観測を持たない**——飛び先を変えて落ちるところは、飛び先が2つ以上になるS9-bで初めて作れる。**既存の観測（Ring 3の破壊5本）が守っている**
  - **到達しないと宣言したもの: そのとおり到達していない。** ELFを実際にロードしていない。Ring 3でユーザープログラムを走らせていない。`USER_PML4_INDEX`の値は動かしていない

  **刻みは15コミットだった。想定は9、上限は18である。** 内訳は機能4（項目3・1・4・2）、記録9、訂正2。**上限は超えていないが、宣言との差は配分にある**——機能は想定どおりで、**記録が9出た（想定は「軸が2つ増える」の2）。**

  **S6・S7と並べると、記録の割合が段ごとに上がっている。** S6は12コミットで記録0（軸の分割2）、S7は14で記録2、S8は11で記録2、**S9-aは15で記録9である。** S6からS8までは「作業の途中で既存の誤りが見つかる」型が中心だったが、S9-aで出たのは**規律と類型が4件**（破壊の受け皿、回帰チェック中のツリー、性質を変えたときの追随、段名の解禁条件）と**事例が3件**である。**この体制では記録のコミットが一定の割合で出る、という見込みは3段ぶんの実績で立つ。** 次の段からは、宣言の時点で記録を「軸が2つ」ではなく**作業コミットと同数程度**として見込むこと。

  **訂正が2件出たことも書いておく。** 自分が直前のコミットで作った古い記述（`ea3acaa`）と、行を最後まで読まずに下した判定（`125a2cf`）である。**どちらも自分の段の中で見つけて直した。**

  **ELFローダーの設計は`vision.md`の「ELFローダーをユーザープログラムへ広げるときに一体で設計すべきこと」を読んでから行う。** `PT_TLS`・`PT_GNU_STACK`・初期スタックレイアウトのチェックリストがそこにある。**S9の対応範囲は`PT_LOAD`のみだが、範囲外と決めることと、存在を知らないことは違う。** チェックリストの側はS9を名指ししていなかったので、参照はこちらから張る（片側だけの記録は受け取る側が知らないと発火しない）。
- [x] **S10. RAM上のext2読み取りと最小VFS**（ADR-0025）: `BlockDevice`から`RamBlockDevice`を介してext2イメージを読む。VFSはext2を最初の実装として、実際に必要になった操作から最小の共通境界を抽出する。ファイル関連syscall（open・read・close・stat・readdir相当）を足す。薄いVFSトレイト境界を先に引くかは未決（`deferred-decisions.md`）。着手前に決めること=ヒープの方針（現在1MiB固定で拡張機構が無い。ADR-0012）とイメージの持ち方（カーネルへ埋め込むか、bootloaderが別モジュールとして読むか）。**ヒープの拡張方式を選ぶときは、`mmap`（ユーザー空間のメモリ管理）を載せる土台になるかを判断材料の1つに入れる。** 動機は`vision.md`にある（構想であって決定ではない。**先回りの抽象化を入れる根拠にはならず、複数案が同等のときに選ぶ理由として使う**）。到達条件=`mke2fs`で作ったイメージを読めること、壊したイメージでカーネルメモリを壊さずエラーを返すこと、パーサのホストテスト。実装しない=書き込み、double/triple indirect、symlink、拡張属性 **宣言のうち`BlockDevice`と`RamBlockDevice`は、実装では置かなかった**（行は消さず状態を書き換える）。**実際は`common::ext2::Ext2`が像のバイト列を直に読む。** 借りて返す形（`Ext2::block_bytes`）が成立するのは**像が`'static`でRAM上にあるから**で、**traitを引く境界は実デバイスの段（S13）である。** 経緯は下の締めにある

  **S9の締めの申し送りが拾えていなかったものが2件ある**（S10の棚卸しで`S10`のgrepをして見つけた。**片側だけの記録になっていた**）。

  - **管理構造はヒープではなく固定配列で足りないかを先に問う**（S7の項に理由つきで書いてある）。**このkernelの様式に合う**（`MAX_CPUS`・`WORKER_COUNT`・フレームアロケータの範囲上限、いずれも固定）ことと、**S6-cで59標本の実測で裏づけた「定常経路に解放されない確保は無い」が守れる**ことの2つが理由である。**S10で開くファイルの表やパス解決のバッファは、まず固定上限で書けるかを問う。** 足りればADR-0012にもヒープにも触れずに閉じる
  - **イメージの持ち方はS12の条件も持っている**（`deferred-decisions.md`）。**`.rodata`に置いた像は書けないので、S12（書き込み）ではフレームへ複製が要る。** また**`e2fsck`で独立検証するには、RAM上の像をホストへ取り出す手段が要る**（QEMUのmonitorの`pmemsave`。xtaskは既にmonitorを駆動している）**この「書けない」は保護ではなく型の話だった（S12の棚卸しで実測）。**像の写像は読み書き可で（起動時の静的テーブルは`0x83`、本流の`map_range`は`writable`を引数に持たず既定で`PTE_WRITABLE`が立つ。W^Xは未実装）、**書けないのは`FS_IMAGE`が`&'static [u8]`だからである。** **複製が要るという結論は変わらないが、`static mut`にしてその場で書く案が「不可能」から「採らない」へ変わる。**

  **S10の着手時に決めた4件**（棚卸しの結果。運用者承認済み）。**カーネルヒープは触らない**（固定配列で足りるかを先に問う）。**VFSは`inode`/`file`の概念だけ置き、traitは引かない**（`dentry`は置かない。抽出の条件は「2つ目のファイルシステムを足すとき、または同じ操作を2箇所で書いたとき」）。**ext2のパラメータは`mke2fs`の既定**（block 4096・inode size 256・rev 1）で、**未対応featureはINCOMPATだけ拒否する**（RO_COMPATとCOMPATの未知ビットは読み取りでは受理する。**この非対称はLinuxと同じ規則である**）。**像は`include_bytes!`で埋め込む**（移す条件は「像を書き換える必要が生じたとき」または「像の大きさが起動時のコピーで測れるほど効いたとき」）。**段はS10-a（読む）とS10-b（VFSとsyscall）に分ける。**

  **S10-aを完了にする（読む側）。到達条件は3つとも達成した。**

  - **`mke2fs`の像を読める。達成。** `build.rs`が`mke2fs -d`で建てた像を`include_bytes!`で抱え、既定ビルドで毎起動、superblock・group descriptor・ルートinode・ルートの走査・`/etc/motd`の中身・単一間接の境界の両側を判定行に出す。**外の道具が作った像を読むことが主張の中身である**——自作の書き手が作った像を読めても、自分の理解どうしの一致しか言わない
  - **壊した像でカーネルメモリを壊さずエラーを返す。達成。** 21通りを既定ビルドで毎起動渡し、21通りとも期待した理由で拒まれてカーネルが続く。**4つの線すべてに当たっている**（線1=切り詰め、線2=ブロックサイズの桁あふれとinodeテーブルの位置、線3=group descriptor・`i_block`・dirent・間接ブロックの4種類の参照、線4=`rec_len`が0）
  - **パーサのホストテスト。達成。** `common::ext2`に47本（`cargo test -p common`は93本。実測）。**`unsafe`を1つも使っていない**ので、ホスト上で全経路を踏める

  **実装しないと宣言したものは、そのとおり実装していない。** 書き込み、double/triple indirect、symlink、拡張属性。**二重・三重間接は「拒む」形で明示してある**（`i_block[13]`/`[14]`を使うファイルはどのブロックも読まない）。

  **刻みは15コミットだった**（S9の締め`8ef4d68`からこの締めまで。`git rev-list --count`で実測）。**宣言は8・上限16である。** 内訳はfeat 7・test 1・docs 7で、**超過分はすべてdocs側から出ている。** **`coding-standards.md`の「軸が2つ増えるものとして数える」がまた出た形だが、今回の出所は「作業中に既存の記録の誤りが見つかる」ではなく「作業中に新しい規律が見つかる」である。**

  **記録は件数で9だった。** 規約へ足したものが5（決定性の潰す場所を差分から引く / 実測値は測った条件が変わると古くなる / 名前は実装の範囲が広がった瞬間に誤りになる / 停止性の試験は信号の形が他と違う / 上限が要る理由と止まる理由を分けて書く）、`deferred-decisions.md`へ足したものが2（ガードページのsplit化に2度目を追記 / ext2の起動時検証を`Result`へ切り出す）、会計行が1、**穴の扱いの宛先が1**（`common::ext2`のmodule doc）。

  **段の完了時の`cargo xtask check --full`。121項目すべてが1回目で通った**（再試行0件）。**`cargo xtask flaky`は4項目とも1回目。6回連続で1回目である**（S9-b-2から数えて）。**5回のときに「比べる材料が5つ揃った」と書いたのが、6つになった。**

  **段閉じのgrep（`S10-a`と`S10`で全docs）。** **古くなった記述が4件見つかり、4件とも直した。いずれも同じ型である**——`deferred-decisions.md`の4行が、条件（「S10に着手する時点」）が発火して決着も済んでいるのに、行が「未決」のまま閉じていなかった。**「条件が発火し、作業も済んだが、行が閉じていない」の再発で、S9-aの棚卸しで6件見つけたのと同じ型である。**

  - **薄いVFSトレイト境界を先に引くか。** 着手時に「引かない」と決めており、抽出の条件も移してあったのに、行は「未決」のままだった
  - **ext2のパラメータ。** 着手時に`mke2fs`の既定と決めてあった
  - **ext2イメージの持ち方と取り出し。** **持ち方だけが決着している**——取り出しはS12が条件なので、行を分けて書いた
  - **カーネルヒープの拡張。** 着手時の判断は「触らない」で、**決着ではなく判定の先送りである。** 条件を「S10-bで固定配列では足りないと分かったとき」へ書き換えた

  **決着の記録がroadmapの側にだけあり、`deferred-decisions.md`の行に戻っていなかった。** 着手時の棚卸しは`roadmap.md`のS10の項へ書いており、**書いた側からは行が見えていない**（`coding-standards.md`の「段を閉じるときは、その段の名前で全docsをgrepする」が言う形そのものである）。**書いたときに閉じるのではなく、段を閉じるときのgrepが捕まえた。**

  **`S10-a`のほうは古い記述が無かった。** `coding-standards.md`の5件（すべてこの段で書いた実例）と`verification-coverage.md`の会計行に現れ、いずれも現在形として正しい。

  **S10-bへの申し送り。**

  - **ヒープが本当に要るかを設計で確かめる。** S10-aは**ヒープを1バイトも使っていない**（像は`.rodata`、パーサは借用だけ、走査はイテレータ）。**判定できるのはS10-bである**——開いたファイルの表とパス解決のバッファが固定上限で書けるなら、`ADR-0012`にもヒープにも触れずに閉じる。**先に問うこと**（S7の項に理由がある）
  - **`MAX_PATH_COMPONENTS`と`PATH_MAX`の関係。** Linuxが要素数の上限を持たないのは`PATH_MAX`（4096バイト）が実質的な上限になるからである。**S10-bでパスの長さに上限を入れるなら、要素数の上限は要らなくなりうる。どちらか一方でよい**
  - **起動時検証の`Result`への切り出し。** 宛先は`deferred-decisions.md`に置いた。**条件は「`halt`する経路が複数の呼び出し側から来るようになったとき」**で、VFSとsyscallが入ると発火しうる
  - **ガードページのsplit化。** **S10-aで2度目を踏んだ（踏んだが回避した）。** `.bss`の大きさでガードページの位置が動く。**S10-bでカーネルが育つので、次に踏む可能性が高い**
  - **穴（sparse file）の扱い。** 宛先は`common::ext2`のmodule docにある。**条件は「穴を持つ像を読む必要が生じたとき」**で、S12で書き込みを実装すると自分で穴を作れるようになる
  - **`BlockDevice`のtraitを引く境界。** S10-aは`Ext2::block_bytes`が**借りて返す**形で、成立の条件は「像が`'static`でRAM上にあるから」である。**実デバイスは要求してから届くのでS13では`read_block`の形になる。そこがtraitを引く境界である**
  - **`UserSlice`はそのまま使える。** ファイル関連syscallがユーザーのバッファを読み書きするので、S9で1つに畳んだ`validate_user_range`の窓がそのまま効く


  **S10-bを完了にする（使う側）。到達条件は2段とも達成した。**

  **棚卸しで「分けないが到達条件を2段に書く」と決めた**（分けるかどうかの判断を、材料が揃う場所へ送るため）。

  - **`open`/`read`/`close`が通る。達成。** Ring 3の`syscall-test`が`/etc/motd`を名前で開き、18バイトを読み、中身が種のファイルと一致し、閉じる。**短い読みと続きの読みで位置が進むことも見ている**
  - **`stat`と`getdents64`が通る。達成。** `stat`はLinuxと同じ144バイトの形で答え、`getdents64`はLinuxと同じレコードでルートの6エントリを返す

  **S10全体の到達条件はS10-aで閉じている**（`mke2fs`の像を読める / 壊した像でエラーを返す / パーサのホストテスト）。**S10-bはその上で「使えるようになった」側である**——読めることは既に示してあり、ここで示したのは**Ring 3からファイルとして扱えること**である。

  **実装しないと宣言したものは、そのとおり実装していない。** 書き込み（`open`は`-EROFS`で拒む）、double/triple indirect、symlink（`d_type`の対応表に載せていない）、拡張属性。**`fstat`と`openat`も置いていない**（要ると分かってから）。

  **刻みは8コミットだった**（`294203a`からこの締めまで。`git rev-list --count`で実測）。**宣言は10・上限20である。**

  **宣言を下回った。理由は見積もりの単位である。** 宣言では「VFSの型とfd表に2、`open`/`close`に2、`read`に1、`stat`に2、`getdents64`に2、締めに1」と見込んでいたが、**実績は各1だった。** 2を見込んだのは「型を置く」と「使う」を分ける想定だったが、**使わない型を先に置くと、その刻みで振る舞いが変わらず、検算も置けない。** 実際には1刻みで型と使い方を揃えるほうが、判定行が毎回増えて観測が途切れなかった。**「2を見込んで1で済んだ」のではなく、「2に分ける形が良くなかった」である。**

  **記録は件数で4だった。** 規約へ2（構成で分けるときも既定側がどちらかを決める / 段閉じのgrepに`.rs`を含める）、`deferred-decisions.md`へ1（受け皿の位置が2度広がった。**この段の中で3度目が来て、締めで解いた**）、`common::ext2`へ1（`d_off`は「ext2だからバイト位置」であって「`d_off`がバイト位置だから」ではない）。

  **段の完了時の`cargo xtask check --full`。125項目すべてが1回目で通った**（再試行0件）。**`cargo xtask flaky`は4項目とも1回目。7回連続である。**

  **段閉じのgrep（`S10-b`と`S10`で、追跡下の`.md`と`.rs`）。** **`.rs`へ広げたのはこの締めが初めてで、棚卸しのD9で「締めの直前に入れる」と決めていた。**

  **`.rs`で68件が出て、古くなっていたのは1件だった。** `main.rs`の`FS_IMAGE`のdocが、**定義が`lib`へ移った後も`use`文に貼り付いたまま残っていた**——docの本体は移したのに、**「抱えるのをやめる条件」の記録だけが元の場所に残っていた。** `use`にdocを書けるので文法上は正しく、**コンパイラも`clippy`も何も言わない。** 記録を`kernel::vfs::FS_IMAGE`へ移して直した。

  **残る67件はすべて札か指し先である**（`（S10-b）`のような注記か、roadmapへの参照）。**広げる価値はその1件の性質にあり、件数ではない**——`.md`側では出ない型が1つ見つかった。手順は`coding-standards.md`に書いた。

  **`.md`側では古くなった記述は無かった。**

  **S10はこれで閉じる。** 副段は2つで、**S10-a（読む）とS10-b（使う）の両方が到達条件を満たした。**

  **S11への申し送り。**

  - **fdの表はプロセスごとに持ってある。** S11で複数のプロセスが同時に生きるようになっても、`UserProcess`に付いているので作り直しは要らない。**遠征の間だけ`kernel::vfs`へ据える形**（`syscall::set_user_window`と同じ）が、そのまま複数プロセスでも効く
  - **`MAX_OPEN_FILES`は16である。** シェルがリダイレクトを持つなら3本、`ls`と`cat`はそれぞれ1本。**足りなくなったときの宛先は`deferred-decisions.md`の「カーネルヒープの拡張」にある**
  - **0/1/2を予約していない。** シェルがstdin/stdout/stderrを要求するなら、**そこが最初の判断になる**——予約するか、シェルが自分で開くか。**Linuxは後者である**（`init`が開く）。**S11-10で消化した。前者（予約する）を採った**——0/1/2は`FileTable`の中に`File::Terminal`として据えてあり、`open`は3から返す。**`init`が開く形を採らなかったのは、開く相手が無いからである**（デバイスノードの仕組みが無く、`/dev/console`は像に無い）
  - **`d_off`はバイト位置である。** `dir_index`を実装したら足りなくなる（索引付きのディレクトリはハッシュ順に返す）。**条件は`common::ext2`の`DirEntry::next_offset`に書いてある**
  - **受け皿の位置は`user.ld`が唯一の出所になった。** `syscall-test`に検算を足しても、直す場所は1行である。**`hello`と`fault-test`は`.org`のままである**（伸びていない）
  - **`write`はまだfdを見ていない。** S9のまま「バイト列を静的領域へ記録して長さを返す」である。**シェルが出力を持つ段で、ここが最初に当たる**。**S11-10で消化した。** `write`は番号ではなく表の中身で分岐する（端末でなければ`-EROFS`）。**`kernel/src/vfs.rs`の`File::Terminal`のdocが「ファイルへ書けるようになった段（S12）でも、同じ形のまま通る」と予告しており、S12の棚卸しで実際にそうなっていた**

- [x] **S11. initと簡易シェル**: 到達条件=シェルから`ls`と`cat /etc/motd`と`/bin/hello`を実行できること。実装しない=POSIX互換、Linux syscall ABI互換、ジョブ制御。**ユーザーランドはRustで書く**（`ADR-0028`）。**画面制御を入れるならANSIエスケープシーケンスで、専用のシステムコールは作らない**（`ADR-0029`）。**入力をRing 3へ配る仕組みを設計するときは、生モードと行モードの切り替えを一緒に見る**（理由は`vision.md`の`zi`にある。**判断材料であって、S11で実装する根拠ではない**）。**最初の実装の形を選ぶとき、複数案が同等なら「コマンドの実行を組み込みと外部で分ける」「入力の解釈を実行から分ける」側を採る**（動機は`vision.md`の「シェルをbashのように育てる」。構想であって決定ではない。**先回りの抽象化を入れる根拠にはならず、層を増やす理由にも使わない**）

  **到達条件を2段で立て、両方を満たした。**

  - **Ring 3からプロセスを作って終了を待てる。達成。** 既定の起動ログが`spawn: /bin/spawn-test ended (Exited(0)) after 2 syscall(s); its kernel entries ran on RSP ... (inside its own excursion stack ... = true); the space was destroyed (9 frame(s) left the allocator and 9 reached quarantine (9 its own + 0 from what it spawned), match=true leaked=0)`を出す。**同期であること・子のカーネル入場が子自身の遠征スタックの上で起きたこと・フレームの会計が閉じることを、同じ行が言う**
  - **シェルから`ls`と`cat /etc/motd`と`/bin/hello`を実行できる。達成。** `cargo xtask run --shell-test`の7判定がすべて`true`である（プロンプトが出る / 打鍵が反響する / `ls`がルートを一覧する / `cat`が`/etc/motd`を出す / `hello`が走る / `exit`で終わる / `init`が起こし直す）。**打鍵はQEMUのmonitorから送る**

  **構造。** `init`はカーネル側に置いた——**Ring 3へ置くと深さが足りない**（シェルが深さ1、その子が深さ2で、`MAX_EXCURSION_DEPTH`は2である）。**`read(0)`は溜まっていなければ`-EAGAIN`を返し、シェルは回して待つ**（カーネル側で眠らせる形はユーザープロセスのスケジューラを要求し、`read`の中で眠るとBKLを保持したまま眠ることになる）。**費用は起動ログに出ている**——1セッションで`328188`回のシステムコールである。**fdの0/1/2は端末で、`open`は3から返す。**

  **実装しないと宣言したものは、そのとおり実装していない。** POSIX互換、Linux syscall ABI互換、ジョブ制御。**`PATH`も無い**——シェルはパスを解決せず、`/bin/ls`と打つ必要がある（`envp`の入口を開けていないため。`kernel/userland/zash.rs`）。

  **刻みは11だった。宣言を3度直した**（8→9→10→11）。**理由の型は3度とも「機構の前提が実コードと違った」で、`b43b443`が診断している**——4件とも呼び出し側から見えるシグネチャと機構の性質を確かめれば分かるもので、**S11の棚卸しは自コードを6件grepしただけだった。** 手順として「棚卸しで使うと書いた関数と型は、その場でシグネチャを読む」を規約へ足した。

  **診断が後半で効いたかを評価した。** `b43b443`は`S11-3`の後・`S11-4`の前にあり、あれが数える4件はすべて`S11-1`から`S11-3`の区間で起きている。**5本目以降で「着手前に止まった」と記録に残っているのは1件である**——`S11-6`で、`S11-5`が立てた解禁条件を実装前に確かめ、**前半に発火する経路がどこにも無いと分かった。** そして**同じ型（機構の前提が違って刻みが増える）の再発は記録されていない。** 後半に出た6件はいずれも型が違い、実装中か、走らせて落ちて分かったものである。

  **観測の限界を書く。** **「着手前に止まった回数」として数えられるのは、止まったことが記録として残ったものだけである。** 作業中のやりとりはリポジトリに無いので、記録に残らなかった停止は数えられない。**したがって「効いた」とは言えず、「同じ型の再発が無かった」までが言えることである。** `S11-6`は機構のシグネチャを読む手順ではなく、書いた条件が発火するかを確かめる手順で止まっている。

  **記録は件数で、ADR新規3・ADR Addendum 3・規約5・`verification-coverage.md` 11・`troubleshooting.md` 1・`deferred-decisions.md`新規4と解禁2・`vision.md` 2だった。** 検査項目は125から134へ9増えた（1本目に1、5本目に3、7本目に2、8本目に1、9本目に1、11本目に1）。

  **段の完了時の`cargo xtask check --full`。134項目すべてが1回目で通った**（再試行0件）。**`cargo xtask flaky`は4項目とも1回目である。**

  **段閉じのgrep（`S11`で、追跡下の`.md`と`.rs`）。** `.md`が10ファイル67件、`.rs`が17ファイル187件。**「定義から離れたdoc」と「条件・宛先」だけを拾う**手順（D9）で3件が出た。

  - **処理済みにしたもの1件。** S9-b-3への申し送りの「`ADR-0027`と`ADR-0023`の失効条件をS11のシェルへ持ち越す」。**S11-11で発火し、両ADRに評価のAddendumを足した。**
  - **未達と分かったもの1件。S7の到達条件5である。** S8の締めとS9の締めが「構成できるのは定常実行の段（S11）である」と書いていたが、**S11でも構成できなかった**——定常実行になったのはBSPの側で、**要求されているのはAPがその空間を走らせることである。2つは別である。** **条件の本体は`kernel/src/ring3.rs`の`enter`へ移し、roadmap側には宛先だけを残した**（条件の本体を2箇所に持たない）。**段名で書いた条件がどの段のgrepでも発火しないまま渡る形は、`coding-standards.md`が7件記録している。これで4回目の持ち越しだった。**
  - **古くなっていたdoc 1件。** `MAX_EXCURSION_DEPTH`の理由が、**「シェルが子を起こすときは`init`が待っている親ではなくなる形も考えられるが、S11の到達条件はそこまで要求しない」**と書いていた。**退けたその形が、S11が実際に作った形である。****「検査が前提を持ったまま古くなる」のdoc版である**（別コミットで直した）。

  **S11はこれで閉じる。**

  **S12への申し送り。**

  - **S7の到達条件5は未達のまま。** 条件は`kernel/src/ring3.rs`の`enter`にある。**S12もS13も、APをRing 3へ入れる見込みは無い。**
  - **ext2イメージの取り出しがここで要る**（`deferred-decisions.md`の「ext2イメージの持ち方と取り出し」。持ち方は決着済みで、取り出しの条件がS12である）。**xtaskは既にQEMUのmonitorを駆動しているので`pmemsave`が素直**
  - **シェルの待ちは回して待つ形である。** 解禁条件は`kernel/userland/zash.rs`にあり、`deferred-decisions.md`には宛先だけを置いた。**スケジューラを入れる段で、待たせる形へ変える**
  - **`write`はfdを見るようになったが、出力先はシリアルだけである。** 画面へ出す形は`S11-8`で退けてある（`verification-coverage.md`の「観測のために置いた仕組みが、機能の制限になっていた」）。**多重化の条件は`deferred-decisions.md`の「コンソール / シリアルへの出力の多重化」にある**
  - **`MAX_OPEN_FILES`は16のままで、開いたfdの漏れは判定行が毎起動見ている**（`left Ring 3 with N still open`）
- [x] **S12. ext2書き込み**: bitmap・割り当て・作成・書き込み・mkdir・unlink・rmdir・truncateを段階的に足し、各段でメタデータの会計を閉じる。Linux側（`e2fsck`とマウント）を独立検証器に使うため、RAM上のイメージをホストへ取り出す手段が要る（xtaskは既にQEMUのmonitorを駆動しているので`pmemsave`が素直）。到達条件=ZaytOSが書いたイメージを`e2fsck`が無傷と判定し、Linuxでマウントして内容とディレクトリ構造が読めること。実装しない=ジャーナリング、電源断耐性

  **S12完了（S12-aからS12-f）。到達条件は両方とも満たした。****`e2fsck`は3構成とも不満0本で、`ext2`ドライバが3構成ともマウントして読めた**（`verification-coverage.md`の「S12の到達条件（締め）」）。**mkdirとrmdirは実装していない**——**S12の記述に挙がっているが、ファイルの作成と削除で到達条件は満たされる。** **ディレクトリを作るには`.`と`..`の始末と親の`i_links_count`が要り、`bg_used_dirs_count`も動く**（実測で「ファイルでは動かない」ことを確かめてある）。**要るようになった段で足す。**

  **判定の型と残っている限界は`verification-coverage.md`の「S12の締め」にある。****手で回すマウントには回帰が無く、回し直す契機は`deferred-decisions.md`にある。**
- [ ] **T3. S12の後の整理**（**候補として名前を置くだけである。何を扱うかはここで決めない**）: T1（S4とS5の間）・T2（S7とS8の間）と同じく、**振る舞いを変えない独立した段**である。**置く理由は、S12で「条件は満たされているのに、発火する日が来ない」持ち越しが出たからである**——`deferred-decisions.md`の「ext2の起動時検証を`Result`へ切り出す」がそれで、**元の条件（呼び出し側が2つ以上）を実測で大きく超えているのに、誰も着手する契機を持っていなかった。** **書き直しただけでは同じことが繰り返される**ので、**行き先の段そのものを先に用意する。** **候補はこの1件から始まる。増えたらそのつど名指しすること。****T1・T2と同じく、着手の可否と範囲は着手時に判断する**（T2は打ち切りで終えており、**完了しないことも結果である**）

  **条件を先に決めた（S12の締めの後。承認済み）。** **T2が打ち切りになったのは終わり方が決まっていなかったからで、候補を集めてから終わり方を決めると際限が無くなる。順序を逆にしてある。**

  **順序**——**掃く → 行を立てる → 一覧を閉じる → 着手する。**
  **掃きが着手より前に済んでいれば、そこで見つかったものも入る条件を正当に満たす。**
  **次に整理の段を置く人も、この順序を使うこと。**

  **入る条件（3つすべて）**——**(1) 振る舞いを変えない**（起動ログの参照が動かないか、下の「確かめ方」で動いてよいとした2つに収まる）。**(2) 同じ形が複数箇所に散っており、その数を実測して行に書けること**（数えられないものは終わりも決められない。T1とT2がそうだった）。**(3) 着手より前に`deferred-decisions.md`へ行があり、解禁条件がこの段を名指ししていること。**

  **入らない条件**——**振る舞いが変わるもの**（判定行の文言が変わるものを含む）。**1箇所しか無いもの**（散らばりでないので整理ではなく設計変更である）。**新しい機構を足すもの**（先回りの抽象化。`vision.md`）。**散文**（コメントとdocs。T1とT2の領域で、測っても終わりが決まらないことが2度実証されている）。**着手後に見つかったもの**（行を立てて次の整理の段へ回す。**ここがT2への直接の手当てである**）。

  **終わる条件**——**着手時に閉じた一覧の全件を片付けたら閉じる。** **件数で切る形は要らない**——一覧が閉じているので「全件」がそのまま有限の量である。**閉じた時点の件数をこの行へ実測で書き、閉じるときに処理した件数と突き合わせる。**

  **打ち切る条件**——**1件について、振る舞いを変えずに切り出せないと分かったら、その1件を落として理由を行へ書き、次へ進む**（段全体は打ち切らない）。**段全体を打ち切るのは、残っている件の全部がそうなったときである。** **途中で振る舞いが変わると分かったときは、打ち切りではなく停止して報告する。**

  **確かめ方**——**振る舞いを変えていないことは3つで見る。** **`--full`と`flaky`が緑であること**、**起動ログの参照が動かないか、動いてよい2つに収まっていること**、**判定行の文言が1つも変わらないこと。** **3つ目がいちばん効く**——**前の2つは通っても、文言が動いていれば主張が変わっている。**

  **参照が動いてよいのは、番地と、像の大きさから機械的に導かれる数の2つだけである。** **後者は差が導出できることまで確かめる**——**ページ数が+1ならPTEも+1、のように突き合わせる。「たぶん伸びたから」で済ませない。** **どちらでもない行が1つでも残ったら、止めて報告する。** **着手時は「番地の移動だけ」と書いていたが、(1)(b)で像が1ページ伸びてU/S監査の`kernel entries`が動いたので、書き直した**（923から924ページへ増え、`kernel entries`は3772から3773へ、2378から2379へ、いずれも+1。**文言そのものは変わっていない**）。

  **一覧を閉じた。3件である**（掃きの後に実測。`deferred-decisions.md`で解禁条件がこの段を名指ししている行の数）。**閉じた後に見つかったものは行を立てて次の整理の段へ回す。この段へは入れない。**

  **実施の順序は次のとおりで、(2)と(3)の順が要点である。**

  1. **ext2の起動時検証を`Result`へ切り出す**（`halt_forever`が**12の関数に49箇所**。内訳は`copy_fs_image_to_frames` 5／`exercise_block_bitmap` 4／`exercise_file_append` 3／`exercise_truncate` 4／`exercise_create_and_unlink` 6／`verify_embedded_fs_image` 3／`verify_path_lookup` 4／`verify_root_inode` 3／`verify_root_directory_walk` 3／`verify_single_indirect_boundary` 6／`verify_corrupt_fs_image_is_rejected` 5／`verify_fs_content_mismatch_is_noticed` 3。**一度25と書いたのは誤りである**——**関数の一覧を手で並べて数えたので、この行が名指ししている`verify_root_inode`など3つを取りこぼしていた。****手で並べた一覧を数の出所にしないこと**）
  2. **ext2の書く側の、同じ算術を切り出す**（バイト位置8箇所・ビットマップ4箇所ずつ・`i_block`のスロット5箇所）
  3. **欄の位置に名前を付ける**（**群descriptor 14箇所**——内訳は`+12`が4・`+14`が6・`+16`が4、**superblock 10箇所**——`+12`が4・`+16`が6）。**着手時に10と8と書いたのは行数だった**——**1行に2つ出るものがある**（`image[descriptor + 12..descriptor + 14]`）。**数えるのは出現数である。****2通りの方法で数えて一致を見た。** **2通りは前提を共有していたら2通りではない**——S13-cで、`logger`と`.error(`を`fmt`が行で割った箇所を、隣接を前提にした2つの数え方が揃って落とした（同じ2件を両方が見落とし、一致した）。**片方は割れ方に依らない形（正規表現の`\s*`や、別の切り口）にすること**

  **(2)を先にやる理由**——**算術を切り出すと、`+12`のような欄の位置がその中へ吸われる。****先に(3)をやると、(2)で消える箇所にも名前を付けることになる。**
  **したがって(3)の件数は(2)の後に測り直す。** **上の数は着手時に測ったもので、一覧は着手時に閉じるものだから書き換えない**——**(3)に着手するときに残っている箇所を実測し直し、着手時の数との差を報告する。差が出るのが正常である。**

  **4群が終わったら、行を1つ立てること**——**切り出しで arm が通せるようになる。**
  **破壊を足すのはこの段の外なので**（「新しい機構を足すもの」に当たる）、**別の段で拾えるように`deferred-decisions.md`へ置く。**
  **群ごとに立てると4行になり、中身が同じになる。1行にまとめる。**
  **ここに書いておくのは、(d)が終わった時点で誰も覚えていないからである。**

  **(1)を閉じた。** **49箇所を由来で4群に分け、小さい順に処理した**——像の複製5（`cd6f94e`）・壊した像の拒否8（`79e9b91`）・書き込みの実演17（`598abcc`）・読み取りの検証19（`a8e5391`）。**約束どおり、armの行は`deferred-decisions.md`へ1行だけ立てた。**

  **突き合わせは合っている。** **着手時に閉じた一覧は49箇所・12関数で、処理したのも49箇所・12関数である。** **いま残る停止は4箇所で、群ごとの包み1つずつである**（`copy_fs_image_to_frames`・`verify_corrupt_fs_image_is_rejected`・`exercise_block_bitmap`・`verify_embedded_fs_image`）。**2通りで数えて一致を見た**——**12関数を個別に集計すると49から4で差が45**、**`kernel/src/main.rs`全体の`cpu::halt_forever()`は194から149で差が45**（`57dcf1e`と`a8e5391`の実測）。**49を取り除いて包みを4つ足した差である。**

  **形は4群を通して同じにした**——`f()`を薄い包みと`try_f()`に分け、**止める理由は列挙で運び、文言は包み側の`match`で作る**（**`format_args!`は一時値を借りるので関数の外へ返せない**。実測）。**判定行（`logger.info`）は検査部に残した**——**検査と検査の間に、検査した値を使って出ているためで、外へ出すと順序か文言が変わる。** **助けの関数は、呼ばれる先が1つなら同じ列挙に入れた。** **(c)と(d)はこの基準を群の中で当てている**——**(c)の`exercise_block_bitmap`は呼ばれる先が1つだが、その1つが(a)の中なので畳まなかった。由来の違う群だからである。**

  **振る舞いを変えていないことは、静的な突き合わせで代えた。** **実行で観測できないためである**——**49箇所のうち、いまの検査のどこかで実際に走るのは1箇所だけだった**（`fs-copy-corrupt-tail-test`。実測）。**残る48箇所は既定でも破壊でも走らない。** **次に整理の段を置く人は、対象を選ぶときにこの比を見ること**——**「切り出しても、切り出す前も後も、動かして確かめられない」対象がある。** 突き合わせは群ごとに毎回行い、**停止の文言の集合が242で完全一致**（切らずに多重集合で比較。**2通りの数え方で一致を見た**）、**停止の箇所数**、**`let _ =`が13**、の3つを見た。**参照は4群とも同じ形で動いた**——**像が1ページ伸び、U/S監査の`kernel entries`が4箇所で1つずつ増えた。導出は機械で確かめてある**（「確かめ方」の2つ目がこの実例である）。**最後に`--full`172項目と`flaky`4項目が緑であることを確かめた。**

  **(2)の着手時に決めること**——`SUPERBLOCK_WANT_EXTRA_ISIZE`と`INODE_EXTRA_ISIZE`の扱い。**S12-f-3であの2つにだけ名前を付けたので、いまはそこだけ揃っていない。****揃え直すか、全部に名前を付けるかを決める。**

  **T3を閉じた。** **着手時に閉じた一覧は3件で、処理したのも3件である**——(1)停止の切り出し49箇所（`cd6f94e`／`79e9b91`／`598abcc`／`a8e5391`）、(2)書く側の算術8/4/4/5箇所（`2a3095b`）、(3)欄の名前14+10箇所（`5ff9ca1`）。**(3)の測り直しは着手時の数と完全一致だった**——(2)が吸ったのは`40`だけで、`+12`／`+14`／`+16`には触れていなかった。**着手後に見つかったものは全部で5行になり、次の整理の段へ回っている**（読む側の算術1行はT3-2で、欄の位置4行はT3-3の測り直しで立てた。`deferred-decisions.md`で相互に指してある）。

  **同じ整理でも、観測の付き方が大きく違った。** **(1)は49箇所のうち実行で確かめられるのが1箇所だけ**で、静的な突き合わせで代えた。**(2)(3)は`common`側なので、ホストのテスト（書く側8関数すべてに最低1つ）と`--full`の破壊項目の両方が同じ経路へ入力を流す。** **次に整理の段を置く人は、対象を選ぶときにこの差を見ること。**

  **条件は3度、実際に効いた。** **「一覧は着手時に閉じる」が**——(a)掃きの時点で近い候補を弾き、(b)(2)のdocが約束した`40`を(3)の一覧が弾き（**約束より一覧が強い**）、(c)(3)の測り直しで見えた欄30箇所超を行に落として次へ回した。**T2が打ち切りで終わった原因への手当てが、機能した記録である。**
- [x] **S13. 永続ブロックストレージ**: **この段の後にCの受け入れを構想している**（`vision.md`の「Cを最小限から順次移植する」。**判断材料であって、S13で実装する根拠ではない**）。一次案はvirtio-blk（QEMU前提で最も素直。確定は着手時判断）。実ストレージではI/O待ちが出るので、BKLを保持したままI/O完了を待たない設計が要る（wait queue、Blocked状態、割り込みによる完了通知）。DMAのため、フレームアロケータに物理連続確保と物理アドレス取得の能力が要る（direct map窓でphys↔virt変換の土台は整っている）。到達条件=実ストレージ上のext2を読み書きできること、ストレージIRQハンドラとBKL保持タスクが循環待ちを作らないこと
  - **S13の締め（完了。清浄な`--full` 189項目の緑で記録）。** 段割りはa=PCI列挙、b=sector読み、c=全像ロード（ADR-0034）、d-1=INTx配送（ADR-0035）、d-2=BKLを解いて眠る（ADR-0036）、e-1〜e-3=全像フラッシュと判定2系統と破壊（ADR-0034のAddendum）。**できるようになったこと**: virtio-blk上のext2を全像で読み書きする（ロード=起動時、フラッシュ=最終形）。完了はINTxで受ける。I/O待ちはBKLを解いて眠る形が成立している（ADR-0036。**最小実演。本番の眠る利用者は未定**）
  - **見込みと違った点が3つある**（行は消さず状態を書き換える）。**(a) S10の締めが予告した「S13で`read_block`のtraitを引く」は実現していない**——全像ロードを採ったので、`Ext2`は像のバイト列を読む形のまま。traitの境界は生まれなかった（ADR-0034の帰結）。**(b) 見込んだ「wait queue、Blocked状態」は作っていない**——d-2はhlt形の最小実演で、本番のflushはポーリングである（起動シーケンスはIF=0・スケジューラ前で眠れない）。**(c) 割り込みによる完了通知は入った（d-1）が、通知で「起こす」者はまだ居ない**（bと同根。眠りの利用者が現れるときに揃う）。**再訪条件の重なりは`deferred-decisions.md`の「S13の締め（再訪条件の重なり）」に1箇所で見える**

### GUI（M6完了後の独立マイルストーン）

粒度は粗いままにする。
カーネル側の責務はフレームバッファへの安全なアクセス・入力イベント・マウス・最小限の共有メモリとIPCまでとし、ウィンドウサーバー・コンポジタ・GUIライブラリ・TTFラスタライズはユーザー空間へ置くことを第一候補とする。
最終判断はM6完了時点のIPCと共有メモリの設計を見てから行う。
カーネルコンソールは起動ログ・診断・パニックのために残し、GUI描画と責務を混ぜない（コンソールのフォントは現行のGNU Unifontのままとする）。
最初のGUIアプリは依存の少ないものから進める（電卓・テキストビューア・簡易エディタ・ファイルマネージャ・ターミナルエミュレータの順が候補）。
ターミナルエミュレータは標準入出力とPTY相当を必要とするので、最初のシェル（S11）はGUIを待たず既存コンソール上で動かす。

---

## 中断可能な区切り
- **M1到達**で「何か画面に映る + ログが取れる」状態になり、他プロジェクトにリソースが移っても死なないプロジェクトになる。
- 以降は各M単位で区切り、到達ごとにADRとロードマップを更新する。

## スコープ外（ロードマップに載せない）
他アーキテクチャ移植 / 動的モジュール / ネットワーク / サウンド。
ネットワークとサウンドは`vision.md`に抽象記述として置く。
SMPはADR-0023でBKLファーストとして「その先」へ移した。ファイルシステムも「その先」へ移した。
着手する場合はまずADRで方針を決めてから。

このうちいくつかは`vision.md`に「その先の構想」として挙げてあるが、構想に挙がっていることと、着手が決まっていることは別である。
ロードマップに載っていないものは、今は手を出さない。
