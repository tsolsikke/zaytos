# 実機の棚卸し——QEMUでしか成り立たない前提（2026-09-22）

**これは材料であって、決定ではない。** **第2段階の設計の入力である**（運用者の指示。2026-09-21）。

**運用者は実機を持っていない。** **この棚卸しを求めた運用者の決定（実機を前提に設計し、VirtualBoxを2台目にする）と、実機の段の設計は`ADR-0068`に在る。** **ここには写さない。**

## 測り方

**3つの出所を分けて書く。** **実測**はQEMUで起こしたもの、**読んだ**はコードを読んで分かったもの、**推測**は確かめていない一般論である。

- **QEMU 8.2.2で、機械の属性だけを変えて既定の像を起こした**（11の変種。`-machine pc`と`q35`、`-m`を256MiBから6GiB、`i8042=off`・`pit=off`・`hpet=off`、`-serial none`）。**像は`1a51869`の既定の構成である。** **起こし方は`tools/qemu-variants.py`に在る**（直した後に同じ変種で起こし直す）。
- **VirtualBoxはホストの属性を読んだだけで、VMは作っていない**（`VBoxManage`の`--version`・`list systemproperties`・`list extpacks`）。**7.2.12がD:に入っていて、拡張パックも7.2.12である。** **ホストはWindows 11 Proで、ハイパーバイザが有効である**（WSL2のため）。
- **`xtask`のQEMUは`-m 256M`で、`-machine`を指定していない**（既定の`pc`＝i440FX）。**実機に近い`q35`と、1GiBを超えるメモリは、これまで一度も通っていない。**

## QEMUの既定の属性と、実機で普通の値

**256MiBと実機の差が、1GiBの壁を隠していた。** **同じ差が他に無いかを、この表で見る**（運用者の指示。2026-09-22）。**「xtaskのQEMU」の欄は`qemu_launch_args`と起動ログから取った。**

| 属性 | `xtask`のQEMU | 実機で普通の値 | 差が隠しうるもの |
|---|---|---|---|
| メモリ | **256MiB**（`-m 256M`） | 4GiBから64GiB（推測） | **1GiBの壁（実測で見つかった）**、4GiBを超えるRAM、メモリマップの記述子の数 |
| 機械（チップセット） | **`pc`＝i440FXとPIIX3**（`-machine`を指定しない。PCIの列挙に`8086:1237`と`8086:7000`） | Q35に近いPCH（PCIe） | ブリッジの先のバス、AHCI |
| ECAM（MCFG） | **無い**（実測。`MCFG tables: 0`） | 在る（推測。PCIeの機械は持つ） | 256バイトを超える構成空間 |
| CPUのモデル | TCGの既定の`qemu64`（QEMUの既定。実測していない）。**一部の検査はKVMの`-cpu host`** | 実物 | CPUIDの機能、TSCの速さ |
| CPUの数 | **1**（一部の検査で2と4） | 4から16以上（推測） | `MAX_CPUS`（2）を超える分。**数えて起こさない形は`--full`で毎回見ている** |
| ファームウェア | **OVMF 2024.02**（Ubuntuの`2024.02-2ubuntu0.9`。セキュアブートの無い版） | 機種ごとのUEFI（推測） | 置き場の配り方、GOPのモード、x2APICで渡すか |
| 画面 | **1280×800、stride 1280、Bgr、物理0x80000000**（実測。std VGAの`1234:1111`） | パネルの解像度（1920×1080から4K）。strideが幅と違うことがある（推測） | 裏バッファの大きさ、転送の量 |
| ディスク | **virtio-blk**（旧来のI/O BAR0を持つ`1af4:1001`） | SATA（AHCI）かNVMe | (B)のとおり、起動が止まる（**`ADR-0068`のHW-dでRAMディスクを足した**） |
| ESP | **QEMUの`fat:rw:`**（ディレクトリをFATに見せる） | GPTのESP（FAT32） | 起動媒体の像 |
| シリアル | **16550（COM1）。QEMUは送る速さを待たない** | 無いか、在っても115,200 baudまで | ログの量の費用（約18秒） |
| i8042・PIT・HPET・8259 | **全部在る**（`pc`の既定） | i8042が無い機種、PITが止まっている機種が在る（推測） | (A)の2行目と3行目（実測） |
| USB | **無い**（制御器を足していない） | xHCI（USBのキーボードとマウス） | 入力が届かない |
| ネットワーク | **e1000**（`8086:100e`。QEMUの既定のNIC） | 在る | 使っていない（スコープ外） |
| 実行 | **TCG**（一部の検査はKVM） | 実物 | 速さと順序の揺れ（`docs/verification-coverage.md`の揺らぎの母数） |

## (A) 実機で止まる・壊れる

| 項目 | 在るか | 実機で何が起きるか | 直す費用（見積もり） | 今か後か |
|---|---|---|---|---|
| **メモリが1GiBを超えると起動しない**（`ADR-0068`のHW-aで直した） | **在る（実測）。** `-m 1G`は通り、`1500M`・`2G`・`6G`は`q35`でも`pc`でも#PFで止まる | **静的な初期ページ表は恒等で[0, 1GiB)しか張っていない。** **OVMFはBootInfoをメモリの上のほうへ置くので、それを最初に読む所で止まる**（`cr2=0x7e66c000`など。目印の`ZAYTBOOT`を読む命令）。**実機もVirtualBoxの普通の構成も、1GiBを超える** | **小さい。** ブートローダがBootInfoとメモリマップの置き場を1GiBの下に取る（UEFIの`AllocateType::MaxAddress`）か、初期ページ表の恒等を4GiBまで広げる。**直した後に6GiBで起こし直し、4GiBを超えるRAMの扱いを確かめる必要がある**（その先は測れていない） | **今。** **VirtualBoxで起動することすらできない。** ページ表か受け渡しに触るのでレビュー必須 |
| **i8042が無いと起動が止まる**（`ADR-0068`のHW-bで直した） | **在る（実測）。** `i8042=off`で`i8042: failed to read the configuration byte (InputBufferStuck); halting` | **状態レジスタが0xFFを返し、入力バッファが空かないと読む。** **ACPIのFADTの`IAPC_BOOT_ARCH`（8042が在るか）を読んでいない**（読んだ） | **小さい。** FADTのビットを見るか、`InputBufferStuck`を「無い」と読んでPS/2の無いまま続ける | **今**（安い。**USBのキーボードだけの機械で起動できない**） |
| **PITが刻まないと黙って止まる**（`ADR-0068`のHW-cで直した） | **在る（実測）。** `pit=off`で較正は上限つきで諦め、PITへ戻り、デモがティックを待ったまま止まる。**`halting`も出ない** | **LAPICタイマの較正の基準がPITである**（読んだ）。**PITの時計を止めている機械では、起動の途中で黙る**（推測。**8254のクロックを止める設定を持つ機種が在る**） | **沈黙を止めるのは小さい**（ティックが来ないことを行に出して止める）。**較正の基準を足すのは中くらい**（HPETかACPIのPMタイマ） | 沈黙は今、代わりの基準は後 |
| HPETが無い | **影響なし（実測）。** `hpet=off`で通る。**HPETを使っていない**（読んだ） | ——— | ——— | ——— |
| シリアルが無い | **止まらない（実測）。** `-serial none`でプロンプトまで進み、画面にも出る | **送信待ちに上限は無く、在るかの確かめも無い**（読んだ）。**ポートが無いとき0xFFが読めるので止まらない**（QEMUで実測。**実機のバスも0xFFを返すのが普通だが、推測**） | 上限を付けるのは小さい | 後（止まらないことを実測した） |
| **シリアルが遅い** | **在る（計算）。** 38,400 baudで、起動ログ564行・69,264バイトを送るだけで約18秒 | **実機のUARTは実際の速さで送る**（QEMUは待たない） | **小さい**（115,200 baudで約6秒） | 実機で起こす段 |
| `q35`で起動する | **通る（実測）。** `q35`・256MiBでプロンプトまで進む | ——— | ——— | ——— |
| GOPのstride | **扱っている（読んだ）。** 描画も裏バッファもstrideで行を送る | ——— | ——— | ——— |
| 画素の並び・解像度 | **並びはRgbとBgrの32bppだけで、それ以外は描画を止めてシリアルで続ける（読んだ）。** **モードは選ばず、GOPの現行モードのまま使う**（読んだ） | **実機の現行モードはパネルの解像度になりうる**（1920×1080や4K。推測）。**裏バッファは大きさから連続で取り、取れなければ画面を止めて続ける**（読んだ）。**4Kで33MiB** | ——— | (C)の遅さと一緒に見る |
| フレームバッファの番地 | **4GiBを超えても表せる（読んだ）。** 張る範囲の計画にフレームバッファの範囲を足している | **実測していない**（QEMUの既定では2GiBに置かれる） | ——— | VirtualBoxで見る |
| MADT | **x2APICの項（種類9）は読む。CPUは2つまで（`MAX_CPUS`=2）で、残りは数えて起こさない**（読んだ。`-smp 4`は`--full`で毎回通る） | **ファームウェアがx2APICを有効にして渡すと、「MMIOでは届かない」と出して先へ進む**（読んだ）。**その先でタイマとSMPがどうなるかは読み切っていない** | 調べるのは小さい | **VirtualBoxのx2APICの設定で見る** |
| CPUIDを確かめずに使う命令 | **見つからなかった（読んだ）。** Local APICと物理アドレスの幅はCPUIDで確かめる。**NX・PAT・1GiBページ・`xsave`は使っていない。** `fxsave`はx86_64の基本 | ——— | ——— | ——— |
| メモリマップ | **張る範囲の表と空きの範囲の表は、どちらも容量256で、超えると止まる**（読んだ）。**QEMUでは5範囲にまとまる**（実測） | **実機の記述子はもっと多い**（推測）。**4GiBを超えるRAMは上の1行目で止まるので、まだ測れない** | ——— | 1行目を直した後に測る |
| APを起こす待ち | **INITからSIPIまでは「ティックが1つ進むまで」で、0〜10msになる**（読んだ。**docは10msと書いている**）。**起動の合図を待つのは50ティック（500ms）までで、超えたら記録して進む**（読んだ） | **規格はINITの後10msを求める**（古いCPU向け。**今のCPUは短くても起きることが多い**。推測） | 小さい（2ティック待てば10ms以上になる） | 後（VirtualBoxで見る） |
| QEMU専用の口 | **既定の起動には無い（読んだ）。** `isa-debug-exit`も`0xE9`も使わない。ポート0x80の`io_wait`だけ | ——— | ——— | ——— |
| **PCIはバス0だけ** | **在る（読んだ）。** ポートI/O（0xCF8）でバス0を列挙する。**virtio-blkは旧来のI/O BAR0だけを探す** | **実機ではAHCI・xHCI・NVMeがブリッジの先のバスに居ることがある**（推測。**Intelの多くはバス0、AMDはブリッジの先が多い**） | 中くらい（ブリッジを辿る列挙） | **USBとAHCIの段の前** |

**VirtualBoxで実地に確かめられるもの**——**メモリの大きさ、シリアルを外した構成、EFIの画面のモードとstride、ACPIの表（MADTとx2APICの設定、HPETの有無）、ディスクの制御器、USBの制御器。** **i8042とPITが無い形はVirtualBoxでは作れない**（どちらも常に在る。推測）——**そちらはQEMUの`i8042=off`と`pit=off`で見る。**

### VirtualBoxのEFIは、CPUが2個と3個だと起動の途中で落ちる（`ADR-0068`のHW-eで実測）

**`!!!! X64 Exception Type - 0D(#GP - General Protection) CPU Apic ID - 00000000 !!!!`が出て、そこで止まる**（`RIP 0x7E50EFD8`。ファームウェアが自分で`CpuDxe.pdb`を指した）。**ZaytOSの行は1行も出ない。**

**こちらの像とは関係が無い**——**ディスクを1つも繋がずに同じ構成で起こしても、同じ所で同じ#GPが出た**（実測）。**ZaytOSが走る前の問題である**（「EFI単体の欠陥」とまでは断定しない）。**この機械のVirtualBoxはHyper-Vの上のNEMで走っている**（`HM: HMR3Init: Attempting fall back to NEM: VT-x is not available`。実測）。

**CPUの数で比べると、1個と4個は起動し、2個と3個は同じ所で落ちる**（実測。ディスク無しで完全停止から2巡。2026-09-23）。**4個ならZaytOSがSMPで起動する**（AP 1が起き、残りは`MAX_CPUS=2`の方針で起こさない）。**VirtualBoxの障害報告#799は「2個で止まり3個以上で起きる」で、こちらと違う**——**同じ族かは未確定である。**

### I/O APICのIDレジスタが、MADTの言う値と食い違う機械がある（`ADR-0068`のHW-eで実測）

**VirtualBoxでは、I/O APICのIDレジスタが0を返すのに、MADTは1と言う**（`apic: the I/O APIC MMIO does not look decoded (version=0x00170020 plausible=true, ID register reports 0 while the MADT says 1, matching=false)`）。**QEMUでは一致する。**

**カーネルは`[ERROR]`を1行出して進む。** **割り込みは実際に届いた**（打鍵がシェルに届いている）。**「プロンプトが出て`[ERROR]`が無い」を判定にしている検査は、この機械では通らない**（`--full`はQEMUで走るので、いまは影響しない）。**持ち越しの行を立てた。**

## (B) 装置が無い

### ディスク——virtio-blkが無いと起動が止まる（`ADR-0068`のHW-dで直した）

**在る（読んだ）。** **virtio-blkが見つからなければ`virtio-blk: no device with an I/O BAR0 was found on bus 0; halting`で止まる。** **ファイルシステムの像は、起動時にvirtio-blkから丸ごとRAMへ写して使い**（`ADR-0034`）、**書いた分は写しに残し**（`ADR-0037`）、**締めでvirtio-blkへ書き戻す。**

**VirtualBox 7.2.12の制御器は、IDE・SATA（AHCI）・SCSI・SAS・NVMe・virtio-scsi・フロッピーである**（実測。`list systemproperties`）。**virtio-blkは無い。** **運用者の推測どおりで、いちばん先に効く。**

**RAMディスクの見積もり**——**ブートローダがESPから像のファイルを読み、置き場をBootInfoで渡す。** **ブートローダは既にESPからカーネルのファイルを読んでいるので、同じ経路で読める。** **カーネルは既に像を丸ごとRAMで使っているので、写す元を差し替えるだけである。** **書き戻しは無くなる**（永続性を失う。**ExitBootServicesの後はUEFIのファイルの口が無い**）。**費用は小さいから中くらい**（ブートローダ・BootInfo・カーネルの分岐・`xtask`がESPへ像を置く）。**VirtualBoxで起動するための最短の道である。**

**次にAHCI**（VirtualBoxにも実機にも在る）。**費用は大きい**（S13のvirtioと同じ桁以上の見込み。PCIのバスの列挙の拡張を含む）。**NVMeは後。**

### USB——`ADR-0068`のHW-fで出す

**今のうちに実装すると決まっている**（運用者の決定）。**実機の段の最後（HW-f）に入れる**（`ADR-0068`。2026-09-22）。**答えることの一覧は運用者の指示（2026-09-21）の「入力」に在る**——**ファームウェアからの引き継ぎ、割り込みの形、ハブ、抜き差し、規模と刻み方。** **PCIのバス0だけの列挙は、ここでも効く**（上の表の最後の行）。

**VirtualBox 7.2.12の拡張パックの説明は「Oracle Cloud Infrastructure integration, PXE ROM」だけである**（実測）。**USBのxHCIは本体に在る見込みで、拡張パックは要らない**（推測。**VMを作って確かめるのは運用者の承認の後**）。

## (C) 遅い

- **フレームバッファがuncached（PCD）で、Write-Combining（PAT）は持ち越しの行に在る**（`docs/deferred-decisions.md`の「Write-Combining / PATの導入」。**契機は「実機で測れるようになった時点」**）。**Seinasは毎フレーム合成するので、ZaytOSへ載せる前に要る見込み**（運用者の見立て）。**4Kの画面では、1枚の転送が1280×800の8倍になる**（計算）。
- **シリアルの38,400 baud**（上の表）。

## PCIと割り込みの能力（実機対応の調査で実測。2026-09-23）

**どれもVMで測った値で、実機では未観測である。** **VMの固定値を、一般の機械の前提にしない。**

| 装置 | QEMU（`pc`・`q35`） | VirtualBox（PIIX3。既定） | VirtualBox（ICH9） |
|---|---|---|---|
| xHCI | MSI-X 16本（MSIは無い）・INTx | INTxだけ | MSI 1本（32ビットの番地）・INTx |
| AHCI | MSI 1本（64ビットの番地）・INTx（`q35`の内蔵） | INTxだけ | MSI・INTx |
| NVMe | MSI-X 65本・INTx | INTxだけ | MSIとMSI-X（2048）・INTx |

- **4GiBの上のBAR**——**QEMU `pc`のxHCIのBAR0は0xC0_0000_0000**（64ビット）。`q35`のNVMeも0xC0_2000_0000。**カーネルの列挙はいまBARの生の値を出すだけで、上位と下位を組んでいない。**
- **bus 0の外**——**`q35`でPCIeのroot portの下に置いたxHCIはbus 1に居て、いまの列挙（bus 0だけ）には出ない。**
- **能力の一覧の指す先が0**——**VirtualBox（PIIX3）のxHCIとNVMeは、状態レジスタが「一覧あり」と言うのに、指す先が0である。**
- **PCIのINTxの行き先**——**VirtualBoxでは割り込み線が10・11なのに、INTA#はI/O APICの20〜23番へ行く**（持ち越しの行を立てた）。
- **xHCIの所有権の受け渡し**——**QEMUにもVirtualBoxにもUSB Legacy Supportの能力が無い**ので、VMでは通らない。**どちらもExitBootServicesの後はHCが止まっている。** **VirtualBoxはファームウェアが設定したDCBAAとコマンドリングの指し先を残している。**
- **I/O APICのID**——**VirtualBoxのMADTはI/O APICのIDをCPUの数と同じ値にし**（CPU 1個で1、4個で4）、**IDレジスタは0のままである。** **それでもIRQ1はI/O APICを通ってベクタ0x42で届く**（VirtualBoxのデバッガのベクタごとの計数で、打鍵4バイトで0x42が+4、8259のベクタ0x21は0回）。

## 起動媒体（`ADR-0068`のHW-eで作った）

**QEMUの既定の起動は、ESPのディレクトリをFATに見せる形である**（`fat:rw:`）。**VirtualBoxにも実機にも、この形は無い。**

**`cargo xtask image`が、GPTとFAT32を入れた66MiBの像（`target/media/zaytos.img`）を建てる**（2026-09-23）。**FATは`xtask`の中で書いた**（`dosfstools`も`mtools`も無く、外の依存を増やさないため）。**分割表は`sfdisk --json`で突き合わせる**（util-linuxはどのLinuxにも在る）。**`media-only`の変種が、この像だけで起動することを`--full`で見る。**

- **VirtualBox**——**GPTの生の像（ESPはFAT32で、`\EFI\BOOT\BOOTX64.EFI`とカーネルと像を置く）を`VBoxManage convertfromraw`でVDIへ変える**（HW-eで採った。**VirtualBox自身の道具である**）。**以前は`qemu-img convert -O vdi`の案だった**が、道具の手持ちで作れる（`qemu-img`と`sgdisk`は在る。実測）。**ISOも作れるが、ISOの道具（`xorriso`）は入っていない。**
- **実機のUSBメモリ**——**同じGPTの生の像を、そのまま書き込める。** **1つの像で両方を賄える。**
- **FATを作る道具（`dosfstools`・`mtools`）は入っていない**（実測）。**`xtask`の中で書くことにした**（HW-e。2026-09-23）。
- **セキュアブートは切る前提である**（運用者の決定）。

## VirtualBoxでの走行を`xtask`に載せるか（費用の見積もり）

**`VBoxManage.exe`はWSLから呼べる**（`/mnt/d/Program Files/Oracle/VirtualBox/`。実測で`--version`が返った）。**載せるなら、VMの作成（`createvm`と`modifyvm --firmware efi`）、シリアルをファイルへ（`--uart1 0x3F8 4 --uartmode1 file`）、像の接続（`storagectl`と`storageattach`）、`startvm --type headless`、画面の読み戻し（`controlvm screenshotpng`）、打鍵（`controlvm keyboardputscancode`）、止めて消す（`controlvm poweroff`と`unregistervm --delete`）の形になる。** **パスはWindowsの形へ直す必要がある**（`wslpath -w`）。**費用は1段の見込み**（起動媒体の像を作る段の後）。

**ハイパーバイザが有効なので、VirtualBoxはHyper-Vの上で動く見込みである**（推測。**VMのログ（`VBox.log`）の`NEM`の行で確かめられる**）。**遅い・一部の機能が制限されることがある**（運用者の推測）。

**VMの作成はホストの状態を変える。** **ホストには既に別のVMが在る。**

## 並べ方の案（今直すもの）

**VirtualBoxで起動するまでに要るのは、1GiBの壁・i8042・ディスク（RAMディスク）・起動媒体の像の4つである。** **i8042はVirtualBoxでは効かないが、実機では効く。**
